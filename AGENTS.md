# 盛和塾运营平台开发约定

## 执行与交付

- 按用户当前目标持续完成已授权工作；“仅审计”“先给建议”“不要修改”等范围限制优先适用。常规实现细节自行判断，保持改动聚焦、可检查、可恢复。
- 仅在缺失信息会实质改变结果、授权范围或不可逆后果，且无法从上下文与检查解决时提问；等待期间继续独立的已授权工作。补充要求只调整受影响步骤。
- 授权、验证和发布采用 [现行工程工作流](docs/engineering-workflow.md)。普通交付默认提交并推送 `main`，普通修复发布完成必要验证后继续；明确的 PR 要求、仓库保护、CI 和发布门禁仍适用。
- 已有有效授权在原对象、动作、环境和范围内沿用。需要新增批准时，先准备可审阅结果和验证证据，说明阻塞动作与规则出处；不得扩大生产数据、权限、密钥或资源变更的授权。
- 完成时报告结果、变更、验证及未完成事项；工具提交成功、测试未运行或等待外部验收不得表述为任务已全部完成。

## 上下文与工具

- 本文件及工程工作流包含仓库可移植的开发规则，不依赖 Git 根目录外的文件自动加载。V1.1 是业务与技术起点；后续已确认决策按其明确范围覆盖，历史报告不构成当前授权。
- 先定位相关代码、测试及文档，再按需读取；避免全量扫描文档、技能引用、部署副本或记忆。独立只读检查可并行，依赖操作与共享状态修改顺序执行。
- 技能按任务选择：普通发布使用 `.agents/skills/platform-release/SKILL.md`；数据库结构验证使用 `.agents/skills/platform-db-validation/SKILL.md`。普通编辑不要求加载发布技能。
- 工具失败后按证据诊断；无新信息不重复调用。长任务查看进度后继续等待；外部写操作结果不确定时先核验状态再决定重试。
- 本地统一记忆库为 `E:\盛和塾运营系统\Codex记忆`，三个组件共用。项目背景相关任务按需读取索引、映射、概览和相关未完成事项；自包含任务可跳过。路径不可用时使用仓库文档并报告必要缺口，不猜测其他记忆库。
- 不在组件内创建记忆库。仅用户明确要求归档时按统一记忆协议处理；`docs` 保存工程文档。历史部署副本中的记忆不得作为当前来源。

## 业务与安全不变量

- 新业务事实使用组织 ID，名称仅用于兼容映射；未确认业务口径使用配置、功能开关或草稿并记录缺口。
- 服务端校验权限；普通列表、导出、日志、缓存及 AI 输入不含完整手机号。密钥、真实完整手机号、精确企业敏感财务和关怀备注不得进入代码、测试夹具或对话。
- 敏感访问、权限变更、导出、目标修改、人工覆盖、电话和走访保留审计。
- 保持现场签到扫码链路的独立部署与可用性；不得借普通优化改变签到规则、生产数据或 IAM 写入门禁。
- 生产直接数据库访问、敏感数据处理、迁移、权限变更、密钥轮换和费用增加执行前核验对应的明确授权。普通发布授权不包含这些动作。
- 数据库结构变化提供前向脚本及安全回滚方案；无法无损回滚时提供拒绝条件、备份恢复或前向修复方案，不强制破坏性降级。

## 验证入口

- 按改动影响选择检查，完成 CI 要求；通过后仅因新增修改、失败或未解决风险扩大或重复验证。具体矩阵见工程工作流。
- API：在 `apps/platform-api` 的隔离测试环境运行 `python -m pytest <相关测试> -q`。
- 管理端：在 `apps/admin-web` 运行 `pnpm typecheck`、`pnpm test`；影响构建时运行 `pnpm build:staging`。
- 小程序：仓库根运行 `node scripts/run_node_tests.mjs miniprogram`；配置或源文件变化时运行 `node scripts/check_wechat_miniprogram.mjs`。
- `pnpm lint` 当前包含自动修复，不能当作只读检查；使用具体工具的检查模式及相关路径。
