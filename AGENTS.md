# Codex 开发约束

本仓库以 `docs/baseline` 中的 V1.1 文档为最终开发基线。

## 禁止事项

- 不得连接、读取、修改或导出生产数据库。
- 不得执行生产部署、生产迁移或生产密钥轮换，除非业务负责人在当前任务中明确批准。
- 不得把密钥、令牌、密码、完整手机号、精确企业财务或敏感关怀备注写入代码、日志、测试夹具或对话。
- 不得修改现有现场签到扫码链路的独立部署与可用性。
- 不得用前端菜单隐藏代替服务端权限校验。
- 不得把未确认的业务口径写死；使用配置、功能开关或草稿状态并记录待确认事项。

## 必须事项

- 一次只完成一个可验收任务，保持变更可测试、可查看、可回滚。
- 所有新业务事实使用组织 ID；文本名称仅作为兼容映射。
- 普通列表、普通导出、日志、缓存与 AI 输入不得包含完整手机号。
- 敏感访问、权限变更、导出、目标修改、人工覆盖、电话和走访必须审计。
- 数据库结构变化必须提供前向与回滚脚本。
- 完成前运行对应单元、集成、权限、隐私和数据核对测试。

