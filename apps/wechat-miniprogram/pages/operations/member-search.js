const { request } = require("../../utils/request");
Page({ data:{ name:"", loading:false, results:[], errorMessage:"" }, handleInput(event){this.setData({name:event.detail.value});}, async search(){ const name=(this.data.name||"").trim(); if(!name){wx.showToast({title:"请输入学长姓名",icon:"none"});return;} this.setData({loading:true,errorMessage:"",results:[]}); try{const response=await request(`/api/v1/wechat/operations/member-search?name=${encodeURIComponent(name)}`,{auth:true});this.setData({results:response.data||[]});}catch(error){this.setData({errorMessage:error.message||"查询失败"});}finally{this.setData({loading:false});} } });
