# V1.2-B2.5 人工验收卡

状态：**NOT REQUESTED / WAITING FOR RUNTIME IDENTITY DECISION**

本轮没有要求业务负责人登录或操作。清理函数尚未部署，生产运行身份、服务端 Token、API 地址和费用边界尚未确认；不能让业务人员在信息不完整时自行找入口或猜凭证。

## 未来受控云端验收（由实施人员填写后才可使用）

| 项目 | 当前值 |
| --- | --- |
| 环境 | 待批准的 CloudBase 测试/受控环境；不得直接使用真实生产业务数据 |
| API 地址 | 待部署后填写，并确认使用 HTTPS 精确内部路径 |
| 管理端网址 | 本轮不需要；如同时做管理端图片查看，填写同一测试环境网址 |
| 登录账号 | 不需要业务登录清理函数；若做管理端查看，使用隔离测试账号 |
| 登录密码/获取方式 | 不写入文档或聊天；由实施人员通过受控凭据入口现场提供 |
| 微信测试身份 | 不需要；本轮只验证服务端合成对象 |
| 测试姓名/手机号 | 不使用；不得绑定真实学员 |
| 测试数据 | 合成对象和隔离记录，图片不含真实人脸 |
| 运行 Token | 仅由云端安全配置注入，业务人员不可见 |
| 测试对象前缀 | `study-meetings/<environment>/` 下的隔离对象；不得使用其他业务前缀 |
| 调度时间 | 部署前确认 Cron 时区；默认模板为每天一次 |

## 实施人员先完成

1. 证明 CloudRun 运行实例可以使用已批准的同环境临时身份访问目标私有存储。
2. 证明 API 与 Cron 函数使用同一服务身份 Token，Token 不出现在日志、浏览器、小程序或报告中。
3. 只用合成对象验证上传、读取、替换、过期拒读、删除和重复清理。
4. 验证删除失败只留下可重试记录，不影响其他对象。
5. 验证非 `study-meetings/` 前缀不能删除。
6. 记录函数调用、错误数和费用读数，不记录图片内容、密钥或完整手机号。

## 业务人员（再次批准后）实际操作

本轮原则上不需要业务操作。若后续需要确认管理端显示：

1. 登录实施人员提供的隔离测试管理端。
2. 打开指定合成学习会记录。
3. 预期能在权限范围内临时查看合成图片。
4. 将测试记录标记为测试取消或由实施人员清理；不转为正式学员，不发积分。

## 预期结果

- 到期前，受权限保护的后端可以查看合成图片。
- 到期后 API 拒绝查看，即便物理删除尚未完成。
- 清理成功后对象不可读取，`storage_deleted_at` 和审计存在。
- 第二次清理不报业务错误、不重复产生有效记录。
- 学习会、名单、课程、组织关系和积分不发生变化。

## 禁止操作

- 不上传真实学员或真实学习会照片；
- 不把 Token、SecretId、SecretKey、SessionToken 或永久 URL 复制到聊天、Git、浏览器或小程序；
- 不执行生产 migration、生产 Feature Flag 开启或正式小程序发布；
- 不创建独立 COS、资源包、CDN 或付费调度服务；
- 不删除非 `study-meetings/` 前缀的任何对象；
- 不把测试人员转换为正式学员，不产生积分。
