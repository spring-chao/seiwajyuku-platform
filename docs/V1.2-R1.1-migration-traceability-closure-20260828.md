# V1.2-R1.1 Migration Traceability Closure

日期：2026-08-28（Asia/Shanghai）
仓库：`seiwajyuku-platform`
代码评审基线：`f3a041d6b032621a29590603c3f24f0dd4af834c`
当前文档基线：`5da5428dc11c3e280cddeb4ef2d85f576971b2cc`

## 结论

> **R1.1 MIGRATION TRACEABILITY CLOSURE PASS (PROVENANCE CAVEAT RECORDED)**

`0037`、`0038` 的原始 SQL 内容已找回并与生产结构/执行记录相符；本次只读核验没有执行任何生产写操作。恢复文件已在闭环提交 `8bc214bd38147b556b380799e18e483ab783e132` 纳入 `main`，现在干净 clone 能取得完整的 `0035`～`0042` 双数据库迁移链。

历史事实必须保留为两层：

1. `0037`、`0038` 确实已经在生产执行，不能重跑；
2. 它们当时没有对应的 Git commit，不能伪造历史 SHA。新的闭环提交只代表“恢复并固定内容”的当前证据，不代表原始部署 commit。

完整机器可读清单见：[V1.2-R1.1-migration-manifest-20260828.json](./V1.2-R1.1-migration-manifest-20260828.json)。

## 生产只读状态（2026-08-28）

生产 `schema_migrations` 共 36 行：

- 已执行：`0037_member_profile_business_fields.sql`（2026-08-27 08:22:27）、`0038_member_social_role.sql`（2026-08-27 09:14:22）；
- 待补齐：`0035`、`0036`、`0039`、`0040`、`0041`、`0042`；
- `members` 的 0037 八个字段及 0038 的 `social_role` 均已只读确认存在且允许 NULL。

因此后续受控生产迁移路径必须是：

```text
0035 → 0036 → 跳过已存在的0037/0038 → 0039 → 0040 → 0041 → 0042
```

每一步仍需独立核验；不得为了补齐 Git 文件而重新执行 `0037/0038`。

## 0037/0038 内容恢复证据

### 0037

- `member-profile-release-20260827/migrations/mysql/0037_member_profile_business_fields.sql` 与当前恢复文件 SHA-256 完全一致：`94139c210cffd269c37fa94e5d814811b4b38e86486fb704928cc7632c18db9f`；
- SQLite/rollback 文件也与该发布快照逐字一致；
- 历史 Codex 会话 `C:\Users\WGC\.codex\sessions\2026\08\18\rollout-2026-08-18T15-24-58-01a013c2-5881-73e1-be9f-e3d1f92cc623.jsonl` 的 7660-7661 行保存了原始创建 patch；
- 同一会话 8105-8123 行保存了生产执行后 `schema_migrations` 和字段的只读核验。

### 0038

- 同一历史会话 8388-8389 行保存了 MySQL/SQLite forward 与 rollback 文件的原始创建 patch；
- 同一会话 8514-8515 行保存了生产执行及 `schema_migrations`、`social_role` 字段的只读核验；
- 当前恢复文件 SHA-256：MySQL `ea4dc3a6ed31d7d87b80f8c5878b6fa002ffe17fbdd7dbd0b125bf29643785b8`，SQLite `3e4f1457b2a914516f445846630840ffcb529ce87f5f9c35f4d65f587735688b`。

这两项归类为 `recovered-original`，不是 reconstructed；但由于当时没有 Git commit，历史 Git SHA 保持 `null`。

## Manifest 规则

- MySQL 和 SQLite forward、rollback 文件分别记录路径与 SHA-256；
- 已在 `main` 的迁移记录其最后一个源码提交 SHA；
- `0037/0038` 在闭环提交前标记为 `recovered-untracked-before-closure`，现已由闭环提交 `8bc214bd38147b556b380799e18e483ab783e132` 固定为可追溯的当前文件；
- `production.status` 只记录本次只读观察到的 `APPLIED` 或 `MISSING`，不把“Git 中存在”误写成“生产已执行”；
- SHA-256 按当前 UTF-8/LF 文件字节计算，避免把工作区换行转换误当成内容变化。

## 安全边界

本阶段仅查 Git、历史工作区/发布快照、历史会话记录和生产只读接口；没有执行生产迁移、CloudRun 发布、Feature Flag 修改、Cron 创建或业务数据写入。

R1.1 闭环后，下一阶段才是 R1.2：查清 revision `188` 的 image digest/build/source。R1.1 不解除 V1.2 的生产 NO-GO。
