# V1.2-R1.2 Revision 188 Traceability Review

日期：2026-08-28（Asia/Shanghai）
仓库：`seiwajyuku-platform`
范围：生产 revision `188` 的只读来源追溯
前置状态：R1.1 已通过（迁移 `0037/0038` 证据链已闭合）

## 结论

> **V1.2-R1.2 REVISION TRACEABILITY BLOCKED**

本次已经证明生产 revision `188` 当前运行的 CloudBase 部署记录、BuildId、镜像标签和镜像 digest；也下载并比对了运行包内容。但现有腾讯云部署记录和构建日志没有保存可验证的 Git commit，下载包内的 `.git` 仅指向一个已经不存在的工作区，因而不能把 revision `188` 精确归因到某一个 commit，也不能建立完整的 `commit → CI → image → revision` 证据链。

在这个缺口补齐前，V1.2 生产状态继续保持 **NO-GO**。本次没有执行生产迁移、流量调整、环境变量修改、Cron 创建、Feature Flag 修改或业务数据写入。

## 生产只读事实

查询时间：2026-08-28 12:42:49（Asia/Shanghai）

| 项目 | 只读结果 |
| --- | --- |
| CloudBase 服务 | `seiwajyuku-platform-api` |
| 当前在线版本 | `seiwajyuku-platform-api-188` |
| 状态 | `normal` |
| 流量 | `100%` |
| 部署记录 | `188` |
| 部署时间 | `2026-08-27 17:16:00` |
| BuildId | `2602046097` |
| 上传方式 | `package` |
| 包名 | `seiwajyuku-platform-api.zip` |
| 包版本 | `1787822154` |
| 镜像标签 | `ccr.ccs.tencentyun.com/tcb-100050666336-puns/ca-tcluygqu_seiwajyuku-platform-api:seiwajyuku-platform-api-188-20260827171605` |
| 镜像 digest | `sha256:db1c3c27dc9bc592c1427a1774d70ead648d8d01835eeeb7736eaec2a37d0fe4` |

镜像 digest 来自 `DescribeCloudRunBuildLog` 对 BuildId `2602046097` 的构建日志；部署记录来自 TCBR API `DescribeCloudRunDeployRecord`，不是根据页面显示或推测得出。

## 来源追溯证据

### CloudBase 部署元数据

`DescribeCloudBaseRunServerVersion` 能返回版本、包和镜像信息，但其 source 字段为空：

- `branch = ""`
- `repo = ""`
- `repo_type = ""`

构建日志包含最终镜像 digest，但没有 40 位 Git SHA、GitHub 仓库 URL 或可用于提交归因的 source label。当前仓库也没有一个把该 BuildId 与 GitHub Actions run 绑定起来的部署工作流；现有 CI 主要生成 `:ci` 镜像标签，不能反向证明生产 BuildId 的源提交。

### 运行包比对

只读下载 revision `188` 的运行包，共 697 个文件；对其中仓库中可追踪的 `apps/platform-api/app`、`migrations`、`data/learning-plans` 文件按 LF 规范化后与 Git blob 比对：

| 候选提交 | 完全一致 | 不一致 | 缺失 |
| --- | ---: | ---: | ---: |
| `16b5166b` | 207 | 3 | 0 |
| `014b258f` | 207 | 3 | 0 |
| `004d1581` | 205 | 5 | 0 |

与最接近的 `16b5166b` 比对时，3 个差异文件为：

- `apps/platform-api/app/api/members.py`
- `apps/platform-api/app/services/enrollment.py`
- `apps/platform-api/app/services/members.py`

这 3 个文件的差异对应当时工作区中尚未形成可追溯提交的业务改动（V1.1.1 结构化登记字段及后续志工任职接口）。因此内容相似性可以帮助缩小候选范围，但不能替代“部署时实际 commit”的证明。

运行包根目录的 `.git` 是一个 gitfile，指向已删除的本地工作区；该指针没有可读取的 HEAD，Git 历史和 reflog 也没有留下可恢复的分支或提交。因此不能把 `188` 直接标成 `16b5166`、`014b258f` 或其他候选提交。

### CI 证据边界

GitHub Actions `33048736229` 证明 `16b5166b` 在 `main` 上 CI 成功；类似地，`014b258f`、`004d1581` 等候选提交也各有成功 CI。但没有证据表明 BuildId `2602046097` 使用了其中任何一个 SHA 构建，故不能把这些 CI 结果当作 revision `188` 的来源证明。

## R1.2 门禁结果

| 门禁 | 结果 | 说明 |
| --- | --- | --- |
| revision `188` 当前状态/流量 | PASS | `normal`，100% 流量 |
| BuildId 与镜像 digest | PASS | `2602046097` → `sha256:db1c…d0fe4` |
| 运行包可下载并可比对 | PASS | 697 个文件已下载，候选内容高度接近 |
| 精确 Git source commit | **BLOCKED** | 部署元数据无 source，包内 git 指针无有效 HEAD |
| commit → CI → image → revision | **BLOCKED** | 缺少 BuildId 与 CI/commit 的绑定证据 |
| 线上公开 build info | **BLOCKED** | 当前 API 没有 `/build-info.json` 或 `/api/version` |
| 生产写入 | PASS（未发生） | 未执行迁移、发布、流量或业务数据写入 |

## 解阻建议（本次不执行）

下一次候选发布应从干净的 `main` checkout 构建，并在构建、镜像和部署记录中同时保留不可变 provenance：

1. API 提供只读 `/build-info.json` 和 `/api/version`，至少返回 `commit_sha`、`build_time`、`image_digest`、`build_id`、`environment`；不得返回任何 Secret。
2. CI/CD 在镜像 label、构建 artifact 和部署 manifest 中注入同一组 provenance 字段，并保留 `commit → CI run → BuildId → image digest → revision` 映射。
3. CloudBase 发布包不再从未提交工作区生成；发布前检查工作区干净、commit 已推送、CI 成功且 manifest 固定。
4. 如需把 revision `188` 纳入 V1.2 生产基线，应先取得原始部署包/构建 provenance；若无法取得，应在明确批准后从干净 `main` 重新发布一个可追溯 revision，再把它作为新的生产回滚基线。

以上属于后续解阻方案，本轮没有重新部署，也没有改变现有生产运行状态。

## 当前状态

```text
R1.1 migration traceability       PASS
R1.2 revision 188 traceability    BLOCKED
V1.2 production                   NO-GO
production migrations             unchanged
production traffic                unchanged (revision 188, 100%)
production feature flags          unchanged
production Cron                   not created
production business data          unchanged
```
