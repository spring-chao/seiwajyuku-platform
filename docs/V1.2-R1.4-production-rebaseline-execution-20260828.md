# V1.2-R1.4 生产重新基线执行记录

日期：2026-08-28（Asia/Shanghai）
状态：`RELEASE STOPPED`
批准范围：`R1.4 EXECUTION APPROVED — GATED`

## 1. 停止结论

本次执行在第一道生产快照门禁停止。已按批准范围尝试创建最新生产 MySQL 快照，但腾讯云返回：

```text
OperationDenied.ServerlessInstanceStatusDenied
[CreateBackup] serverless实例当前状态pause不允许该操作。
```

由于没有取得新的、可用的 `PRE_R1_4_BACKUP_ID`，按 R1.4 硬门槛不得继续构建、部署、切流量或生成生产 release manifest。此次没有执行迁移、镜像部署、流量调整、Feature Flag 修改、Cron 创建或 V1.2 业务写入。

## 2. 执行尝试

### 2.1 生产快照

执行的唯一生产写操作是经批准的快照创建请求（没有执行 DDL 或业务写入）：

```text
tcb db backup create -e shengheshu-d2g2zyyl99f6c6fc2
  --type snapshot
  --name pre-v1.2-r1.4-rebaseline-20260828
  --json
```

结果：`FAILED / OperationDenied.ServerlessInstanceStatusDenied`。CloudBase request ID 已保留在本次操作日志中，不写入 Secret。

失败后再次只读查看备份列表，没有出现新备份：

| 项目 | 结果 |
| --- | --- |
| 最近备份 ID | `10313270` |
| 名称 | `pre-member-social-role-20260827` |
| 类型/状态 | `snapshot / success` |
| `IsUsable` | `yes` |
| 完成时间 | `2026-08-27 17:13:39` |
| 本次 `PRE_R1_4_BACKUP_ID` | 未取得 |

`10313270` 是历史快照，不能替代本次窗口要求的最新快照。

### 2.2 实例只读状态

目标为生产 MySQL 8.0 实例 `cynosdbmysql-ins-ogsjrqq9`，区域 `ap-shanghai`。快照失败时腾讯云错误明确报告 Serverless 状态为 `pause`；随后只读查询显示：

```text
Status: running
ServerlessStatus: resume
IsFreeze: yes
UpdateTime: 2026-08-28 14:24:11
```

状态变化不等于快照已经成功，且本次未执行重启、扩容、解除冻结或其他实例修改。

### 2.3 生产服务与数据库只读核验

快照门禁失败后，仅做了只读核验：

| 检查 | 结果 |
| --- | --- |
| `/__tcb_probe__` | HTTP 200，`status=ok` |
| `/health/live` | HTTP 200，`status=ok` |
| `/health` | HTTP 200，`status=ok` |
| `/api/v1/health` | HTTP 200，`status=ok` |
| `/api/v1/system/environment` | HTTP 200，生产环境；既有运行策略未修改 |
| `/api/v1/system/build-info` | HTTP 404；当前仍为旧 revision 188 的预期现状 |
| `schema_migrations` | 36 条，`MAX(version)=0038_member_social_role.sql` |

当前 API 仍为 revision `188`、100% 流量；本次没有改变流量或 revision。revision 188 的 BuildId/digest 及“source commit historically unprovable”结论继续以 R1.2 报告为准。

## 3. 未执行项目

以下项目均未执行：

- `0035`、`0036`、`0039`、`0040`、`0041`、`0042`；也未重跑 `0037/0038`；
- 生产 API/管理端构建或 CloudBase deploy；
- canary `5% → 25% → 100%` 或任何流量调整；
- `APP_*` provenance 运行时配置修改；
- V1.2 Feature Flags、Cron、身份绑定、学习会/合影、积分或组织关系写入；
- 最终生产 release manifest 生成；
- revision 188 删除、覆盖或回滚。

## 4. 恢复执行前置条件

下一次窗口不得直接复用本次失败结果，必须重新完成：

1. 由有权限的云平台负责人确认生产 Serverless MySQL 恢复到允许备份的运行状态；如需解除暂停/冻结或产生费用，先取得单独批准；
2. 创建并确认一份新的生产快照，记录 `backup_id`、完成时间、`IsUsable=yes` 和 PITR；
3. 重新冻结 clean `origin/main` 的 release SHA，核对 CI、migration manifest 和源树指纹；
4. 重新核验 revision 188、健康端点、流量和回滚可用性；
5. 只有上述门禁全部通过，才可重新进入构建和受控灰度步骤。

本记录不把 Serverless 实例自动从 `pause` 转为 `resume` 视为人工修复，也不授权任何人绕过快照门禁。R1.4 仍为 `EXECUTION BLOCKED / NOT COMPLETE`，V1.2 生产功能继续 `NO-GO`。
