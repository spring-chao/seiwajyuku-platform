# V1.2-R1.4.1 Serverless Backup Capability Recovery

日期：2026-08-28（Asia/Shanghai）
状态：`BACKUP GATE BLOCKED`
范围：生产只读恢复预审；未执行 Resume、解冻、计费/资源配置、迁移、部署或业务写入。

## 1. 结论

R1.4.1 只读核验确认：生产 MySQL 的 Serverless 计算状态已经显示为 `resume`，但集群仍为 `IsFreeze=yes`。上一轮创建最新生产快照仍因实例处于 `pause` 而被腾讯云拒绝；当前没有新的 `PRE_R1_4_BACKUP_ID`，因此 R1.4 不能重新开始。

```text
V1.2-R1.4.1 PRODUCTION BACKUP GATE: BLOCKED
R1.4 Attempt #2: NOT AUTHORIZED
V1.2 production: NO-GO
```

`ServerlessStatus=resume` 不能替代 `IsFreeze=no`，也不能证明 CreateBackup 已可用。本轮不把自动恢复任务视为人工解除冻结或快照成功。

## 2. 只读实例与集群状态

目标生产环境：`shengheshu-d2g2zyyl99f6c6fc2`，区域：`ap-shanghai`。目标 MySQL 8.0 集群/实例：

```text
ClusterId:  cynosdbmysql-1u9lqtf0
InstanceId: cynosdbmysql-ins-ogsjrqq9
```

最近一次 `DescribeClusterDetail` 只读响应（请求时间约 2026-08-28 15:25，RequestId `2c4fbcd4-bf83-4633-b696-1d978d0494c5`）的关键字段：

| 字段 | 结果 | 判断 |
| --- | --- | --- |
| `Status` | `running` | 基础实例状态正常 |
| `ServerlessStatus` | `resume` | 计算状态已恢复显示 |
| `IsFreeze` | `yes` | **仍是阻断项** |
| `Tasks` | `[]` | 当前没有集群任务 |
| 实例 `InstanceStatus` | `running` | 基础实例状态正常 |
| 实例 `ServerlessStatus` | `resume` | 与集群状态一致 |
| `ProcessingTask` | 空 | 当前没有处理任务 |
| `PitrType` | `redo_pitr` | PITR 类型存在 |
| `Ability.IsSupportManualSnapshot` | `yes` | 产品能力支持，但当前状态不允许实际创建 |

`IsFreeze` 的原因没有在本次 API 返回中给出。不能从 `yes` 单独推断欠费、资源策略、平台冻结或其他原因；需要有权限的腾讯云控制台/账号负责人查看实例详情和账单/资源提示。

## 3. 上次快照失败与操作追踪

### 3.1 CreateBackup

执行的批准操作返回：

```text
OperationDenied.ServerlessInstanceStatusDenied
[CreateBackup] serverless实例当前状态pause不允许该操作。
requestId: d46550b8-b84a-42d3-91a2-163ea547a5d2
```

失败后 `db backup list` 未出现新快照。最近可用快照仍是：

| 字段 | 值 |
| --- | --- |
| `BackupId` | `10313270` |
| 名称 | `pre-member-social-role-20260827` |
| 状态 | `success` |
| `IsUsable` | `yes` |
| 完成时间 | `2026-08-27 17:13:39` |

该快照属于历史窗口，不能替代 R1.4 Attempt #2 的新快照门禁。

### 3.2 任务列表

只读调用 `DescribeTasks`（按目标 `ClusterId`、时间窗口过滤）返回的最近任务均为自动 Serverless 状态切换，未产生成功的手工备份任务：

| 任务时间 | 类型 | 状态 | 说明 |
| --- | --- | --- | --- |
| 2026-08-28 15:18:29 | `taskAutoResumeServerless` | `success` | 自动恢复 |
| 2026-08-28 15:02:15 | `taskAutoPauseServerless` | `success` | 自动暂停 |
| 2026-08-28 14:24:11 | `taskAutoResumeServerless` | `success` | 自动恢复 |

任务查询 RequestId：`0e4bca0e-3220-41d8-9fad-9a0cf57ef39c`。上次 CreateBackup 被拒绝没有对应的成功 `taskManualBackup` 记录。

## 4. PITR 与数据库/API健康

### 4.1 PITR

`DescribeRollbackTimeRange` 只读结果（RequestId `7f85e7ad-22ae-4653-8f7d-7dd26e32bada`）：

```text
TimeRangeStart: 2026-08-21 15:25:11
TimeRangeEnd:   2026-08-28 15:25:10
```

PITR 当前窗口可读，约覆盖 7 天；但它不能替代一份新的、可用的发布前快照。

### 4.2 数据库与 API

只读 SQL：

```text
SELECT 1 AS ok, MAX(version) AS max_version,
       COUNT(*) AS migration_count
FROM schema_migrations;
```

结果：`ok=1`、`max_version=0038_member_social_role.sql`、`migration_count=36`。本轮没有改变数据库。

生产 API 只读检查（约 2026-08-28 15:24:53）：

| 路径 | 结果 |
| --- | --- |
| `/__tcb_probe__` | HTTP 200，`status=ok` |
| `/health/live` | HTTP 200，`status=ok` |
| `/health` | HTTP 200，`status=ok` |
| `/api/v1/health` | HTTP 200，`status=ok` |
| `/api/v1/system/environment` | HTTP 200，既有生产运行策略未修改 |
| `/api/v1/system/build-info` | HTTP 404，当前 revision 188 的历史现状 |

当前 API 仍为 revision `188`、100% 流量；没有发生部署、流量、Flag、Cron 或业务数据变化。

## 5. R1.4.1 验收门禁

| 门禁 | 结果 |
| --- | --- |
| `Status=running` | PASS |
| `ServerlessStatus=resume` | PASS（仅说明计算状态显示恢复） |
| `IsFreeze=no` | **BLOCKED：当前仍为 `yes`** |
| `ProcessingTask`/`Tasks` 无运行任务 | PASS |
| 数据库只读连接 | PASS |
| PITR 可读 | PASS |
| 新快照 `success + IsUsable=yes` | **BLOCKED：未创建** |
| 新 `PRE_R1_4_BACKUP_ID` | **BLOCKED：未取得** |
| 生产结构、revision、流量、Flags、业务数据未改变 | PASS |

因此不能输出 `PRODUCTION BACKUP GATE READY`。

## 6. 恢复执行前置条件

必须由有权限的云平台负责人先在腾讯云控制台/运维通道查明并处理 `IsFreeze=yes` 的真实原因。若涉及 `ResumeServerless`、解除冻结、自动暂停策略、计费或资源配置，须作为单独基础设施变更批准，不能沿用本次 R1.4 执行批准。

处理后必须重新只读确认：

```text
Status          = running
ServerlessStatus= resume
IsFreeze        = no
ProcessingTask  = none
Tasks           = []
```

然后在新的窗口创建一份新的生产快照，记录：

```text
backup_id       = <new id>
BackupStatus    = success
IsUsable        = yes
PITR            = verified
```

新快照成功后，第一次 R1.4 执行批准不再自动复用。必须重新冻结 clean `origin/main` SHA、核对 CI 和 migration manifest、确认 revision 188/健康状态，并重新取得：

```text
R1.4 EXECUTION APPROVED — GATED / ATTEMPT #2
```

在此之前继续保持：不执行生产迁移、不部署新 revision、不切流量、不改 Feature Flags、不创建 Cron、不写 V1.2 业务数据。
