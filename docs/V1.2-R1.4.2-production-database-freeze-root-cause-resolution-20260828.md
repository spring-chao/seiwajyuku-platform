# V1.2-R1.4.2 Production Database Freeze Root Cause Resolution

日期：2026-08-28（Asia/Shanghai）
状态：`IN PROGRESS / EXTERNAL ACTION REQUIRED`
前置：R1.4 Attempt #1 在生产快照门禁停止

## 1. 范围与红线

本阶段只解决生产 TDSQL-C MySQL Serverless 集群的 `IsFreeze=yes` 原因和快照能力恢复问题。

明确禁止：

- 不调用 `ResumeServerless`；
- 不修改数据库数据、表结构、迁移记录或权限；
- 不修改规格、自动暂停策略、计费/超限策略或其他资源配置；
- 不部署 API/管理端，不替换 revision 188，不切流量；
- 不修改 Feature Flags，不创建 Cron，不写身份、学习会、合影、积分或组织关系数据；
- 不使用历史快照 `10313270` 绕过最新快照门禁。

本阶段不是 V1.2 上线，也不是 R1.4 Attempt #2。

## 2. 当前只读事实

生产环境：`shengheshu-d2g2zyyl99f6c6fc2`，区域：`ap-shanghai`。目标 MySQL 8.0 集群和实例：

```text
ClusterId:  cynosdbmysql-1u9lqtf0
InstanceId: cynosdbmysql-ins-ogsjrqq9
```

最近一次只读状态：

```text
Status           = running
ServerlessStatus = resume
IsFreeze         = yes
ProcessingTask   = empty
Tasks            = []
```

集群详情同时报告 `Ability.IsSupportManualSnapshot=yes`，但实际创建快照仍被状态门禁拒绝。`IsFreeze` 的 API 返回没有给出原因，不能从该字段推断欠费、资源、平台策略或其他后台状态。

上一次 CreateBackup 返回：

```text
OperationDenied.ServerlessInstanceStatusDenied
[CreateBackup] serverless实例当前状态pause不允许该操作。
RequestId: d46550b8-b84a-42d3-91a2-163ea547a5d2
```

失败后备份列表没有新增记录，最近可用快照仍为：

```text
BackupId:   10313270
Status:     success
IsUsable:   yes
FinishTime: 2026-08-27 17:13:39
```

该快照不是本次发布窗口快照，不能作为 R1.4 Attempt #2 的门禁凭证。

PITR 只读窗口当前为：

```text
2026-08-21 15:25:11 → 2026-08-28 15:25:10
```

数据库只读查询结果仍为：

```text
ok=1
max_version=0038_member_social_role.sql
migration_count=36
```

生产 API `/__tcb_probe__`、`/health/live`、`/health`、`/api/v1/health` 均 HTTP 200；当前服务仍为 revision 188、100% 流量。以上均未被本阶段修改。

## 3. Serverless 自动任务证据

按目标集群查询任务列表，最近任务均为自动暂停/恢复：

| 时间 | 任务类型 | 状态 |
| --- | --- | --- |
| 2026-08-28 15:18:29 | `taskAutoResumeServerless` | `success` |
| 2026-08-28 15:02:15 | `taskAutoPauseServerless` | `success` |
| 2026-08-28 14:24:11 | `taskAutoResumeServerless` | `success` |

没有成功的 `taskManualBackup` 对应本次失败请求。自动恢复任务不等于解除冻结，也不等于快照能力已经恢复。

## 4. 需要腾讯云权限人员处理的最小事项

请有腾讯云控制台/账号权限的负责人或腾讯云支持，根据 CreateBackup RequestId 查询后台原因：

```text
生产 TDSQL-C MySQL Serverless 集群：
ClusterId = cynosdbmysql-1u9lqtf0
InstanceId = cynosdbmysql-ins-ogsjrqq9
Region = ap-shanghai

当前只读状态：
Status=running
ServerlessStatus=resume
IsFreeze=yes
ProcessingTask=empty
Tasks=[]

创建手工快照时返回：
OperationDenied.ServerlessInstanceStatusDenied
serverless实例当前状态pause不允许该操作
CreateBackup RequestId:
d46550b8-b84a-42d3-91a2-163ea547a5d2

请确认：
1. IsFreeze=yes 的准确原因；
2. 是否为账号/计费、资源冻结、Serverless 状态或平台侧异常；
3. 在不改数据库数据、不变更规格的前提下，如何恢复为 IsFreeze=no；
4. 恢复后是否可以创建手工 snapshot；
5. 若必须修改计费、自动暂停或资源配置，先说明预计费用和影响，不要直接操作。
```

本项目当前没有腾讯云账号操作插件或可授权的账号级变更连接，不能代替负责人提交解冻、变配或计费操作。

## 5. 恢复验收标准

外部处理完成后，先只读确认全部字段：

```text
Status           = running
ServerlessStatus = resume
IsFreeze         = no
ProcessingTask   = empty
Tasks            = []
```

然后在新的受控窗口创建最新生产快照，必须取得：

```text
new backup_id    = <new id>
BackupStatus     = success
IsUsable         = yes
PITR             = verified
```

仅当上述条件全部满足，才能输出：

```text
PRODUCTION BACKUP GATE READY
```

## 6. 重新申请 R1.4 Attempt #2

第一次 R1.4 执行批准不自动续用。新快照成功后必须重新：

1. 冻结 clean `origin/main` release SHA；
2. 核对该 SHA 的 GitHub Actions CI 为 `success`；
3. 校验 R1.1 migration manifest 和源树 SHA；
4. 重新记录 revision 188、100% 流量和健康端点；
5. 将新 `backup_id` 绑定到新的 execution packet；
6. 重新取得 `R1.4 EXECUTION APPROVED — GATED / ATTEMPT #2`。

在此之前状态保持：

```text
R1.4.1 Backup Gate        BLOCKED
R1.4.2 Freeze Resolution  IN PROGRESS / EXTERNAL ACTION REQUIRED
R1.4 Production Rebase    BLOCKED
V1.2 Production            NO-GO
revision 188               100% unchanged
```
