# 任务 4：年度 MP 新模型与 2026 工作簿只读导入

## 模型

- 稳定指标定义与年度指标版本分离；
- 年度方案、启用指标、组织目标、月 MP/预定/实绩分表；
- 空值显式保存为 `NO_DATA`、`NOT_APPLICABLE`、`NOT_DUE`、`ZERO_IS_VALID` 或 `VALUE`；
- 自动计算、人工覆盖、批次、数据质量问题和六类重点行动分别建模；
- 利润率本期只保存人工填写值，系统不自行推导。

## 工作簿门禁

1. 上传仅生成预览和数据质量问题；
2. 应用后年度方案仍为 `DRAFT` 且 `write_enabled=false`；
3. 六中心目标分解与苏州塾总目标差额显式展示；
4. 负数新增按原值保留并生成待确认警告；
5. 黄埔班是苏州塾直属班级，但本批次不生成其 14 项区域指标。

## 回滚

- 按 `import_batches.id` 追踪来源；
- 在 staging 备份后删除该批次创建的 DRAFT 方案及其级联值；
- 不覆盖旧系统 `annual_kpi_values` 或 `mp_kpi_values`。
