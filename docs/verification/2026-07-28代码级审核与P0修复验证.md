# 代码级审核与 P0 修复验证报告

**审核日期**：2026-07-28
**审核范围**：`seiwajyuku-platform` + `signin` 两个仓库实际代码
**审核类型**：静态代码级审核（尚未验证云端环境、生产数据库、CI 执行结果、CloudRun 镜像）

---

## 一、审核覆盖范围

1. 核心目录与技术架构
2. SQLite/MySQL 双驱动迁移模型
3. 登录、角色、权限及组织范围
4. 学员主数据及手机号安全
5. 年度MP导入、填报和自动指标
6. 关怀任务与企业走访
7. 续费运营
8. 签到系统活动、名单、签到和导出
9. 两个系统之间的接口边界
10. 班级学习会三次签到积分需求

---

## 二、P0 问题验证

### P0-1：手机号绕过 — 已验证、修复并收紧

**代码确认**：
- `services/members.py` 第 214 行原代码 `"phone": decrypt_text(member["phone_ciphertext"])` 直接返回完整手机号
- `DIRECT_PROFILE_ROLES = {"system_admin", "operations_admin", "regional_manager"}` 无条件放行
- 不要求关联跟进任务

**修复验证**：
- `get_member_detail()` 现返回 `phone_masked`，不返回 `phone_ciphertext` 和 `enterprise_financial_ciphertext`
- 新增 `get_member_enterprise_detail()` 要求 purpose 参数（≥4字符）+ 审计日志，且不返回完整手机号
- `members:detail_view` 仅代表脱敏基本资料查看，仍受主组织或正式班级/小组关系限制
- `test_privacy.py` 已更新：验证 `phone_masked` 而非 `phone`

### P0-2：MP导入穿透 — 已验证并修复

**代码确认**：
- `mp_import.py` 的 `apply_preview()` 接收 `actor_user_id` 但从不调用 `accessible_org_ids()`
- API 层仅检查 `plans:write`，`regional_manager` 拥有此权限

**修复验证**：
- 权限拆分：`plans:period_write`（区域负责人）+ `plans:import_global`（运营管理员）
- `api/imports.py` 两处 `require_permission` 改为 `plans:import_global`
- `api/plans.py` 填报改为 `plans:period_write`
- `apply_preview()` 增加 `user_context` 检查和 org scope 过滤

### P0-3：班级关系模型 — 代码已闭环，历史数据回填待执行

**代码确认**：
- 14个迁移文件中无 `member_org_relations` 表
- `members` 表仅 `org_unit_id` + `development_org_unit_id` 两个外键
- `class_name`/`group_name` 为文本字段

**修复验证**：
- 新增 `migrations/sqlite/0008_member_org_relations.sql`
- 新增 `migrations/mysql/0008_member_org_relations.sql`
- 支持 5 种关系类型，含 `valid_from`/`valid_until` 时间有效期
- `services/checkin_rosters.py` 基于关系表查询名单
- 学长列表、详情和个人积分授权已识别有效关系

### P0-4：Dockerfile 不一致 — 已验证并修复

**代码确认**：
- 根 Dockerfile 包含 `COPY --chown=app:app migrations ./migrations`
- `apps/platform-api/Dockerfile` 缺少此行
- CI 使用 `docker build -t ... apps/platform-api`

**修复验证**：
- CI 改为 `docker build -t ... .`（根上下文）
- `apps/platform-api/Dockerfile` 已删除

### P0-5：名单接口未实现 — 已验证并修复

**代码确认**：
- `main.py` 未注册 `checkin_rosters` 路由
- `settings.py` 未读取 `SIGNIN_API_BASE_URL`/`SIGNIN_SERVICE_API_KEY`
- `.env.example` 已预留这两个变量

**修复验证**：
- 新增 `api/checkin_rosters.py`：`GET /options` + `GET /members`
- `settings.py` 新增 `signin_api_base_url` + `signin_service_api_key`
- `main.py` 注册 `checkin_rosters_router`
- 使用 `hmac.compare_digest` 验证 X-API-Key

### P0-6：重复名额机制 — 已验证并修复

**代码确认**：
- `remainingIndexes.slice(0, quantity)` 按顺序选择前N条
- Registration 无 `member_code` 字段
- Event 无 `event_group_id`/`session_code`/`scheduled_start_at`/`scheduled_end_at`

**修复验证**：
- 签到系统新增 `/create_class_meeting_sessions` 创建三场次
- Registration 新增 `member_code`、`event_group_id`、`session_code`
- Event 新增 `scheduled_start_at`/`scheduled_end_at`
- `publicEvent()` 返回新字段
- 新增增量拉取接口供运营平台同步
- 运营平台同步时按 `member_code` 解析 `member_id`，未匹配记录进入 `UNMATCHED`
- 记录分页会持续消费 `next_cursor`，部分失败标记为 `PARTIAL`

---

## 三、新增个人出勤积分模型验证

### 迁移文件

- `migrations/sqlite/0009_attendance_scoring.sql` — 7张表
- `migrations/mysql/0009_attendance_scoring.sql` — 7张表

### 积分规则种子数据

| session_code | base_points | late_deduction | early_leave_deduction |
|-------------|------------|---------------|---------------------|
| MORNING | 7 | 1 | 1 |
| AFTERNOON | 7 | 1 | 1 |
| KONPA | 4 | 1 | 1 |

### 服务层验证

- `attendance_scoring.py`：`calculate_score()` 正确处理 score_eligible=False → 0分
- `calculate_score()` 正确处理 ABSENT → 0分
- `calculate_score()` 正确判断迟到（`checked_at > scheduled_start_at`）
- `has_active_early_leave()` 检查未撤销的早退裁定
- `upsert_score_record()` 幂等更新（按 attendance_record_id 唯一）
- `recalculate_event_group()` 批量重算
- `class_meeting`/`CLASS_MEETING` 在规则查询前统一标准化
- 撤销早退、补签、请假、作废和人员重关联会更新有效业务状态并触发重算

### API 端点验证

| 端点 | 权限 | 功能 |
|------|------|------|
| POST /attendance/sync | attendance:sync | 触发增量同步 |
| GET /attendance/event-groups | members:read | 活动列表 |
| GET /attendance/event-groups/{id} | members:read | 活动详情 |
| GET /attendance/records | members:read | 出勤明细 |
| POST /attendance/records/{id}/adjudications | attendance:adjudicate | 人工裁定 |
| POST /attendance/event-groups/{id}/recalculate | attendance:adjudicate | 重算积分 |
| GET /members/{id}/attendance-scores | members:read | 个人积分 |

---

## 四、语法验证

| 文件类型 | 验证方式 | 结果 |
|---------|---------|------|
| Python 代码 | `compileall` | ✅ 全部通过 |
| 签到系统 JS | `node --check` | ✅ 通过 |
| API 自动测试（全新 SQLite） | `pytest -q` | ✅ 31 passed |
| 当前 CI 测试命令 | `python -m pytest -q` | ✅ 与本地完整套件一致 |
| SQLite 0008/0009 迁移 | 启动迁移 + 测试 | ✅ 通过 |

---

## 五、仍未验证项

1. staging MySQL 真实迁移、约束和回滚演练
2. 生产备份恢复演练
3. 两个系统在 staging 的写入同步、积分并行核对和回滚演练
4. member_org_relations 历史数据回填与无法匹配清单

---

## 六、实施顺序建议（后续）

| 阶段 | 内容 | 前置条件 |
|------|------|---------|
| 1 | 在 CI 环境运行完整测试套件 | 无 |
| 2 | member_org_relations 数据迁移 | 测试通过 |
| 3 | 黄埔班组织类型修正 | 关系表就绪 |
| 4 | 两个系统联调（密钥配置+并行核对） | 关系表+名单接口 |
| 5 | 签到系统部署三场次功能 | 联调通过 |
| 6 | 运营平台部署出勤积分 | 签到系统就绪 |
| 7 | 小范围班级试点 | 双系统部署完成 |
| 8 | 生产 MySQL 备份恢复演练 | 独立于上述 |

---

## 七、P0.1 生产安全收口验证（2026-07-28）

### 签到服务

- 提交 `3e2d202` 已推送到 `signin/main`，云函数 `checkinApi` 采用仅代码更新方式部署成功。
- 管理员密码改为环境变量摘要，生产默认密码已移除；新密码登录 200 并签发令牌，旧默认密码返回 401。
- 入站 `SIGNIN_SERVICE_API_KEY` 与出站 `OPS_ROSTER_API_KEY` 已拆分，旧 `OPS_API_KEY` 已从函数环境移除。
- 正式读取契约：缺少密钥 401、旧共享密钥 401、新独立密钥 200。
- HTTP 路由 `/api/*` 指向 `checkinApi`、保持路径透传，配置总 QPS 300、单 IP QPS 30。

### 运营平台

- 提交 `cc85208` 修正签到 API 路径，提交 `cf8ba08` 增加根级 `.cloudbaseignore`；两次 GitHub Actions CI 均通过。
- 云端服务环境已显式设置只读三开关，并配置签到 API 基址和独立服务密钥。
- CloudRun `027` 构建正常；先分配 10% 灰度流量，40 次环境读取全部 200、响应均为 production/read-only，10 次写入探针全部 403。
- 灰度验证后 `seiwajyuku-platform-api-027` 已提升为稳定版并承载 100% 流量；健康检查 200。
- 生产继续保持只读，因此没有触发真实同步写入；完整同步和积分核对必须在 staging 完成。

### 回滚与密钥保管

- 稳定旧版本 `025` 保留，可作为应用版本回滚点。
- 函数旧环境、平台旧环境、新管理员凭据与新服务密钥已写入 Windows 当前用户 DPAPI 加密恢复包。
- 仓库与文档未写入明文密钥。
