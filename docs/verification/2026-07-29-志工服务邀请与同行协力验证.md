# 志工服务邀请与同行协力验证

日期：2026-07-29

## 验证范围

- 0012 数据库迁移与回滚；
- 服务邀请状态转换；
- 邀请人与受邀人的组织及任职权限；
- 担当人与同行伙伴的数据访问边界；
- 生产保护开关；
- 前端类型检查与构建。

## 自动验证

- 平台 API 全量：`58 passed`。
- 管理端 `pnpm typecheck`：通过。
- 管理端 `pnpm build:staging`：通过，产物约 2.4 MB。
- SQLite 专项覆盖：
  - 未接受邀请前不能记录服务或查看联系方式；
  - 接受后担当人可记录、查看必要联系方式并结束事项；
  - 时间调整建议可由发起人回应并回到待接受；
  - 暂时无法参与是终止回应并写入审计；
  - 同行伙伴可记录，但不能查看完整联系方式或结束事项；
  - 范围外任职不能受邀；
  - 过期邀请不能接受；
  - 0012 回滚只删除新增表，保留旧 `followup_tasks`。

## 尚未执行

- 未连接生产数据库；
- 未修改生产账号、人员归属或权限；
- 未启用生产功能开关；
- 未部署；
- 未开展真实账号试点。

## 试点前门禁

1. 明确批准试点组织、账号、时间窗口和回滚责任人；
2. 仅在隔离环境开启 `VOLUNTEER_SERVICE_INVITATIONS_ENABLED=true`；
3. 核对受邀志工的有效任职和组织边界；
4. 使用非敏感试点数据演练接受、调整、暂时无法参与和同行协力；
5. 验证审计日志后，另行决定是否进入生产审批。
