# 直属四班生产前迁移预览

> 日期：2026-07-29
> 状态：只读预览已完成；生产写入未执行
> 数据来源：`直属班级名单.xlsx` / `2026 新在册表`

## 本次只读结果

| 项目 | 结果 |
| --- | ---: |
| 在册直属学习班学长 | 123 |
| 拟创建或复用的直属 `CLASS` | 4 |
| 拟创建或复用的普通班小组候选 | 11 |
| `PRIMARY_REGION` | 123 |
| `DEVELOPMENT_RELATION` | 123 |
| `STUDY_CLASS` | 123 |
| `STUDY_GROUP` | 61 |
| 关系写入合计 | 430 |
| 原小组转备注 | 4 |
| 发展分中心缺失或未知 | 0 |

直属班级分布为黄埔一班 31、黄埔二班 31、先锋班 45、神仙班 16。发展归属覆盖六个既有三级分中心；直属班级不构成第七个分中心。

## 组织与关系候选

四个直属班级均为 `CLASS`，父级唯一解析为“苏州分中心”：黄埔一班、黄埔二班、先锋班、神仙班。仅黄埔一班与黄埔二班的有效小组可作为 `GROUP` 候选；先锋班不会创建小组。

每名学长的主数据访问归属和发展归属都使用其工作簿中的实际发展分中心；直属班级仅写入学习班关系。先锋班的 3 条非空原小组值及黄埔二班“目前不读书”1 条均写作 `原所属小组：原值` 的备注，不建立 `STUDY_GROUP`。

## 尚未满足的生产门禁

1. 仍需以有权账号读取当前生产组织树及脱敏成员标识，验证苏州根、六个分中心、四个班级和既有成员的唯一匹配；本次未连接生产数据库。
2. 旧运营服务比工作簿多 2 名（黄埔二班、先锋班各 1 名）；它们仅作为差异保留，绝不随导入自动停用或删除。
3. 写入当天必须重新计算工作簿指纹；若不等于 `00836053071641c27631ec96d4eb4b2468b2b81926c9520f6f6d871963c17307`，预览失效并重新审计。
4. 写入必须在单一事务中进行，写入前导出组织/成员关系快照，写入后核对 430 条关系和 4 条备注，并保留按导入批次回滚的清单。

因此本文件不是导入授权，也不包含任何生产写入操作。
