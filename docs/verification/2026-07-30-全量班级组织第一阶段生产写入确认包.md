# 全量班级组织第一阶段生产写入确认包

> 生成日期：2026-07-30  
> 当前状态：待明确批准；尚未执行生产写入。  
> 范围：仅创建普通班和普通班小组组织节点，不修改任何学员、任职、权限、签到记录或扫码链路。

## 生产只读预检基线

数据源：`2026年盛和塾苏州分中心最新学员表(2).xlsx`

工作簿 SHA-256：

`59B621084FAEE13C02332E5735583AECAA7FA54F967A73E86164AB9EECA404CF`

| 类别 | 生产预检结果 |
| --- | ---: |
| 在册学员 | 834 |
| 已有班级 / 未分班 | 816 / 18 |
| 普通班 | 20 个 / 693 人 |
| 直属班 | 4 个 / 123 人 |
| 普通班小组 | 112 个 |
| 直属班小组 | 11 个 |
| 生产唯一在册匹配 | 722 |
| 生产无匹配 | 84 |
| 人工复核 | 28 |

人工复核构成：

- 工作簿重复手机号记录 8 条；
- 手机号格式无效 9 条；
- 手机号缺失 11 条；
- 未分班 18 条。

## 第一阶段允许动作

1. 在六个分中心下创建 20 个普通班 `CLASS` 节点。
2. 在对应普通班下创建 112 个 `GROUP` 节点。
3. 小组按“父班级 ID + 小组名称”隔离，同名小组不跨班合并。
4. 记录导入批次、工作簿指纹、创建节点和审计日志。

第一阶段明确不允许：

- 创建、更新或停用任何学员；
- 写入 `member_org_relations`；
- 修改学员的班级、小组或发展归属文本；
- 处理 84 名生产无匹配人员；
- 猜测 18 名未分班人员的班级；
- 修改签到系统扫码链路或名单接口。

## 技术门禁

写入同时要求：

- 管理员具备 `members:manage`；
- `ALLOW_PRODUCTION_MUTATIONS=true`；
- 独立开关 `CLASS_ROSTER_ORG_IMPORT_ENABLED=true`；
- 工作簿 SHA-256 与本确认包完全一致；
- 输入确认文字：`确认创建20个普通班和112个普通班小组`；
- 生产预检计数、问题计数和组织动作与本确认包完全一致；
- 事务内再次确认根节点、六个分中心和普通班无并发冲突。

任一条件不满足时，整批停止并回滚。

## 回滚清单

第一阶段批次在 `import_batches.preview_json` 中记录全部 132 个创建节点。
若第二阶段尚未开始，回滚顺序为：

1. 确认这些节点不存在任何 `member_org_relations`；
2. 先将本批次创建的 112 个小组设置为停用；
3. 再将本批次创建的 20 个普通班设置为停用；
4. 保留导入批次和审计日志，不物理删除记录；
5. 重新运行生产只读预检，普通班恢复为待创建状态。

如果任何节点已产生人员关系，禁止执行第一阶段独立回滚，必须转入第二阶段联合回滚。

## 写入后验收

第一阶段成功后立即重新运行同一工作簿的全量只读预检，预期：

- 24 个班级全部为 `REUSE`；
- 123 个小组全部为 `REUSE`；
- 人员匹配仍为 722 / 84 / 28；
- 学员字段与关系数据保持不变；
- 签到扫码链路保持不变。

只有上述结果成立，才生成第二阶段“唯一匹配人员关系”确认包。
