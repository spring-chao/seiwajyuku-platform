# 只读异常复核与身份试点前置

- 日期：2026-08-02
- 模式：生产只读核对；不创建或修改人员、权限、签到事实
- 生产版本：`seiwajyuku-platform-api-057`

## 当前异常快照

以下仅保留聚合数量，不输出姓名、手机号、成员编号或组织明细：

| 检查项 | 数量 | 处理边界 |
|---|---:|---|
| 签到未匹配（`UNMATCHED`） | 276 | 不计入积分，待身份核对后再处理 |
| 有效学员缺少手机号哈希 | 44 | 需人工确认数据质量，不自动补写 |
| 有效学员无 `STUDY_CLASS` 关系 | 105 | 不推断班级，不写入签到默认名单 |
| 有效学员无 `STUDY_GROUP` 关系 | 192 | 不推断小组，保留班级级别可见范围 |
| 有效学员无主分中心 | 0 | 当前无此类阻断项 |
| 重复有效手机号哈希组 | 0 | 当前无此类阻断项 |

## 身份试点前置门槛

1. 旧 `system_admin` 仅作为待确认候选，不自动拆分为技术管理员或业务岗位。
2. 专职身份必须确认运营中心雇佣、具体岗位、任期和服务责任范围；服务责任不改变人员主组织归属。
3. 志工任职必须确认任职名称、任职组织、范围、起止时间和任命依据；不得形成永久角色。
4. 技术管理员默认不获得业务敏感数据权限；身份授权开关和身份写入开关继续关闭。
5. 试点范围暂定为少量专职账号、一个三级分中心、一个班级和少量志工；稳定一个业务周期后再评估扩大。

## 下一步

- 先完成首个自然工作日 00:00 同步日志核验及连续失败告警核对；
- 再由业务逐项提供上述身份任职确认依据；
- 依据确认结果生成只读候选清单，另行取得生产身份写入批准后才进入试点。
