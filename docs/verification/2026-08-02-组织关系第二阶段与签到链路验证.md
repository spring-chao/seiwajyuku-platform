# 组织关系第二阶段与签到链路验证

- 日期：2026-08-02
- 代码提交：`7344f40`
- 生产服务版本：`seiwajyuku-platform-api-057`
- 数据库环境：`shengheshu-d2g2zyyl99f6c6fc2`

## 第二阶段关系写入

第二阶段只写入 `member_org_relations`，不修改 `members`、班级/小组名称、主分中心归属或签到事实：

- `STUDY_CLASS`：594 条；
- `STUDY_GROUP`：587 条；
- 匹配学员：713 人；
- 成员资料变更：0；
- `import_batches`：1 条，状态 `APPLIED`；
- 小组父级不是班级：0；
- 重复关系边：0。

来源标识为 `CLASS_ROSTER_REL_P2`，工作簿 SHA-256 与确认版本一致。

## 签到名单自动核验

生产接口 `GET /api/v1/checkin-rosters/validate` 返回：

- 来源：`PLATFORM_ORG_RELATIONS`；
- 查询模式：`ORG_UNIT_ID`；
- 回退模式：`FAIL_CLOSED`；
- 班级：30 个；小组：123 个；
- 小组与班级归属不一致：0；
- 无效组织关系：0；
- `passed=true`。

`/api/v1/checkin-rosters/options` 返回 30 个班级和 123 个小组，签到扫码路由未改动。

## 工作日同步

签到云函数 `checkinApi` 的触发器仍为 `attendanceSyncWeekdays0000`，Cron 为 `0 0 0 ? * MON-FRI *`，状态启用。

已手动走完整定时同步入口验证：运行号 `15`，成功处理 9 个场次、384 条签到记录，0 错误（新增 0、更新 384、忽略 276），生产数据库最新同步运行状态为 `SUCCESS`。

2026-08-03 首个自然工作日复核：云函数在 `00:00:05` 开始执行，`00:00:44` 结束；该时间窗共 57 条日志，包含定时同步、签到和名单同步记录，错误或异常记录为 0。由此确认自然 Cron 路径已按时触发并正常结束。连续失败达到 3 次才进入严重状态并写入告警审计的逻辑已有自动测试覆盖；本次自然运行成功，不应产生失败告警。

## 写入门禁收口

一次性内部关系写入接口已从代码和 OpenAPI 移除。生产版本 057 的门禁为：

- `ALLOW_PRODUCTION_MUTATIONS=false`；
- `CLASS_ROSTER_ORG_IMPORT_ENABLED=false`；
- `DEPLOYMENT_READ_ONLY=true`；
- `RUN_BOOTSTRAP_ON_STARTUP=false`。

版本 057 同时保留 VPC 数据库连接配置，并将已认证的定时同步入口列入只读部署允许的机器间 POST 白名单；其他迁移、导入和管理写入仍保持关闭。真实接口回归结果：环境门禁返回生产只读、名单核验 `passed=true`，定时同步返回 `SUCCESS`。

## 后续优先级

1. 对当前只读异常清单进行人工复核：签到未匹配 276 条、缺少手机号哈希 44 人、无班级关系 105 人、无小组关系 192 人；
2. 在隔离 MySQL CI 完成 0011 身份/任职迁移和回滚验证；
3. 只读核对旧 `system_admin` 候选，再设计专职、志工和技术管理员的真实账号试点；不自动迁移生产权限。
