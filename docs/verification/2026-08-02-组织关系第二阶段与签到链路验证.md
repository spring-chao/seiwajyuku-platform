# 组织关系第二阶段与签到链路验证

- 日期：2026-08-02
- 代码提交：`f212154`
- 生产服务版本：`seiwajyuku-platform-api-054`
- 数据库环境：`shengheshu-d2g2zyyl99f6c6fc2`

## 第二阶段关系写入

第二阶段只写入 `member_org_relations`，不修改 `members`、班级/小组名称、主分中心归属或签到事实：

- `STUDY_CLASS`：594 条；
- `STUDY_GROUP`：587 条；
- 匹配学员：713 人；
- 成员资料变更：0；
- `import_batches`：1 条，状态 `APPLIED`；
- 小组父级不是班级：0；
- 重复关系边：0。

来源标识为 `CLASS_ROSTER_REL_P2`，工作簿 SHA-256 与确认版本一致。

## 签到名单自动核验

生产接口 `GET /api/v1/checkin-rosters/validate` 返回：

- 来源：`PLATFORM_ORG_RELATIONS`；
- 查询模式：`ORG_UNIT_ID`；
- 回退模式：`FAIL_CLOSED`；
- 班级：30 个；小组：123 个；
- 小组与班级归属不一致：0；
- 无效组织关系：0；
- `passed=true`。

`/api/v1/checkin-rosters/options` 返回 30 个班级和 123 个小组，签到扫码路由未改动。

## 工作日同步

签到云函数 `checkinApi` 的触发器仍为 `attendanceSyncWeekdays0000`，Cron 为 `0 0 0 ? * MON-FRI *`，状态启用。

已手动走完整定时同步入口验证：成功处理 9 个场次、384 条签到记录，0 错误；生产数据库最新同步运行状态为 `SUCCESS`。下一步仍需查看首个自然工作日 00:00（2026-08-03）的云函数日志，确认自然触发路径。

## 写入门禁收口

一次性内部关系写入接口已从代码和 OpenAPI 移除。生产版本 054 的门禁为：

- `ALLOW_PRODUCTION_MUTATIONS=false`；
- `CLASS_ROSTER_ORG_IMPORT_ENABLED=false`；
- `DEPLOYMENT_READ_ONLY=true`；
- `RUN_BOOTSTRAP_ON_STARTUP=false`。

## 后续优先级

1. 查看 2026-08-03 00:00 自然同步日志并确认连续失败告警；
2. 对当前只读异常清单进行人工复核：签到未匹配 276 条、缺少手机号哈希 44 人、无班级关系 105 人、无小组关系 192 人；
3. 在隔离 MySQL CI 完成 0011 身份/任职迁移和回滚验证；
4. 只读核对旧 `system_admin` 候选，再设计专职、志工和技术管理员的真实账号试点；不自动迁移生产权限。
