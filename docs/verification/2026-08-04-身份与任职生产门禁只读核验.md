# 身份与任职生产门禁只读核验

- 日期：2026-08-04
- 范围：生产管理端身份与任职页面、只读开关状态
- 生产操作：无数据库写入、无角色变更、无权限变更

## 核验结果

- 已使用现有管理员登录会话打开“身份与任职”页面。
- 页面明确显示“身份与任职功能尚未启用”，说明 `IDENTITY_AUTHORIZATION_ENABLED` 当前仍为关闭状态。
- `IDENTITY_ADMIN_WRITES_ENABLED` 未被打开；没有出现自然人关联、雇佣、志工任职、技术任期的写入按钮或表单。
- 生产环境继续保持 `ALLOW_PRODUCTION_MUTATIONS=false` 与部署只读门禁。
- 既有活动与签到页面、签到名单和扫码链路未被修改。

## 下一阶段门槛

只有在业务确认试点账号、任命依据、组织范围、任期和回滚责任后，才可单独批准打开身份授权读取灰度。读取灰度打开后仍应保持身份写入开关关闭，先完成账号与组织范围的只读核对；身份写入必须另行批准并限定配置窗口。
