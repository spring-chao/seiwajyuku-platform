# 隔离环境身份、权限与审计验证

- 日期：2026-08-04
- 环境：本地隔离 SQLite 合成账号数据库
- 生产操作：无

## 验证结果

- 角色模板权限矩阵可以读取并按敏感级别展示。
- `technical_admin` 仅保留 IAM、组织技术配置、审计和集成权限，不包含学员、企业、联系方式或敏感导出权限。
- 运营中心岗位可在没有三级分中心组织归属的情况下生效；服务责任范围单独控制组织访问。
- 志工任职按组织范围和有效期生效，撤销后立即退出授权上下文，不污染专职岗位。
- 身份关联、雇佣、志工任职、技术任期和状态变更均产生审计记录。
- 0011 身份模型前向/回滚只影响新增表，保留旧 IAM、学员关系和签到事实。

## 自动验证

| 验证 | 结果 |
|---|---|
| `test_identity_authorization` | 8 passed |
| `test_identity_admin` | 5 passed |
| 合计 | 13 passed |
| Docker/MySQL 本机运行时 | 当前机器未安装，未伪造 MySQL 结果 |

## 试点准备边界

下一步仅准备不含真实姓名、手机号和密码的试点清单模板，字段包括：账号标识、身份类别、确认依据、任职组织、服务范围、开始/结束时间、回滚责任人。真实账号和任命依据由业务方逐项确认后才能进入隔离试点；生产写入开关继续关闭。
