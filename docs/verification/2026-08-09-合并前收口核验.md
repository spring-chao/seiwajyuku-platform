# 2026-08-09 合并前收口核验

## 已完成的合并前技术门禁

- 后端完整测试：102 项通过；
- 前端 `pnpm typecheck`：通过；
- 前端 `pnpm build:staging`：通过；
- `git diff --check`：通过；
- SQLite 0014–0017 回滚脚本已补齐并完成内存数据库验证；
- MySQL CI 回滚链已扩展为 0017 → 0011，并增加新增表、加密预览列和手机号唯一索引的清理断言；
- 生产健康检查 HTTP 200；
- 生产环境保持 `production_mutations_allowed=false`、`deployment_read_only=true`、`identity_admin_writes_enabled=false`、`volunteer_service_invitations_enabled=false`。

## 合并提交前仍需执行

1. 审查并纳入本次代码和验证文档，确认没有临时调试文件或敏感信息；
2. 从干净提交执行一次 GitHub Actions，确认 API、前端、容器和真实 MySQL staging 四个作业全绿；
3. 用合并提交重新部署 staging，完成学员编辑/历史、续费工作台和身份多岗位展示冒烟测试；
4. 合并后保留生产写入门禁关闭，不在合并动作中迁移真实人员或权限。

## 合并后、生产扩大前的业务门槛

- 小范围真实账号试点：苏州运营中心、一个三级分中心、一个班级和少量志工；
- 逐人确认自然人、雇佣岗位、服务责任、志工任职、任期和回滚责任人；
- 查看首次工作日 00:00 自动签到同步日志并确认失败告警；
- 重新核对签到未匹配、缺少分中心和手机号质量异常；
- 在非生产克隆环境完成备份恢复演练；
- 业务负责人再次批准后，才可临时开启身份或志工写入。
