# 2026-08-09 多岗位身份前端展示核验

## 本阶段完成

- 身份与任职页面的账号角色列改为根据有效运营中心岗位、志工任职和技术管理任期动态展示。
- 同一雇佣下的多个岗位会同时显示；已结束或已撤销的历史任职不计入当前角色标签。
- `admin` 继续固定标识为“平台最高管理账号”，不参与业务岗位迁移。
- 本次仅更新前端展示，不开启身份写入，不创建或修改生产账号、雇佣和任职。

## 验证结果

- `pnpm typecheck`：通过；
- `pnpm build:staging`：通过；
- CloudBase Hosting：41 个文件上传成功，0 个失败；
- 身份管理懒加载资源 HTTP 200，已包含多岗位标签和技术职责标签逻辑；
- 生产 API 健康检查 HTTP 200；
- 生产门禁保持 `production_mutations_allowed=false`、`deployment_read_only=true`、`identity_admin_writes_enabled=false`。
