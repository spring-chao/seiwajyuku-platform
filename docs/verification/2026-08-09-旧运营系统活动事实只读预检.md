# 旧运营系统活动事实只读预检

日期：2026-08-09

## 结论

隔离服务已通过 VPC 只读连接旧运营库与统一平台生产库。旧库规划迁移的七类历史活动表均为 0 条，因此本批没有可导入活动事实，不应为了空批次应用生产迁移或开启生产写入。

统一平台生产库当前有 839 名学员，`0018_legacy_member_activity_facts` 尚未应用。本次没有创建、修改或删除统一平台生产数据。

## 七类活动事实

| 来源表 | 只读数量 |
| --- | ---: |
| `group_sessions` | 0 |
| `class_sessions` | 0 |
| `courses` | 0 |
| `report_meetings` | 0 |
| `study_tours` | 0 |
| `reading_checkins` | 0 |
| `reading_shares` | 0 |
| 合计 | 0 |

只读匹配预检结果：总计 0、可导入 0、已匹配 0、缺失学员编号 0、未匹配学员 0、重复事实 0。

## 旧库其他聚合数量

| 表 | 数量 | 处理结论 |
| --- | ---: | --- |
| `members` | 1280 | 不覆盖统一平台主档；只作退役盘点 |
| `member_identifiers` | 0 | 不具备稳定 `member_code` 匹配条件 |
| `attendance_events` | 11 | 签到链路已有独立权威来源，不二次迁移 |
| `attendance_registrations` | 471 | 视为旧签到缓存，不二次迁移 |
| `attendance_sync_log` | 3232 | 运维日志不迁移 |
| `member_org_assignments` | 1265 | 不覆盖统一平台正式组织 ID 关系 |
| `member_cohort_enrollments` | 245 | 不覆盖统一平台正式组织 ID 关系 |
| `member_lifecycle_history` | 25 | 缺少稳定编号，禁止姓名或手机号猜测迁移 |
| `org_change_log` | 1759 | 旧组织审计仅随旧库归档保留 |
| `mp_kpi_values` | 563 | 旧系统停止录入；统一平台继续按现有 MP 口径运行 |

旧库中 `companies`、`reading_sync_log`、`annual_kpi_values` 均为 0。

## 隐私与安全核验

- 导出包只允许 `member_code`、日期、类型、状态、标题、时长和来源标识；
- 隐私契约确认不含姓名、手机号和叙事文本；
- 实际导出包为空事实包，统一平台解析器按设计拒绝空合并包，未创建导入批次；
- 两个数据库连接均设置会话只读；
- 未开启生产写入门禁，未应用生产 `0018`，未改变签到扫码链路。

## 退役判断

旧系统的七类活动事实没有迁移内容。旧库仍含主档、旧组织关系、签到缓存、MP 值和审计记录，但它们不得继续成为业务来源；其中签到与组织数据已有统一平台权威来源，其他数据随旧库只读归档保留。

## 2026-08-10 退役执行

- 停用前 MySQL 快照 `legacy-retirement-20260810` 已成功完成并确认可用；
- 旧工作日签到同步定时触发器已删除，云函数代码保留但不会自动运行；
- 旧 `seiwajyuku-ops` 云托管公开服务已删除；
- 旧数据库和历史快照继续保留，没有删除历史数据；
- 新平台生产 API 健康状态为 `ok`，生产写入、身份写入门禁仍为关闭状态。

因此旧系统已完成业务停用和技术入口下线，统一平台成为唯一运营入口。
