# 2026-08-09 续费正式导入与收口

## 本阶段目标（已完成）

完成正式导入前的异常可见性、生产主档关联口径、首次导入防覆盖和整批回滚准备，并在批准窗口完成一次性导入和门禁收口。

## 工作簿只读核对

使用当前桌面文件进行本地只读解析：

- 待续费名单：795 条；
- 学员主档匹配：785 条；
- 待人工确认：10 条；
- 无效数据：0 条；
- 需要协助：97 条；
- 待确认原因：主档手机号重复 8 条、未匹配主档 2 条。

以上“学员主档匹配”只表示两个工作簿之间匹配成功，不等同于已经关联生产学员。新版生产预检增加“已关联生产学员、正式导入候选、未关联生产学员”三个独立数字，正式导入只接受已关联生产学员且通过匹配门禁的记录。

## 新增安全门禁

1. 生产只读预检不保存批次、不创建暂存行、不写续费周期；
2. API 不再返回工作簿原始行、手机号或生产学员 ID；
3. 全部待确认/无效记录单独返回，不再受前 50 行样本限制；
4. 正式导入明确排除 `NEEDS_REVIEW`、`INVALID` 和未关联生产学员的记录；
5. 首次整批导入发现目标年度已有同一学员续费周期时整批停止，禁止覆盖已有跟进；
6. 导入批次可在没有后续修改和跟进记录时整批回滚；已经开始跟进的批次禁止自动回滚，必须生成联合回滚清单；
7. 正式导入和回滚端点继续受生产只读门禁保护，本阶段未执行任何生产写入。

## 自动验证

- 后端全量测试：102 项通过；
- 前端 TypeScript/Vue 类型检查：通过；
- 前端 staging 构建：通过；
- `git diff --check`：通过；
- 覆盖中文/标准化英文主档表头、只读不落库、完整异常队列、响应脱敏、待确认禁止导入、首次导入防覆盖、整批回滚。

## 导入前确认项（已完成）

部署新版只读预检后，业务负责人已确认 2026 年导入范围、候选数量和回滚窗口，随后在限定窗口完成正式导入。

## 部署核验

- CloudRun 服务更新时间：2026-08-09 01:18:34，运行状态 `normal`；
- API 健康检查：HTTP 200；
- 生产环境继续保持 `deployment_read_only=true`、`production_mutations_allowed=false`；
- 续费预检未登录返回 HTTP 401，证明只读预检已通过部署门禁并继续受账号权限保护；
- 回滚写入端点在生产只读状态返回 HTTP 403，证明写入门禁未被放开；
- 新版前端资源已包含生产关联数、完整异常队列和正式导入候选口径。

## 生产只读预检结果（用户线上核验）

用户在生产页面完成当前工作簿预检，结果如下：

- 总计：795 条；
- 主档匹配：786 条；
- 已关联生产学员：732 条；
- 正式导入候选：724 条；
- 未关联生产学员：62 条；
- 待确认：9 条；
- 无效数据：0 条；
- 需要协助：97 条；
- 待确认原因：主档手机号重复 8 条、未匹配到学员主档 1 条。

其中 724 条满足当前正式导入门禁；71 条（62 条未关联生产学员、9 条待确认）将被安全跳过，不会进入正式续费周期。

## 导入范围决策（已完成）

正式导入范围为“724 条候选，跳过 71 条异常”，年度为 2026 年；该范围已获业务负责人确认并完成一次性导入。

## 正式导入完成（2026-08-09）

- 用户已批准导入 724 条 2026 年续费周期；生产导入已完成。
- 当前年度正式续费对象为 724 人；待确认和未关联生产学员记录未写入。
- 实际预检候选曾显示 725 条，其中 1 条与另一条候选指向同一生产学员。正式导入按 `member_id + renewal_year` 唯一约束保留首条，跳过重复行，最终创建 724 条，未覆盖既有周期。
- 导入后的生产写入窗口已关闭：`deployment_read_only=true`、`production_mutations_allowed=false`、`identity_admin_writes_enabled=false`。
- CloudRun 服务版本 `seiwajyuku-platform-api-091` 承载 100% 流量，健康检查 HTTP 200。
