# 0018 与学员服务提示生产发布核验

日期：2026-08-10

## 结论

旧运营系统停用后，统一平台完成生产数据库结构对齐和第一阶段“学员服务提示”发布。生产 API、管理门户和新版学员页面资源均可访问，生产写入、身份管理写入门禁继续关闭，现场扫码签到链路未修改。

本次发布不导入旧系统空活动事实，不创建关怀任务，不保存提示结果，也不修改学员档案或组织关系。

## 数据库结构对齐

- 变更前创建生产快照：`pre-0018-platform-api-20260810`，状态成功且可用；
- 应用 `migrations/mysql/0018_legacy_member_activity_facts.sql`；
- `schema_migrations` 已登记 `0018_legacy_member_activity_facts.sql`；
- `member_activity_facts` 表结构已创建，核验为 14 列；
- 旧系统七类规划活动事实均为 0，因此生产事实表保持 0 条；
- 结构迁移只用于统一主分支与生产架构，不代表导入了旧系统业务事实。

## 学员服务提示

第一阶段提供四类可解释提示：

1. 联系方式待核对；
2. 学习班关系待核对；
3. 关怀事项待跟进；
4. 续费节点待处理。

提示只返回规则代码、标题、说明、关注等级、建议动作、规则版本和聚合证据，不返回手机号、备注、企业敏感资料或任务原文。提示使用既有权限与组织范围，属于实时只读计算。

生产只读聚合核验结果：

| 规则 | 命中人数 |
| --- | ---: |
| 联系方式待核对 | 44 |
| 学习班关系待核对 | 105 |
| 关怀事项待跟进 | 0 |
| 续费节点待处理 | 266 |

以上数量用于发现资料整理和续费服务工作量，不用于评价学员积极性、贡献度、流失风险，不形成排名。

## 自动验证

- 后端完整测试：111 项通过；
- Python 编译检查：通过；
- 前端 TypeScript/Vue 类型检查：通过；
- 前端生产构建：通过；
- PR #31 的 API、容器、管理端和 MySQL staging 检查均通过。

## 生产发布核验

| 项目 | 结果 |
| --- | --- |
| API 版本 | `seiwajyuku-platform-api-097` |
| 流量 | 100% |
| 服务状态 | `normal` |
| `/api/v1/health` | `status=ok` |
| 管理门户 | HTTP 200 |
| 新版学员页面资源 | HTTP 200 |
| 未登录访问学员时间线 | HTTP 401 |
| VPC | 已配置 |
| `DEPLOYMENT_READ_ONLY` | `true` |
| `ALLOW_PRODUCTION_MUTATIONS` | `false` |
| `IDENTITY_ADMIN_WRITES_ENABLED` | `false` |

已登录浏览器能够进入生产“学员管理”页面。自动读取页面结构时浏览器连接连续超时，因此本轮不把弹窗的人工视觉验收标记为自动通过；新版资源发布、接口契约和自动测试均已验证，后续只需在真实档案时间线中抽查提示文字与业务口径，无需重复验证部署或权限门禁。

## 签到边界

- 新平台工作日 00:00 定时触发器 `attendanceSyncWeekdays0000` 保持启用；
- 名单继续以统一平台正式组织关系为唯一来源；
- `/event` 与 `/checkin` 现场扫码链路未修改；
- 已删除的旧系统重复定时触发器未恢复。
