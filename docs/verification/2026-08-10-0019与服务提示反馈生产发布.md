# 0019 与学员服务提示反馈生产发布验证

日期：2026-08-10

## 结论

任务 16 已完成生产数据库迁移、API 灰度发布和管理端静态发布。API 版本 `seiwajyuku-platform-api-101` 当前状态正常并承接 100% 流量，健康检查为 `ok`；生产环境仍为只读，反馈功能保持关闭，没有产生生产反馈记录。

本次发布只交付后续试点所需的结构和代码，不授权真实账号试点，不开放生产反馈写入。

## 代码与持续集成

- PR：`#35`；
- 生产代码提交：`8915157 feat: add member service signal feedback (#35)`；
- GitHub Actions 的管理端、API、API 容器和 MySQL staging 共 4 项作业全部通过；
- MySQL staging 已覆盖 0019 建表、备份恢复和逆序回滚。

## 数据库迁移

- 迁移前建立手工快照：`pre-0019-platform-api-20260810-task16`；
- 快照 BackupId：`10091032`，FlowId：`6753100`，状态：成功；
- 已登记迁移：`0019_member_service_signal_feedback.sql`；
- 发布后复核：反馈表 9 个字段、4 个索引、0 条反馈记录；
- 未修改学员档案、组织关系、关怀任务、续费周期、签到事实或现场扫码链路。

## API 发布与安全门禁

- 灰度版本：`seiwajyuku-platform-api-101`；
- 灰度期间先保持 0% 流量，核对版本状态、数据库连接和环境变量，再切流验证并完成晋升；
- 发布后流量：101 为 100%，100 为 0%；
- `/api/v1/health`：`ok`；
- `/api/v1/system/environment`：production；
- `deployment_read_only=true`；
- `production_mutations_allowed=false`；
- `identity_admin_writes_enabled=false`；
- `member_service_signal_feedback_enabled=false`；
- 反馈 POST 实测返回 HTTP 403。

## 管理端发布

- 使用 staging 模式构建，以 `/ops-platform/` 为公开路径并连接生产 API；
- 41 个静态文件全部上传成功；
- 生产入口返回 HTTP 200；
- 生产 CDN 资源 `members-mmWok_sg.js` 返回 HTTP 200，并包含“确认有效”和“反馈试点已开启”文案；
- 因反馈开关关闭，真实页面仍应保持“规则只读”，不显示可提交反馈的操作入口。

## 回滚与后续门槛

- API 可将流量回退到 `seiwajyuku-platform-api-100`；
- 数据库可基于发布前快照恢复，也保留 0019 逆向迁移脚本；
- 管理端可重新发布上一版静态资源；
- 下一步必须由运营人员完成真实学员时间线的视觉与业务口径验收；
- 开启真实账号反馈试点前，必须明确账号、组织范围、周期、业务负责人和回滚责任人，并单独批准生产写入窗口。
