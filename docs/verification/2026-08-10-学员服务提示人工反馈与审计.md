# 学员服务提示人工反馈与审计验证

日期：2026-08-10

## 结论

任务 16 的本地实现和自动验证已完成。系统可以在独立开关开启时，对当前仍然命中的学员服务提示提交“确认有效、暂不适用、数据已修正”三种结构化反馈，并追加保存规则版本、脱敏证据快照和审计记录。

本阶段未连接生产数据库、未执行生产迁移或部署、未开启真实账号试点，也未修改学员档案、组织关系、关怀任务、续费周期或签到事实。

## 已实现

- 新增 `0019_member_service_signal_feedback` 的 SQLite/MySQL 前向迁移和回滚脚本；
- 新增反馈接口，提交前同时校验 `members:manage`、正式组织范围、当前规则命中和规则版本；
- 反馈历史只追加不覆盖，时间线只展示相同规则版本下的最新反馈；
- 证据快照仅来自现有隐私安全证据对象，不保存手机号、姓名、备注、企业资料或任务原文；
- 审计记录保存反馈前后状态、规则代码、规则版本和证据字段名，不复制证据值或敏感原文；
- 管理端在反馈开关开启时显示三种操作和最新反馈，在开关关闭时保持“规则只读”；
- 反馈不会隐藏仍然命中的提示，也不会自动创建关怀任务。

## 门禁

- `MEMBER_SERVICE_SIGNAL_FEEDBACK_ENABLED` 在所有示例环境中默认 `false`；
- 生产环境未设置 `ALLOW_PRODUCTION_MUTATIONS=true` 时，开启反馈开关会被启动安全检查拒绝；
- 生产只读中间件不允许反馈 POST 接口；
- 开关关闭时不查询 0019 反馈表，任务 14 的第一阶段只读提示可以继续工作；
- 系统环境接口显示反馈开关状态，便于灰度前后核验。

## 自动验证

| 验证 | 结果 |
| --- | --- |
| 后端完整测试 | 120 项通过 |
| 反馈、权限、隐私与追加历史测试 | 通过 |
| SQLite 0019 前向与回滚 | 通过 |
| Python 编译检查 | 通过 |
| 前端 TypeScript/Vue 类型检查 | 通过 |
| 前端 staging 构建 | 通过 |
| `git diff --check` | 通过 |

覆盖的拒绝场景包括：不支持的反馈状态、旧规则版本、没有学员维护权限、组织范围外访问、反馈开关关闭，以及生产只读路径不放行反馈写入。

## 尚未执行

- GitHub Actions 的真实 MySQL 建表、备份恢复和逆序回滚；
- 本地或 staging 的浏览器实际交互验收；
- 真实账号、真实组织范围和业务口径试点；
- 生产 0019 迁移、开关开启或任何生产写入。

## 下一阶段门槛

1. 从干净提交运行 GitHub Actions，确认 API、管理端、容器和 MySQL staging 作业全绿；
2. 在隔离环境使用合成账号核对三种按钮、最新反馈、重复反馈和开关关闭状态；
3. 运营人员先完成任务 14 的真实档案只读口径抽查；
4. 若继续真实账号试点，必须明确账号、组织范围、试点周期、业务负责人和回滚责任人；
5. 生产迁移、部署和反馈写入仍需在当次任务中单独明确批准。
