# 续费跟进筛选与保存优化生产发布核验

日期：2026-08-11

## 结论

续费跟进筛选与保存优化已完成生产发布。API 版本 `seiwajyuku-platform-api-103` 状态正常并承接 100% 流量；管理端静态资源已发布到 `/ops-platform/`。生产环境继续保持只读，续费修改写入仍由服务端拒绝。

## 代码与自动验证

- PR：`#36`；
- 生产代码提交：`13093c0 fix: improve renewal follow-up filtering and saves`；
- GitHub Actions 的管理端、API、API 容器和 MySQL staging 共 4 项作业全部通过；
- `python -m pytest -q`：121 项通过；
- `pnpm typecheck`：通过；
- `pnpm build:staging`：通过；
- `git diff --check`：通过；
- 本次无数据库结构变更，不执行数据库迁移。

## API 发布与门禁

- API 版本：`seiwajyuku-platform-api-103`；
- 发布前先保持 0% 流量，核对版本环境与数据库连接配置指纹后灰度验证并晋升；
- 发布后流量：103 为 100%，101 为 0%；
- `/api/v1/health`：HTTP 200，`status=ok`；
- `/api/v1/system/environment`：`production=true`；
- `deployment_read_only=true`；
- `production_mutations_allowed=false`；
- `identity_admin_writes_enabled=false`；
- 续费周期修改请求实测返回 HTTP 403，写入门禁有效。

## 管理端发布

- 使用 staging 模式构建，以 `/ops-platform/` 为公开路径并连接生产 API；
- 41 个静态文件全部上传成功；
- 生产入口返回 HTTP 200；
- 续费资源 `renewals-BkPyv1Ob.js` 返回 HTTP 200，并包含分中心筛选、默认当月至 12 月、只读提示和保存跟进文案。

## 功能范围

- 支持按分中心、月份、续费状态和姓名查询；
- 默认显示当月至 12 月且未续费的记录；
- 查看详情后保存跟进信息时保留当前记录并显示清晰错误；
- 对跟进阶段、结果长度和状态值进行校验；
- `/event` 与 `/checkin` 现场链路未修改；
- 生产写入、身份管理写入和服务反馈写入门禁未开启。

## 回滚

- API 可将流量回退到 `seiwajyuku-platform-api-101`；
- 管理端可重新发布上一版静态资源；
- 本次无数据库迁移，无需数据库回滚。
