# 学员管理审核 P0 优化发布准备

日期：2026-08-12

## 本次范围

- 保留编辑页完整手机号核对与更换能力；无手机号的历史档案可留空保存其他资料。
- 编辑页改为读取当前档案，页面展示的普通资料字段均可真实保存。
- 年销售额、利润率仅对具有企业敏感资料权限的岗位读取和维护。
- 时间线、服务提示、关怀任务列表统一按任务自身组织范围和保密级别判断。
- 手机号临时查看补充任务组织范围校验。
- 创建和转中心时强制主归属为正式区域分中心，班级和小组只进入学习关系。
- 续费历史导入归属保留不变；个人时间线和到期提示按学员主档当前续费归属判断。

## 隐私与数据边界

- 普通列表、档案详情和时间线继续只返回脱敏手机号。
- 完整手机号仅由受组织范围约束的编辑接口读取，读取动作写入审计但不记录号码。
- 学员更新审计只保存组织、状态、脱敏手机号和变更字段名；不保存完整手机号、企业敏感财务或备注正文。
- 本次无数据库结构变化，不执行生产数据迁移。
- 身份与任职写入、志工邀请、服务提示反馈开关不在本次范围内。

## 自动验证

- 后端全量回归：`128 passed`。
- 前端 TypeScript/Vue 类型检查：通过。
- 前端 staging 构建：通过。
- 新增回归覆盖：
  - 班级范围账号看不到其他分中心的关怀任务元数据和到期提示；
  - 班级组织不能作为学员主归属写入；
  - 编辑页当前普通字段可读取并保存，敏感财务仍按权限隔离；
  - 转中心后续费时间线和提示使用当前归属，导入归属仍作为历史证据保留。

## 正式生产发布与核验

- 业务负责人已在当前任务中明确确认正式生产部署。
- 发布代码：`fef04e9 fix: harden member operations after audit`。
- 正式 API 新版本：`seiwajyuku-platform-api-112`。
- API 按 `5% -> 50% -> 100%` 灰度放量；各阶段健康检查均正常，未登录访问新增编辑接口均返回 `401`。
- 最终流量核验：版本 `112` 状态 `normal`、流量 `100%`、`HasTraffic=true`、`IsReleasing=false`；上一版本 `111` 流量已归零。
- 正式门户已更新，入口资源为 `index-Cgh8zzer.js`，学员管理资源为 `members-BDtB3Pss.js`，均返回 `200`。
- 在线健康检查返回 `ok`；正式环境业务写入已开启，部署只读已关闭。
- 身份与任职写入、志工邀请、服务提示反馈三个独立高风险开关继续保持关闭。
- 本次没有数据库结构迁移，也没有批量修改生产学员数据。

## 发布后边界

- 已完成服务、流量、静态资源、权限拒绝和环境门禁的在线自动核验。
- 登录后的“编辑学员 -> 查看或更换手机号 -> 保存”仍建议由运营账号在正式页面完成一次人工抽检；本次自动浏览器会话超时，未以真实账号代替运营人员提交生产业务数据。
- 如需回退，API 可恢复到版本 `111`，门户可按上一稳定代码 `4d6e451` 重新构建发布；本次无数据库迁移，因此不涉及数据库回滚。
