# 2026-08-12 续费主数据正式生产版确认

## 发布结论

续费跟进台账“学员管理为唯一日常主数据源”已完成正式生产发布确认。当前生产 API 与管理门户均指向已发布版本；学员姓名、归属、班级和小组由学员管理数据库提供，Excel 不作为日常续费操作的数据入口。

## 当前生产状态

- 云托管服务 `seiwajyuku-platform-api` 状态为 `normal`；
- 线上版本为 `seiwajyuku-platform-api-105`，流量比例为 100%；
- API 健康检查返回 `status=ok`；
- 管理门户 `/ops-platform/` 返回 HTTP 200；
- 未登录访问续费周期接口返回 HTTP 401；
- 已发布续费页面资源包含“学员数据唯一来源”提示，且不包含“名单融合预检”日常 Excel 区块。

## 数据与安全边界

- 学员相关信息从学员管理主档读取；续费周期、状态、责任人和跟进记录保留在续费业务表，并以 `member_id` 关联；
- Excel 仅限首次初始化或受控迁移，不提供给日常续费台账操作；
- 生产继续保持 `deployment_read_only=true`、`production_mutations_allowed=false`、`identity_admin_writes_enabled=false`；
- 本次确认未执行学员、续费、组织关系或身份任职业务写入。

## 追溯

- 代码提交：`33fb0a1 fix: use member master as renewal ledger source`；
- 首次发布与回归验证：[[2026-08-11-续费台账学员管理唯一数据源]]；
- 本次为线上状态复核与正式生产版确认，不改变业务数据或写入门禁。
