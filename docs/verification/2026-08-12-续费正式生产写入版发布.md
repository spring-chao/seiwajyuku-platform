# 2026-08-12 续费正式生产写入版发布核验

## 发布结论

续费跟进台账已完成正式生产写入版发布。学员相关数据继续以学员管理数据库为唯一日常主数据源；已授权的续费跟进修改可写入续费业务表，身份与任职写入仍保持关闭。

## 发布前保护

- 已创建生产数据库全量快照 `10104057`，状态 `success`、可用；
- 旧稳定版本 `seiwajyuku-platform-api-105` 在新版本健康核验前持续承接流量；
- 部署过程中未向学员、续费、组织关系或身份任职业务表写入测试数据。

## 最终线上状态

- 最终在线版本：`seiwajyuku-platform-api-110`；
- 流量比例：100%；
- API 健康检查：`status=ok`；
- 环境：`production=true`；
- 正式业务写入：`deployment_read_only=false`、`production_mutations_allowed=true`；
- 身份与任职管理写入：`identity_admin_writes_enabled=false`；
- 未登录访问续费周期接口继续返回 HTTP 401；
- 管理门户 `/ops-platform/` 返回 HTTP 200。

## 数据与回退边界

- 学员姓名、分中心、班级和小组继续从学员管理主档读取；Excel 仅保留首次初始化或受控迁移用途；
- 续费状态、责任人和跟进记录仍通过 `member_id` 与学员主档关联；
- 如出现发布后异常，可先将流量切回 `seiwajyuku-platform-api-105`，数据库恢复以本次快照和业务确认清单为准；
- 本次不启用角色申请、身份任职管理写入或志工邀请写入。

## 关联记录

- 数据源改造与测试：[[2026-08-11-续费台账学员管理唯一数据源]]；
- 正式生产版只读确认：[[2026-08-12-续费主数据正式生产版确认]]；
- 代码提交：`33fb0a1 fix: use member master as renewal ledger source`。
