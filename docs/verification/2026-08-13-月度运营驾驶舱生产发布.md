# 月度运营驾驶舱生产发布核验（2026-08-13）

## 发布结论

“运营总览”已在保留年度 MP 战略视图的基础上，新增月度运营驾驶舱，并正式发布到生产环境。月度视图以学员主档、续费周期、组织关系、活动与班会数据为来源；无可靠来源的课程指标明确显示“未接入”，不以零值代替缺失数据。

## 本次能力

- 本月续费、新增、当前在册、生日、班会、课程和其他活动指标；
- 各分中心当前在册分布；
- 本月生日清单；
- 各班级本月班会序次、日期及待排期状态；
- 本月服务与活动日历；
- 按账号组织范围和权限过滤，避免越权展示；
- 原年度 MP 看板与月度填报能力保持不变。

## 发布内容

- 功能提交：`dc681cc feat: add monthly operations dashboard`；
- GitHub Actions：运行 `31615897127` 成功；
- 后端测试：`133 passed`；
- 前端类型检查与生产配置构建通过；
- CloudBase 云托管版本：`seiwajyuku-platform-api-114`，当前承载 100% 流量；
- 上一稳定 API 版本：`seiwajyuku-platform-api-113`，保留为回退版本且当前流量为 0%；
- CloudBase Hosting 的 `ops-platform/` 共 41 个文件发布成功，失败 0 个；
- 本次无数据库结构迁移，未直接读写生产数据库，也未调用生产业务写入接口。

## 线上自动核验

- `/api/v1/health` 持续返回 HTTP 200；
- 新接口 `/api/v1/analytics/operations-snapshot` 在未登录时返回 HTTP 401，鉴权边界有效；
- 灰度阶段连续 40 轮、全量阶段连续 30 轮健康与鉴权探测均通过；
- 生产门户入口与新主资源返回 HTTP 200；
- 已发布驾驶舱资源包含“各分中心当前在册”“本月服务与活动日历”等新模块；
- 生产环境实时状态为 `production=true`、`deployment_read_only=false`、`production_mutations_allowed=true`、`identity_admin_writes_enabled=false`；本次发布未改变这些既有门禁状态。

## 数据口径与边界

- 学员相关指标以学员管理数据库主档为唯一基础数据入口，Excel 仅用于首次导入，不作为日常查询源；
- 当前在册为当下主档快照，不伪装为历史月末快照；
- 续费归属按续费周期与学员组织关系统计；直属班级与区域分中心分开表达；
- 班会与活动取自现有活动/班会数据；缺少日期的班会显示“待排期”；
- 当前没有可靠课程排期数据源，因此课程指标显示“未接入”。

## 运营侧最终确认

自动化发布、资源、接口、鉴权和运行状态均已通过。已登录生产页的标题和路由已确认为“运营总览”；由于外部浏览器在读取完整登录态页面内容时多次超时，本记录不把该超时误记为业务数据目视验收通过。

运营人员刷新“运营总览”后只需确认三项：月度指标卡有真实值或明确“未接入”、分中心在册分布符合账号范围、班会与活动日历能正常展开。该确认只读，不需要新增或修改任何业务数据。

## 回退方案

- API：将流量切回 `seiwajyuku-platform-api-113`；
- 管理门户：以父提交 `03dfb13` 重新构建并覆盖 `ops-platform/`；
- 本次无数据库迁移，不需要数据库回退。
