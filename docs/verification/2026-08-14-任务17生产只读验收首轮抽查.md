# 任务 17：生产只读验收首轮抽查（2026-08-14）

## 范围与边界

本轮仅执行登录态页面的只读查看，不开启身份与任职、志工邀请或服务提示反馈写入门禁，不修改学员、续费、关怀或签到记录。

## 当前环境核验

- 健康检查：`200 / ok`；
- 全局业务写入当前可用：`production_mutations_allowed=true`、`deployment_read_only=false`；
- 高风险功能仍保持独立关闭：`identity_admin_writes_enabled=false`、`volunteer_service_invitations_enabled=false`、`member_service_signal_feedback_enabled=false`。

## 首轮页面抽查

在学员管理中抽查 3 份在册档案：

- 时间线均展示“服务提示”和“规则只读”标识；
- 页面明确说明提示只依据数据规则，不评价个人、不用于排名；
- 三份样本均显示“当前没有需要提示的事项”，未出现反馈按钮或保存入口；
- 脱敏手机号、服务原文和企业资料未在提示区域泄露；
- 一份小组为空的样本未被错误标记为“学习班关系待核对”，符合规则仅检查正式学习班关系的设计。

## 结论与未完成事项

首轮验证确认只读展示、数据最小化和反馈门禁正常。本次不构成任务 17 完成验收：仍需由业务负责人选取并确认联系方式、学习班关系、关怀到期和续费到期四类真实提示样本（当前无样本的类别须记录为“当前无样本”），并形成仅含内部编号和处理角色的数据质量清单。

后续身份与任职试点仍应按任务 18 在隔离环境进行；不得因全局业务写入已开启而直接开放身份、志工或反馈写入。

## 业务负责人反馈（2026-08-14）

- 联系方式：正常；
- 学习班关系：有问题。吴越三班实际属于吴江分中心，但页面将其及下属小组标为“历史归属，需复核”，且无法选择；
- 关怀到期：当前无数据；
- 续费节点：有问题，续费运营与学员管理的名单和口径仍需继续对齐。

本轮据此将“学习班关系”和“续费节点”转为缺陷修复项。组织关系修复须保留稳定组织 ID 和历史关系，只调整错误的父级归属；圆融二班是否停用须先核验当前学员、下级组织和活动引用，不直接依据页面观感处理。

## 2026-08-15 第二轮：生产只读汇总与组织修复后复核

- 吴越三班重复节点已合并并停用；正式节点保留 5 个启用小组，32 条当前在册关系和 1 个活动。该项已不再构成学习班关系提示来源；
- 服务提示反馈记录为 0，且线上 `identity_admin_writes_enabled`、`volunteer_service_invitations_enabled`、`member_service_signal_feedback_enabled` 均为 `false`；
- 当前聚合待处理量：联系方式待核对 43、学习班关系待核对 102、逾期关怀事项 0、到期未结案续费学员 64；
- 学习班关系待核对按当前主归属汇总：园区 31、姑苏相城 20、新吴 18、张家港 14、吴江 10、昆山 9；
- 联系方式待核对：吴江 12、昆山 8、园区 7、姑苏相城 7、张家港 5、新吴 4；到期未结案续费学员：园区 26、新吴 12、姑苏相城 10、昆山 8、吴江 4、张家港 4；
- 浏览器自动化两次仅读取生产页面均超时，未执行点击或写入。因此本轮不能替代运营人员对真实样本的页面视觉验收；其余数据均通过受控只读查询获得，未查询或记录完整手机号、企业资料、备注或服务原文。

### 结论

任务 17 的技术与数据汇总部分已完成，且“关怀到期无数据”的业务反馈与当前聚合结果一致。联系方式、学习班关系和续费节点仍有实际待处理项，不能标记为全量收口。后续逐条处理必须分别从学员管理和续费运营进入；真实身份与任职隔离试点继续等待逐项身份事实确认，不进入生产写入。

## 2026-08-15 第三轮：正式组织关系唯一展示源

- 根因：学员列表、档案、时间线与续费台账此前可能回退到旧文本班级/小组，或读取已结束的历史组织关系，导致“班级与小组管理”完成调整后不同页面仍可能显示旧归属；
- 修复：以上读取统一只采用 `member_org_relations` 中当前、启用的正式班级/小组关系。已结束关系、已停用组织与旧文本字段不再作为展示回退来源；没有当前正式关系时明确显示为空，并保留“学习班关系待核对”提示；
- 同时服务提示仅将启用正式班级计为有效学习班关系，避免停用班级掩盖待核对项；
- 自动验证：组织管理、续费与服务提示专项 `25 passed`；后端全量 `152 passed`；覆盖“已结束正式关系 + 旧文本仍存在”时，学员与续费页面均不得显示旧班级/小组的回归场景；
- 生产发布：`seiwajyuku-platform-api-138` 经 5% 灰度后升至 100%；灰度和全量后健康检查合计 60/60 成功，`identity_admin_writes_enabled=false`、`member_service_signal_feedback_enabled=false`；
- 生产只读复核：当前学习班关系待核对仍为 102、到期未结案续费仍为 64。这些是待业务逐条维护的真实存量；本次未改写任何学员、班级、小组或续费记录。

## 2026-08-15 第四轮：提示直达业务入口

- 联系方式和学习班关系提示新增“进入学员编辑”，在有 `members:manage` 权限时直接打开当前学员编辑页；无权限时显示明确说明，不产生无效点击；
- 续费节点提示新增“进入续费运营”，仅在有 `renewals:read` 权限时可用；
- 前端 `pnpm typecheck`、staging 构建通过，静态站点 47/47 文件上传成功；生产资源已复核包含两个入口文案及学习班关系规则标识；
- 本轮仍不启用反馈、不创建任务、不自动改写真实学员或续费记录。
