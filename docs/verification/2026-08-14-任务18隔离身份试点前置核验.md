# 任务 18：隔离身份试点前置核验（2026-08-14）

## 本阶段结论

任务 18 的隔离环境技术前置核验已完成；真实账号试点尚未启动。生产身份、岗位、志工任职和技术职责写入继续关闭。

## 自动验证

- 隔离身份种子、身份管理、身份授权和志工邀请回归：`25 passed`；
- 隔离脚本仅允许 `dev`、`test` 或 `staging` 与隔离 SQLite，不连接生产或旧系统数据库；
- 测试覆盖专职、担当志工、同行志工、技术管理员的最小范围校验与邀请边界。

## 生产只读核验

- 生产“身份与任职”页面可访问；
- 页面显示“当前为只读灰度状态：IDENTITY_ADMIN_WRITES_ENABLED 尚未开启”；
- 当前未出现自然人关联、雇佣、岗位、志工任职或技术职责写入入口；
- 本次未修改任何生产账号、权限、组织、学员、续费或签到数据。

## 进入真实隔离试点前仍需确认

每个样本必须提供并确认：账号标识、身份类别、自然人关联依据、雇佣或志工任职依据、岗位或任职键、任职组织与 `UNIT`/`SUBTREE` 范围、起止时间、业务负责人和回滚责任人。任一字段缺失或存在争议时，不进入试点。

## 生产边界

即使生产业务写入开关已启用，也不代表身份与任职写入获批。真实账号试点须先在隔离环境完成至少一个业务周期的核验；生产身份写入另需当次明确批准、限定窗口和回滚责任。
