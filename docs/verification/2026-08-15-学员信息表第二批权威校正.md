# 学员信息表第二批权威校正执行验证

执行时间：2026-08-15（Asia/Shanghai）

生产备份：CloudBase 逻辑备份 `10154226`（`member-authoritative-second-batch-20260815`，成功）

执行编号：`member-authoritative-second-batch-20260815101710`

## 已确认的数据优先级

- 状态、分中心、手机号：以《学员信息表-2026.xlsx》为准。
- 班级、小组：以“班级与小组管理”的稳定组织关系为准；本次不按文档覆盖已有班级或小组关系。
- 新增：仅在身份唯一、手机号不重复、稳定组织 ID 已解析时创建。

## 实际写入范围

| 项目 | 数量 |
| --- | ---: |
| 状态校正 | 110 |
| 分中心校正 | 11 |
| 手机号校正 | 21 |
| 新增学员 | 333 |
| 新增在册 | 67 |
| 新增流失 | 266 |
| 既有档案涉及人数 | 141 |

源文件 SHA-256 前缀：`f4051104a428`。

## 生产后核验

- 学员总数：`840 → 1,173`；在册数：`836 → 793`。在册数变化与 110 条状态校正及 67 名在册新增一致。
- 333 名新增学员均包含加密联系方式、手机号哈希和脱敏展示值。
- 新增关系：333 条主分中心、159 条班级、68 条小组；与预检的稳定组织映射一致。
- 变更审计：474 条；既有学员变更历史：141 条；校验错误：0。
- 既有学员的班级、小组和发展组织字段在执行器逐项后验中保持不变。
- 服务健康检查返回 `ok`；生产运行环境保持 `production`，身份管理写入仍为关闭状态。

## 自动隔离项

以下记录未自动写入：源表手机号重复或缺失、组织未解析、与现有档案发生手机号/身份碰撞，以及 1 条会与已确认班级关系冲突的分中心变更。它们均保留在预检清单中，未覆盖现有数据。
