# 运营节奏中台一期生产发布核验（2026-08-18）

## 发布结论

依据《盛和塾运营节奏中台一期_少数运营人员驱动方案_V1.0》，运营节奏一期已完成后端、管理端和数据库结构发布。现有生日助手保持可用，并作为月度运营节奏中的生日关怀业务节点入口；本次没有自动发送消息、批量补写业务数据或修改签到链路。

2026-08-18 后续自检发现管理端曾以根路径 `/static/` 引用资源，而门户实际部署在 `/ops-platform/`，导致浏览器停留在初始 loading。已将生产构建前缀统一为 `/ops-platform/`，并补齐生产 API 地址；随后以 API 版本 143 完成生日关怀模板优化发布并保持 CloudRun 路由可用。

## 本次发布内容

- 后端新增运营模板、月度周期、运营事项、进度记录和模板审计能力；
- 支持固定日期、星期序号、相对锚点、跨月截止和生日月份节点；
- 管理端新增“今日 / 未来 7 天 / 本月 / 需关注”四种运营视图；
- 生日事项可打开现有生日助手，责任角色与外部协作角色分开显示，不要求外部角色登录；
- 数据库迁移 `0025_operation_rhythm` 已随 CloudRun 启动完成，提供 SQLite/MySQL 回滚脚本；
- 不改变 `/event`、`/checkin` 或旧签到系统资源。

## 自动验证

- 后端全量测试：`157 passed`；
- 管理端 TypeScript/Vue 类型检查：通过；
- 管理端生产构建：通过，构建产物约 2.58 MB；
- `git diff --check`：通过；
- 本地运营节奏专项：覆盖幂等生成、生日节点、日期规则、组织范围、状态进度和审计；
- 生产未登录探针：新增快照、模板、生成接口均返回预期 `401`，没有出现 `404` 或 `500`；现有生日助手接口仍返回预期 `401`。

## 生产发布核验

- CloudRun 服务：`seiwajyuku-platform-api`；
- 在线版本：`seiwajyuku-platform-api-143`；
- 候选版本启动日志：`Application startup complete`，CloudRun 探针 `200`；
- 健康检查：`/health` 和 `/health/live` 均为 HTTP `200`；
- 新路由运行核验：运营节奏快照、模板、生成和生日助手接口在当前版本正确执行鉴权；
- 管理端静态资源：CloudBase Hosting `ops-platform/` 上传 `47/47` 成功；门户 HTTP `200`；远端 `index.html` 已引用 `/ops-platform/static/`，入口 JS、运营总览 JS、运营节奏 JS/CSS 均返回 HTTP `200`，并包含本次模块内容。

## 门禁与后续边界

- 本次没有新增或修改身份管理写入门禁，也没有执行生产业务数据批量写入；
- 线上环境自检结果：`production_mutations_allowed=true`、`deployment_read_only=false`、`identity_admin_writes_enabled=false`；这些是发布前既有状态，本次未改动；
- 运营事项生成、状态更新和模板调整仍要求对应权限，并由服务端做组织范围校验和审计；
- 尚未使用真实登录账号执行运营人员的视觉验收，因此不把未登录接口探针等同于真实页面业务验收。下一步由运营人员登录生产门户，打开“运营总览”确认月度节奏卡片和生日事项入口即可。

## 生日关怀文案优化追加核验（2026-08-18）

- 生日祝福开头统一为“XX学长好，祝您生日快乐！”，去掉“新的一岁”表达，语气更自然亲切，也避免在关怀文案中强调年龄阶段变化。
- 当学员主档姓名已经带“学长”或“学姐”时，生成器不会重复追加称谓，避免出现“学长学长”。
- warm、concise、standard 三种语气均移除“新的一岁”；生日关怀专项测试 2 项通过，后端全量测试 157 项通过。
- 文案由 API 版本 `seiwajyuku-platform-api-143` 生成并已承接 100% 流量；本次未执行业务数据写入、消息发送或身份管理写入。

## 生日名单空白自检与页面容错修复（2026-08-18）

- 生产页面控制台曾出现 `AxiosError: Network Error`；页面原先使用 `Promise.all` 加载运营实况、年度 MP 和运营节奏，只要其中一个请求发生网络失败，就会中断整次加载，生日和学员数据也随之显示为默认空状态。
- API 跨域预检返回 HTTP `200`，健康、存活和 CloudBase 探针均返回 HTTP `200`；生产“学员管理”页仍能读取在册学员，未发现数据被删除的证据。
- 管理端已改为分模块容错加载：运营实况和生日主数据成功时先展示，运营节奏或年度 MP 失败只提示对应模块，不再清空整页；年度方案首次加载失败也不再阻断本月运营数据加载。
- 前端类型检查通过，生产构建通过，CloudBase Hosting `ops-platform/` 上传 `47/47` 成功；线上 dashboard JS 已确认包含 `Promise.allSettled`。

## 学员管理班级筛选追加核验（2026-08-18）

- 学员管理页在原有“全部分中心”和关键词搜索之间新增“全部班级”下拉筛选；班级选项来自当前已加载的正式班级字段，另提供“未分配班级”选项，便于排查班级归属缺失。
- 分中心切换会清空班级条件并重新加载列表；关键词搜索、默认只显示在册和非在册搜索口径保持不变。
- 前端类型检查通过，生产构建通过；CloudBase Hosting `ops-platform/` 上传 `47/47`，线上 `members` 脚本 HTTP `200` 且已包含“全部班级”和“未分配班级”。
- 线上登录页面 DOM 已确认同时显示“全部分中心”和“全部班级”两个下拉控件；本次仅更新前端静态资源，未执行生产数据库、API、身份/IAM 或权限门禁变更。

## 学员管理小组筛选追加核验（2026-08-18）

- 在“全部班级”旁新增“全部小组”下拉筛选；小组选项按当前数据生成，并提供“未分配小组”选项。
- 切换班级会自动清空小组条件，避免保留不属于当前班级的小组筛选；小组筛选与关键词、在册状态口径可以同时生效。
- 前端类型检查通过，生产构建通过；CloudBase Hosting `ops-platform/` 上传 `47/47`，线上 `members` 脚本 HTTP `200` 且已包含“全部小组”和“未分配小组”。
- 本次仍仅更新前端静态资源，未执行生产数据库、API、身份/IAM 或权限门禁变更。
