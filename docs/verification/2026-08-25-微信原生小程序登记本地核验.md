# 微信原生小程序登记本地核验

更新时间：2026-08-25

## 已完成

- 新增 `apps/wechat-miniprogram` 原生小程序工程，不使用 `web-view`。
- 新增移动端登记页，覆盖基础资料、企业资料、学习与入塾意愿、隐私同意和提交成功状态。
- 新增 `POST /api/v1/enrollment-links/{link_id}/mini-program-code`，后端使用服务端 AppID/AppSecret 调用微信小程序码接口，响应只返回临时图片数据，不保存小程序码图片或微信凭证。
- 管理后台把小程序码作为主入口，H5 链接和二维码保留为备用入口。
- 新建/轮换入口令牌调整为短 URL-safe 随机值，旧 H5 令牌仍可由现有解析逻辑兼容。

## 验证结果

- `apps/platform-api/tests/test_enrollment.py`：9 passed。
- Python API 编译检查通过。
- 小程序 JSON 配置解析通过。
- 管理端 `pnpm typecheck` 通过。
- 入塾 API 与管理端改动文件 ESLint 通过。

## 尚未执行

- 未配置真实微信 AppID/AppSecret，未调用真实微信接口生成生产小程序码。
- 未提交微信公众平台审核，未配置生产 request 合法域名和隐私保护指引。
- 未部署本次 V1.1 变更；发布前需取得当前任务的生产写入/部署确认，并重新核验 API、静态资源、微信配置和回滚条件。
