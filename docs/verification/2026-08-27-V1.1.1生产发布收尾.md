# V1.1.1 生产发布收尾

## 发布基线

- 版本：V1.1.1
- API 发布 commit：`3aa1db837998a7f56b450bb07df8d6e253e75f75`
- CloudRun revision：`seiwajyuku-platform-api-186`
- GitHub Actions：`33024629515`，5 个任务全部成功
- Git tag：`v1.1.1`，指向 API 发布 commit `3aa1db8`
- 数据库迁移：`0033 → 0034`，最终版本 `0034_enrollment_political_social_role.sql`
- 发布前生产备份：`10307903`

## 发布结果

- `0033`、`0034` 已按顺序执行并完成字段及迁移记录核验。
- API 已完成灰度检查并提升至 100% 流量，发布状态为成功。
- 管理端静态资源已上传至 `/ops-platform/`，本次上传 56/56 成功。
- `/__tcb_probe__`、`/health/live`、`/health`、`/api/v1/health`、`/api/v1/system/environment` 发布后连续 50 次检查全部 HTTP 200。
- 生产环境仍保持 `identity_admin_writes_enabled=false`；未执行学习计划 B2、正式微信小程序发布、身份写入或学员积分入账。

## 烟测申请收尾

- 申请：`ENR-20260827-0CB7738B`（数据库 ID 3）
- 原状态：`SUBMITTED`
- 收尾状态：`CANCELLED`（内部发布烟测完成，不进入正式入塾）
- `converted_member_id`：`NULL`
- 未产生正式学员或积分记录。
- 已写入审计记录：`enrollment.application.test_cancel`，审计 ID 5003。

## 线上版本信息

管理端 Vite 构建增加 `build-info.json`，由构建插件自动写入：

- `commit_sha`
- `build_time`
- `version`
- `environment`

构建时可通过 `VITE_BUILD_COMMIT_SHA`、`VITE_BUILD_TIME`、`VITE_RELEASE_VERSION`、`VITE_BUILD_ENV` 固定发布指纹；未提供时回退到 Git HEAD、构建时间、包版本和 Vite mode。

本次收尾实现提交：`9dbe1977d513d4456ee5c8348befc3c61e8e1f7b`；对应 CI `33027720478` 成功。线上 `/ops-platform/build-info.json` 已回读为 `version=1.1.1`、`environment=production`。

## 观察与回滚

- 发布后最近 30 分钟 CloudRun 日志未发现 `ERROR`、`Traceback` 或 `Exception`。
- 未发生回滚；revision 185 保留为应用回滚基线。
- 数据库新增字段保持兼容，不执行机械 DROP。
