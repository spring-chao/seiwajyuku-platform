# 全班级学习计划基线只读 DRY-RUN

日期：2026-09-03
环境：CloudBase 正式环境，只读查询
结论：`DRY-RUN COMPLETE`，生产写入未执行

## 读取范围

使用 CloudBase CLI `tcb db execute --read-only` 读取：

- 当前启用的 `CLASS` / `SPECIAL_COHORT` 组织节点；
- 每个节点的活动学习计划绑定；
- 活动绑定已实际打开的最新运行周期、班会状态和小组会策略。

未读取学员手机号等个人字段，未执行绑定、周期、组织、审计或其他生产写入。

## 生产只读结果

| 指标 | 数量 |
| --- | ---: |
| 当前启用正式班级节点 | 28 |
| 已有活动学习计划绑定 | 1 |
| 已有绑定且与本次基线一致 | 1（吴越二班） |
| 已解析 ID、待首次绑定/复核 | 17 |
| 已按 ID 标记人工确认 | 10 |
| 业务简称未能安全关联到生产名称 | 1（不一班 → 苏州不一班候选） |

本轮基线文件为：
`data/learning-plans/class-learning-plan-migration-baseline-2026-09.json`。

## 关键组织 ID 结论

生产当前同时存在两套炎武一至四班节点：

- 昆山分中心下的 UUID 节点作为本次业务基线候选；
- `org-yanwu-1` 至 `org-yanwu-4` 为苏州直属同名 active 节点，单独列为 `MANUAL_REVIEW_REQUIRED`。

两套节点未按名称合并、停用或迁移。生产 ID 以 2026-09-03 的只读组织清单为依据；后续任何数据修正仍须在执行窗口再次核对。

## DRY-RUN 分类

- `NO_CHANGE`：吴越二班，现有 `standard-3y` / `2026` / 4 月模板 / 第 5 周期 / `PLANNED`。
- `MISSING_BINDING_REVIEW`：17 个已具备业务预期且 ID 已解析的班级。该结果只表示需要逐班复核后首次绑定，不代表可以批量套用默认模板。
- `MANUAL_CONFIRMATION`：真干二班、真干三班、真干班、苏州不一班、先锋班、神仙班，以及 4 个旧炎武直属同名节点。
- `NEEDS_ID_LOOKUP`：业务简称“不一班”，不能自动替换为“苏州不一班”。

尤其是：

- 炎武二班的预期仍是 2026 版、10 月模板、第 11 周期、班会 `POSTPONED`，小组会策略保持 `REQUIRED`；
- 振华一班的预期是正式开始前 `current_cycle=null` / `NOT_STARTED`，不能通过首次绑定表单提前制造第 1 周期；
- 真干三班只记录日历显示第 5 次班会，模板和实际周期未确认，不进入自动修正。

## 后续执行门槛

本报告不授权生产批量写入。下一步需先逐班确认：

1. 17 个 `MISSING_BINDING_REVIEW` 班级的版本、模板、正式开始时间和历史周期事实；
2. 炎武旧直属同名节点是否仍有独立运营事实；
3. “不一班”与“苏州不一班”是否为同一业务班级；
4. 真干二班、真干三班、真干班及直属班的历史班会记录。

确认后只能按组织 ID 逐班执行，并保留 audit、历史轮次和原有学习事实；暂停只做状态调整，不创建虚假班会、不推进周期、不新开轮次。
