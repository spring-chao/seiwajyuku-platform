# C7.1.2.3｜权威平台快照包与吴越三班最终真实对账准备报告

日期：2026-09-07
基线：`main @ 7161d8ae208325e45fc597e151980bb0e3229a58`

## 结论

已完成可交付给当前平台数据所有者 / DBA 的最小化只读快照包，以及一个不连接任何数据库的本地完整性检查工具。本阶段没有连接、读取、导出或修改生产数据库；没有执行生产迁移、部署、来源事实写入、学分账本写入或正式结算。

真实权威快照尚未交付，因此本报告不把任何真实平台业务对账标为通过。

## 本次交付

```text
docs/operations/c7-1-2-3-readonly-snapshot/
  readonly-snapshot.mysql.sql
  field-whitelist.md
  manifest.template.json
  credit-scope.template.json
  README.md

apps/platform-api/scripts/validate_c7_authoritative_snapshot.py
apps/platform-api/tests/test_c7_1_2_3_snapshot_package.py
```

快照包固定范围为吴越三班和 2026-08-31 至 2026-09-04。它要求：

- 先导出同名班级候选，只有唯一有效 `CLASS` 才能继续；
- 保留目标班级、直属父组织参考和历史有效的直属小组；
- 保留 `STUDY_CLASS` / `STUDY_GROUP` 的原始 `valid_from` / `valid_until`，不以当前归属替代历史事实；
- 保留该班级全部 binding/cycle 及冻结规则引用；
- 只在隔离库确实缺少时补充最小冻结规则或 2026 `CHINA_MAINLAND` 日历引用；
- 不查询 `learning_credit_entries`、不相关班级、全量通讯录、明文手机号、关怀备注或任何凭据。

离线检查工具会校验：manifest 与所有 CSV 的 SHA-256、字段白名单、目标班级候选唯一性、组织有效期、小组映射、冻结规则引用、学习轮次覆盖，以及安全身份确认后每个积分范围人员在每个事实日期的唯一班级/小组关系。它不含数据库驱动，也不写任何业务表。

本地新增工具专项验证：

```text
python -m pytest tests/test_c7_1_2_3_snapshot_package.py -q
4 passed
```

测试覆盖：完整快照结构、身份确认前不伪称人员对账完成、安全身份范围后的历史关系覆盖、同名班级候选重复阻断、缺少历史小组关系阻断。

## 当前门禁

| 门禁 | 当前状态 | 说明 |
| --- | --- | --- |
| `PLATFORM_SNAPSHOT_SOURCE` | `BLOCKED / AUTHORIZED_READ_ONLY_SNAPSHOT_REQUIRED` | 包已就绪，等待数据所有者在受控只读事务中导出并签署 manifest |
| `PLATFORM_SNAPSHOT_INTEGRITY` | `NOT_RUN` | 等待真实 CSV 与 SHA-256 manifest |
| `ORG_HISTORICAL_COVERAGE` | `NOT_RUN` | 等待吴越三班、1–5 组、精进组的权威历史组织节点 |
| `MEMBER_RELATION_COVERAGE` | `NOT_RUN` | 等待正常组和精进组人员的历史 `STUDY_CLASS` / `STUDY_GROUP` 关系 |
| `LEARNING_ROUND_COVERAGE` | `NOT_RUN` | 等待可解释五个事实日期的 binding/cycle |
| `HQ_IDENTITY_RECONCILIATION_CREDIT_SCOPE` | `NOT_RUN` | 必须在隔离库中用班级、小组、姓名、脱敏账号和已确认绑定安全核验 |
| `NO_GROUP_IDENTITY_COVERAGE` | `NOT_RUN / separately excluded` | 空小组保留审计，但不阻断正常组/精进组 |
| `CLASS_RATE_RECONCILIATION` | `NOT_RUN` | 权威关系确认后按五天 `39/39=100%` 人工基准核对 |
| `CREDIT_DRY_RUN_RECONCILIATION` | `NOT_RUN` | 只能在隔离库且开关关闭时运行 |
| `ZERO_LEDGER_WRITE` | `NOT_RUN` | 最终 DRY-RUN 前后必须重新证明账本增量为 0 |
| `REAL_FACT_RECONCILIATION` | `BLOCKED / PLATFORM_MEMBER_SNAPSHOT_MISSING` | 尚未取得权威成员、组织关系和学习轮次快照 |
| `C7.2` | `NOT_READY` | 正式结算保持关闭 |

```text
LEARNING_CREDIT_SETTLEMENT_ENABLED=false
```

## 收到快照后的唯一后续步骤

在新的隔离数据库中按如下顺序执行：

```text
权威只读快照
→ 本地完整性检查
→ 0050
→ 同一份总部 Excel（230 observations / 46 identities）
→ 正常组 + 精进组身份安全确认
→ DAILY_READING DRY-RUN
→ 五天班级率 + 逐条积分人工对账
→ ledger_entries_delta = 0
```

空小组五人无论身份是否最终确认，都保持：

```text
personal_credit_eligible = false
class_rate_denominator_eligible = false
class_rate_numerator_eligible = false
```

只有正常组和精进组积分/统计范围人员 100% 安全确认、没有历史关系冲突、学习轮次完整、班级率和 DRY-RUN 无未解释差异、且账本零写入时，才允许把 `REAL_FACT_RECONCILIATION` 改为 `PASS`。在此之前不得进入 C7.2。
