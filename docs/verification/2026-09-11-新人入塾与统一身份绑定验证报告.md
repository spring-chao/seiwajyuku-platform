# 新人入塾与统一身份绑定验证报告

## 结论

本轮“新人入塾选择 + 小程序统一身份绑定 V2”已完成代码实现和自动化验证。

- 开发实现：PASS
- 后端全量回归：PASS（611 passed、2 skipped、1 warning）
- 微信身份/入塾定向回归：PASS（新增及相关定向用例通过）
- 小程序 Node 测试：PASS
- 管理端类型检查：PASS
- 管理端 staging build：PASS
- 0061/0062/0063 三塾业务配置迁移：生产已按顺序执行并完成只读核验
- 生产 API：PASS（revision `seiwajyuku-platform-api-245`，BuildId `2602394938`，100% 流量；旧 revision 244 保留）
- 管理端静态托管：PASS（66 个文件上传，线上 `build-info.json` 指向 `56692fd`）
- 微信体验版上传：PASS（正式 AppID，版本 `2026.09.11.1`）
- 三塾真实收款、联系人及年度费用/地址资料：已写入生产并通过配置隔离核验
- Volunteer 2.0、IAM2 授权事实：未修改

## 实现内容

### 新学长入塾申请

- 公开入口先显示苏州塾、常州塾、无锡塾选择；江南塾不作为申请目标。
- 选择后通过 `GET /api/v1/public/enrollment/{token}?target_shuku_org_unit_id=...` 重新读取目标塾 metadata，再进入共用资料表。
- 提交继续保存 `target_shuku_org_unit_id`；审核后的 `org_unit_id` 不被替代或反推。
- 第 05 部分只消费 `enrollment_shuku_profiles`、`enrollment_shuku_profile_terms` 和 `enrollment_shuku_contacts` 返回的加入说明、缴费、服务地址和多人联系方式；没有配置时显示 `BUSINESS_CONFIG_REQUIRED`，不再展示旧的收款账户和联系人硬编码。
- 三塾年度学员服务费统一为 4800 元/人/年，同时适用于首次入塾和年度续费；苏州保留原缴费说明和服务地址，常州、无锡由 0063 补入已确认服务地址。
- 收款账号和联系人支持一键复制；页面不提供直接支付按钮。

### 统一身份绑定

- 新增 `POST /api/v1/wechat/person-bindings/verify`。
- 服务端以姓名 + 标准化手机号哈希查询 `members`、`member_identities`、`employee_profile_details` 和 `operations_employments`，按 `person_id` 去重。
- 0 个候选、多个候选和唯一候选分别返回安全提示；不会让用户用塾或身份选择器绕过重复资料治理。
- 唯一候选可返回学员、志工和专职人员复合身份；专职候选要求微信手机号所有权验证，且仍受账号启用、在职和 IAM2 授权门槛控制。
- 旧兼容接口和旧工作人员绑定页保留，普通首页不再显示工作人员独立绑定入口。

## 自动化验证

```text
python -m pytest -q tests
611 passed, 2 skipped, 1 warning

node apps/wechat-miniprogram/tests/*.test.js
全部通过

pnpm run typecheck
通过

pnpm run build:staging
通过（Vite 2133 modules）
```

新增覆盖包括：

- 目标塾 metadata 按选择加载和配置缺失状态；
- 0061 SQLite 迁移、空数据回滚和有配置时回滚阻断；
- 0062 三塾配置、费用/服务地址、六条联系人记录、幂等执行和有业务数据时回滚阻断；
- 0063 三塾年度费用统一为 4800、常州/无锡服务地址、业务改写冲突保护和保守回滚；
- 续费行动卡与入塾 metadata 读取同一年度服务费，并声明适用 `ENROLLMENT` / `RENEWAL`；
- 学员单身份、工作人员手机号门槛、同一 person 的复合身份和重复资料阻断；
- 小程序统一绑定 endpoint、手机号授权按钮和去除普通工作人员独立入口；
- 入塾页移除旧银行账号、联系人和固定缴费主体，并验证所有新账号/电话不出现在小程序静态代码中。

生产业务配置门禁：

```text
THREE_SHUKU_ANNUAL_FEE_4800 = PASS
CHANGZHOU_SERVICE_ADDRESS = PASS
WUXI_SERVICE_ADDRESS = PASS
ENROLLMENT_RENEWAL_SHARED_FEE = PASS
MULTI_CONTACT_SUPPORT = PASS
```

## 发布边界

本轮生产执行记录：

- 发布源为干净 `origin/main` SHA `56692fddb5ebb79bb171855d664e25aea9ab51f7`。
- 发布前新建快照被 CloudBase 以 `ServerlessInstanceStatusDenied` 拒绝（备份服务判定实例为 pause）；未唤醒或改动资源，采用最近可用快照 `10506344` 作为回滚基线（`success` / `IsUsable=yes`）。
- 生产数据库按 `0061 → 0062 → 0063` 顺序执行并登记；三塾费用均为 4800 元/人/年、每塾 2 位有效联系人，常州/无锡服务地址与业务确认一致，苏州既有资料保持不变。
- API 先 5% 灰度观察 5 轮：`/__tcb_probe__`、`/health/live`、`/health`、`/api/v1/health`、`/api/v1/system/environment`、`/api/v1/system/build-info` 均为 HTTP 200，未授权学员接口为 401；随后提升到 100%。API build-info 仍返回 `unknown`，不影响本次业务发布。
- 管理端上传到 `ops-platform` 既有路径，未删除历史哈希资源；线上入口 HTTP 200。
- 微信开发者工具上传成功；公众平台中将该版本设为体验版、生成二维码及真机业务验收仍由业务人员完成。
