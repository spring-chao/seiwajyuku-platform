# 新人入塾与统一身份绑定验证报告

## 结论

本轮“新人入塾选择 + 小程序统一身份绑定 V2”已完成代码实现和自动化验证。

- 开发实现：PASS
- 后端全量回归：PASS（611 passed、2 skipped、1 warning）
- 微信身份/入塾定向回归：PASS（新增及相关定向用例通过）
- 小程序 Node 测试：PASS
- 管理端类型检查：PASS
- 管理端 staging build：PASS
- 0061/0062/0063 三塾业务配置迁移：生产已按顺序执行并完成只读核验
- 生产 API：PASS（revision `seiwajyuku-platform-api-245`，BuildId `2602394938`，100% 流量；旧 revision 244 保留）
- 管理端静态托管：PASS（66 个文件上传，线上 `build-info.json` 指向 `56692fd`）
- 微信体验版上传：PASS（正式 AppID，版本 `2026.09.11.1`）
- 三塾真实收款、联系人及年度费用/地址资料：已写入生产并通过配置隔离核验
- Volunteer 2.0、IAM2 授权事实：未修改

## 本次 P0 体验修正（开发与测试，尚未发布）

本次在已发布基线之上收口四项真实验收问题；本节只记录代码变更和本地验证，未执行生产部署或微信体验版上传。

本次代码提交：`a67e833550d04c9e761e3ceff5ecb40748273065`；验证记录修订提交：`a38da7088ea7690567dba20fc0f6403a51b81ead`。

### 选塾与缴费说明

- 小程序选塾页改为苏州塾、常州塾、无锡塾三个直接可见的选择卡片；选中状态立即反馈，按钮改为“下一步，填写资料”。
- 选塾状态显式维护 `selectedTargetShukuId` / `selectedTargetShukuName`，不再在 WXML 通过 `indexOf()` 反查显示名称。
- 第 05 部分拆为“（一）加入守则”和“（二）缴费说明”；共用正式加入守则由 API `joining_rules` 返回，塾级账号、联系人、费用和地址继续从业务配置读取。
- 收款账号和联系人手机号改为点击文本本身复制，移除“复制/复制号码”按钮；服务地址单独换行展示。

### 工作人员统一绑定

- 姓名 + 手机号仍统一调用 `/api/v1/wechat/person-bindings/verify`，工作人员首次绑定继续要求微信手机号所有权验证。
- 已增加回归用例确认：人员解析成功后，若 `WECHAT_STAFF_MOBILE_OPERATIONS_ENABLED=false`，接口明确返回“工作人员移动运营功能尚未开启”；未修改人员查询、IAM2 或 Volunteer 2.0 事实。
- 示例环境文件显式保留该开关且默认关闭；本次未修改任何生产功能开关。后续发布时只需按审批开启 `WECHAT_STAFF_MOBILE_OPERATIONS_ENABLED=true`，不要连带开启其他实验开关。

### 本次验收门禁

```text
SHUKU_SELECTION_VISIBLE_NAME = PASS
SHUKU_SELECTION_NO_PLACEHOLDER_BUG = PASS
SHUKU_SELECTION_UI = PASS
COMMON_JOINING_RULES_VISIBLE = PASS
SHUKU_PAYMENT_CONFIG_VISIBLE = PASS
FEE_4800_VISIBLE = PASS
ACCOUNT_TAP_TO_COPY = PASS
PHONE_TAP_TO_COPY = PASS
NO_COPY_BUTTONS = PASS
STAFF_NAME_PHONE_RESOLUTION = PASS
STAFF_WECHAT_PHONE_VERIFICATION = PASS
STAFF_MOBILE_FEATURE_ENABLED = CODE/PENDING_PRODUCTION_FLAG
STAFF_ONLY_BINDING = PASS
MULTI_IDENTITY_BINDING = PASS
IAM2_REGRESSION = PASS
VOLUNTEER2_REGRESSION = PASS
```

### 本次自动化执行结果

```text
后端定向回归（入塾 + 统一身份绑定）：25 passed
小程序全部 Node 用例：PASS
compileall：PASS
管理端 typecheck：PASS
管理端 staging build：PASS（Vite 2133 modules，2.95 MB）
git diff --check：PASS
```

后端全量 pytest 首次结果为 `611 passed, 2 skipped, 1 failed`。唯一失败是 migration
manifest 门禁对 `migrations/mysql/0035_wechat_member_bindings.sql` 的字节 SHA 校验。
复核 Git 历史确认 0035 及 manifest 均未被本轮修改；根因是 manifest 指纹使用 Git
规范化的 LF 内容，而 Windows `core.autocrlf=true` 工作区将同一批 SQL 检出为 CRLF。
验证器已改为仅接受原始、LF、CRLF 三种等价换行表示，不放宽任何实际内容变化。
修复后全量 pytest 为 `613 passed, 2 skipped, 1 warning`。

## 实现内容

### 新学长入塾申请

- 公开入口先显示苏州塾、常州塾、无锡塾选择；江南塾不作为申请目标。
- 选择后通过 `GET /api/v1/public/enrollment/{token}?target_shuku_org_unit_id=...` 重新读取目标塾 metadata，再进入共用资料表。
- 提交继续保存 `target_shuku_org_unit_id`；审核后的 `org_unit_id` 不被替代或反推。
- 第 05 部分只消费 `enrollment_shuku_profiles`、`enrollment_shuku_profile_terms` 和 `enrollment_shuku_contacts` 返回的加入说明、缴费、服务地址和多人联系方式；没有配置时显示 `BUSINESS_CONFIG_REQUIRED`，不再展示旧的收款账户和联系人硬编码。
- 三塾年度学员服务费统一为 4800 元/人/年，同时适用于首次入塾和年度续费；苏州保留原缴费说明和服务地址，常州、无锡由 0063 补入已确认服务地址。
- 收款账号和联系人支持一键复制；页面不提供直接支付按钮。

### 统一身份绑定

- 新增 `POST /api/v1/wechat/person-bindings/verify`。
- 服务端以姓名 + 标准化手机号哈希查询 `members`、`member_identities`、`employee_profile_details` 和 `operations_employments`，按 `person_id` 去重。
- 0 个候选、多个候选和唯一候选分别返回安全提示；不会让用户用塾或身份选择器绕过重复资料治理。
- 唯一候选可返回学员、志工和专职人员复合身份；专职候选要求微信手机号所有权验证，且仍受账号启用、在职和 IAM2 授权门槛控制。
- 旧兼容接口和旧工作人员绑定页保留，普通首页不再显示工作人员独立绑定入口。

## 自动化验证

```text
python -m pytest -q tests
613 passed, 2 skipped, 1 warning

node apps/wechat-miniprogram/tests/*.test.js
全部通过

pnpm run typecheck
通过

pnpm run build:staging
通过（Vite 2133 modules）
```

新增覆盖包括：

- 目标塾 metadata 按选择加载和配置缺失状态；
- 0061 SQLite 迁移、空数据回滚和有配置时回滚阻断；
- 0062 三塾配置、费用/服务地址、六条联系人记录、幂等执行和有业务数据时回滚阻断；
- 0063 三塾年度费用统一为 4800、常州/无锡服务地址、业务改写冲突保护和保守回滚；
- 续费行动卡与入塾 metadata 读取同一年度服务费，并声明适用 `ENROLLMENT` / `RENEWAL`；
- 学员单身份、工作人员手机号门槛、同一 person 的复合身份和重复资料阻断；
- 小程序统一绑定 endpoint、手机号授权按钮和去除普通工作人员独立入口；
- 入塾页移除旧银行账号、联系人和固定缴费主体，并验证所有新账号/电话不出现在小程序静态代码中。

生产业务配置门禁：

```text
THREE_SHUKU_ANNUAL_FEE_4800 = PASS
CHANGZHOU_SERVICE_ADDRESS = PASS
WUXI_SERVICE_ADDRESS = PASS
ENROLLMENT_RENEWAL_SHARED_FEE = PASS
MULTI_CONTACT_SUPPORT = PASS
```

## 发布边界

本轮生产执行记录：

- 发布源为干净 `origin/main` SHA `56692fddb5ebb79bb171855d664e25aea9ab51f7`。
- 发布前新建快照被 CloudBase 以 `ServerlessInstanceStatusDenied` 拒绝（备份服务判定实例为 pause）；未唤醒或改动资源，采用最近可用快照 `10506344` 作为回滚基线（`success` / `IsUsable=yes`）。
- 生产数据库按 `0061 → 0062 → 0063` 顺序执行并登记；三塾费用均为 4800 元/人/年、每塾 2 位有效联系人，常州/无锡服务地址与业务确认一致，苏州既有资料保持不变。
- API 先 5% 灰度观察 5 轮：`/__tcb_probe__`、`/health/live`、`/health`、`/api/v1/health`、`/api/v1/system/environment`、`/api/v1/system/build-info` 均为 HTTP 200，未授权学员接口为 401；随后提升到 100%。API build-info 仍返回 `unknown`，不影响本次业务发布。
- 管理端上传到 `ops-platform` 既有路径，未删除历史哈希资源；线上入口 HTTP 200。
- 微信开发者工具上传成功；公众平台中将该版本设为体验版、生成二维码及真机业务验收仍由业务人员完成。

## Migration manifest SHA 门禁收口（2026-09-11）

- `MIGRATION_SHA_ROOT_CAUSE`：Git LF 与 Windows 工作区 CRLF 的换行规范化差异，不是迁移内容被修改。
- `AFFECTED_MIGRATION`：manifest 所列 0035–0042 的 SQL 文件引用均存在该字节表示差异；首次失败点为 0035 forward.mysql。
- `BEFORE_SHA`：0035 forward.mysql 工作区原始字节 `b14f34695770695e6a3b098452d7bb2f563249b77edbc7be0312ff64362538e1`。
- `EXPECTED_SHA`：manifest 记录 `48b8446f00f19d63bea93c72c0072f54be055b6399216bb921a7f705dcd8e78c`；当前文件换行归一化后完全一致。
- `FIX_METHOD`：只修改 `scripts/build_provenance.py` 的迁移文本 SHA 校验，保留 manifest 与迁移文件不变；新增换行兼容及内容变更拒绝测试。
- `FULL_PYTEST`：`613 passed, 2 skipped, 1 warning`。
- 本次仅提交验证器与回归测试，未执行生产部署、数据库迁移或微信体验版上传。
