# G5.4-C2B 班级与成员身份审核收口报告

核查日期：2026-09-18（Asia/Shanghai）
范围：`苏州分中心2026年学分.xlsx` 的隔离解析、权威只读快照和审核台账模型。
本报告不输出成员姓名、完整账号或手机号；候选包只允许保存脱敏手机号。

## 门禁结论

| 门禁 | 结果 |
| --- | --- |
| 班级候选生成 | PASS |
| 班级自动确认 | `0`（别名和历史节点不自动确认） |
| 570 条高置信成员批量确认机制 | PASS（仅隔离 staging 执行） |
| 90 条班级映射歧义 | BLOCKED / 待业务确认 |
| 63 条成员未找到分类 | PASS（分类完成，仍不得入账） |
| 昆山“精进组” | 独立 `SOURCE_IS_GROUP_LABEL`，不创建伪班级 |
| 生产写入/历史 ledger | `0` |

## 快照和可重放证据

| 项目 | 值 |
| --- | --- |
| `snapshot_id` | `c2a-20260918-c4f397fbe1f0` |
| `snapshot_fingerprint` | `c4f397fbe1f0a07b97b4378acbf30a7651e687169e4e262df1febd13059b71e1` |
| 匹配算法 | `C2A-HISTORICAL-MATCH-V1` |
| 来源行数 | `723` |
| 自动匹配 | `570` |
| 班级映射歧义 | `90` |
| 成员未找到 | `63` |

批量确认要求同时提供快照 ID、指纹、算法版本和预期数量；数量或指纹变化会在同一事务内中止。隔离 staging 中已用上述证据完成一次可重放的 570 条批量审核，重复执行保持幂等且 `ledger_writes=0`。这不是生产成员主档写入，也不是历史学分入账。

## 班级映射

| 映射状态 | 数量 |
| --- | ---: |
| `EXACT` | 18 |
| `AMBIGUOUS` | 3 |
| `NOT_FOUND` | 6 |
| `CONFIRMED_ALIAS` | 0 |

90 条来源行的歧义按来源班级分组为：吴越三班 `34`、不一班 `27`、真干一班 `29`。本轮只保存候选和证据，不把苏州不一班、真干一班或吴越三班新旧历史组织节点自动确认为同一业务班级。

班级人工确认受到候选集合约束：确认的组织 ID 不在当前快照候选范围内会被拒绝；确认后才在同一事务中重新执行该来源班级的历史成员匹配。这样不会出现“先确认班级、再留下旧成员结果”的半完成状态。

## 63 条 `NOT_FOUND` 行级分类

| 分类 | 行数 | 处理 |
| --- | ---: | --- |
| `NO_MEMBER_CANDIDATE` | 31 | 保留未找到，待主档/来源复核 |
| `MEMBER_EXACT_NAME_OUTSIDE_CLASS` | 12 | 不跨班级强行归属 |
| `SOURCE_IS_GROUP_LABEL` | 10 | 精进组是来源组别标签，不创建伪班级 |
| `SOURCE_CLASS_MISSING` | 9 | 来源没有班级身份，不猜测 |
| `MULTIPLE_GLOBAL_CANDIDATES` | 1 | 重名必须人工确认 |
| `MEMBER_EXACT_NAME_IN_CENTER` | 0 | — |
| `MEMBER_NORMALIZED_NAME_CANDIDATE` | 0 | 不以模糊姓名自动确认 |
| `INACTIVE_MEMBER_CANDIDATE` | 0 | 历史有效关系已纳入判断 |

昆山/精进组的 10 条来源行独立归入 `SOURCE_IS_GROUP_LABEL`；它们的原始事实仍保留，但不生成个人成员绑定、班级率或 ledger。

## 候选数据和审计

审核工作台的候选包包含：候选 `member_id`、候选姓名、候选状态、平台班级摘要、候选原因和 `phone_masked`。代码、报告、CI 和提交内容不保存完整手机号。所有人工确认、批量确认和拒绝/待补源决定都写入统一 `learning_credit_import_decisions`，带快照、算法、原因、操作人和幂等键；本轮没有任何正式 ledger 写入。

## 当前边界

90 条班级映射和 63 条成员未找到仍是业务审核队列，不得因为 DRY-RUN 能运行就视为可入账。生产别名确认、成员主档修改、历史学分 POST、正式结算开关和生产迁移均未执行。
