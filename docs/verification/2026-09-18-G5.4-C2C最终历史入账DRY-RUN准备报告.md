# G5.4-C2C 最终历史入账 DRY-RUN 准备报告

核查日期：2026-09-18（Asia/Shanghai）
环境：全新隔离 SQLite staging；来源 Excel 和权威快照均为本地私有验证输入。
本报告不是正式入账批准。

## 来源和解析基线

| 项目 | 结果 |
| --- | ---: |
| 来源行 | 723 |
| 来源 item | 10,122 |
| 来源规则身份 | `LEGACY_SUZHOU_2026_V1` |
| 身份自动匹配 | 570 |
| 班级映射歧义 | 90 |
| 成员未找到 | 63 |
| 学分校验 PASS | 706 |
| `TOTAL_MISSING` | 15（隔离审核已批准计算总分） |
| `ZERO` | 2（隔离审核已确认无学分） |
| 审核决策 | 19 |

历史文件始终按 `LEGACY_SUZHOU_2026_V1` 解释；本次没有调用 `STANDARD_3Y_2026`，也没有把现行规则倒算到历史分值。

## 最终 DRY-RUN 结果

| 轨道 | item 数 | 拟得分 |
| --- | ---: | ---: |
| `HISTORICAL_BASELINE` | 7,122 | 119,076 |
| `YEAR_ACCEPTED` | 1,080 | 23,780 |
| **READY 合计** | **8,202** | **142,856** |
| BLOCKED | 1,920 | 33,680 |

9 月双轨单独保留为 `138` 条、`2,049` 分；其中当前可识别结果中仍有 `111` 条、`1,613` 分因 `DUAL_TRACK_PENDING` 阻断，整批不进入本次 READY 汇总。

### BLOCKED item 分类

| 原因 | item 数 | 分值 |
| --- | ---: | ---: |
| `DUAL_TRACK_PENDING` | 111 | 1,613 |
| `NO_MEMBER_CANDIDATE` | 290 | 4,868 |
| `MEMBER_EXACT_NAME_OUTSIDE_CLASS` | 137 | 2,503 |
| `CLASS_MAPPING_BLOCKED` | 1,275 | 22,972 |
| `MULTIPLE_GLOBAL_CANDIDATES` | 23 | 418 |
| `SOURCE_CLASS_MISSING` | 37 | 657 |
| `SOURCE_IS_GROUP_LABEL` | 47 | 649 |
| **合计** | **1,920** | **33,680** |

这里是 item-level 阻塞统计；63 条 `NOT_FOUND` 的 row-level 分类见 C2B 报告，两个层级不能混称。

## 零写入证明

```text
ledger_count_before       = 0
ledger_count_after        = 0
ledger_entries_delta      = 0
historical_ledger_post    = 0
production_writes         = 0
staging ledger writes     = 0
LEARNING_CREDIT_SETTLEMENT_ENABLED = false
```

DRY-RUN 只读取导入事实、审核台账、历史匹配和冻结期间信息；没有历史 ledger POST endpoint，也没有任何生产数据库迁移或写入。

## 放行状态

```text
C2B_IDENTITY_REVIEW       = CONDITIONALLY_READY
C2C_TIME_PRECISION        = SQLITE_PASS / MYSQL_PASS（CI 35355010323）
FINAL_HISTORICAL_DRY_RUN  = PASS_AS_ZERO_WRITE_PREVIEW
PRODUCTION_HISTORICAL_POST= NOT_READY
C7.2                      = NOT_READY
```

正式历史入账仍需先完成 MySQL 0066 隔离 CI、90 条班级映射和 63 条成员异常的业务处理、9 月双轨决策，以及生产迁移/入账的独立授权。当前结果只证明隔离 DRY-RUN 可重放且零写入。
