# G5.4-C3.2 生产恢复执行批准包

```text
NEW_RELEASE_COMMIT = PENDING_MAIN_MERGE
CI_RUN             = PENDING_MAIN_CI

PREVIOUS_STOP_POINT = RULE_RECONCILIATION
BACKUP              = 10619067
```

本文件不是生产 APPLY 授权。本轮 `PRODUCTION_APPLY=0`，`0064/0065/0066=NOT_APPLIED`，未修改 ledger，也未开启 settlement。

由于 release baseline 已变化，恢复生产执行前必须：

1. 再次执行只读 PRE-FLIGHT；
2. 核对原 backup `10619067` 是否仍满足恢复需要，必要时创建新 backup；
3. 重算 production fingerprint；
4. 重算 canonical fingerprint；
5. 重新确认 `learning_credit_entries=0`；
6. 重新确认 `LEARNING_CREDIT_SETTLEMENT_ENABLED=false`；
7. 仅为获批窗口打开 `G5_4_PRODUCTION_RULE_APPLY_ENABLED`，结束后立即关闭。

未来唯一允许的下一步：

```text
RULE_RECONCILIATION APPLY
→ verify 25 canonical DRAFT / ledger delta 0 / settlement false
→ stop/checkpoint
```

本批准包不包含 0064 授权；0064 不得与 RULE_RECONCILIATION APPLY 合并执行。
