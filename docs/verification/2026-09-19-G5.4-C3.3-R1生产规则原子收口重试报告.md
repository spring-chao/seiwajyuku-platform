# G5.4-C3.3-R1 生产规则原子收口重试报告

执行日期：2026-09-19

生产环境：CloudBase `shengheshu-d2g2zyyl99f6c6fc2`

服务：`seiwajyuku-platform-api`

## 1. 执行结论

本轮按 G5.4-C3.3-R1 授权，仅尝试为生产 `RULE_RECONCILIATION APPLY` 建立满足门禁的真实运行窗口。新建的开关开启修订版在生产数据库连接检查阶段失败，故严格停止在 APPLY 之前：

```text
RULE_RECONCILIATION             = NOT_APPLIED
STOP_REASON                     = FLAG_ON_REVISION_DATABASE_READINESS_FAILED
APPLY_CALL_COUNT                = 0
APPLY_RESULT                    = NOT_CALLED
COURSE_RULE_VERSION             = DRAFT
COURSE_RULES                    = 14
PRODUCTION_APPLY_FEATURE_FLAG   = false（最终在线修订版）
0064                            = NOT_APPLIED
0065                            = NOT_APPLIED
0066                            = NOT_APPLIED
RULE_MAPPING                    = 0
ACTIVE_BINDINGS_FROZEN          = 0/19
learning_credit_entries         = 0
ledger_points                   = 0.00
LEARNING_CREDIT_SETTLEMENT      = false
C7.2                            = CLOSED
```

没有发送 APPLY 请求，没有产生业务数据写入，也没有修改适配器、数据库连接串、规则数据或权限门禁。`0064`、`0065`、`0066` 均不在本轮授权范围内，未执行。

## 2. 固定发布来源与原始生产状态

```text
PRODUCTION_RUNTIME_RELEASE_COMMIT = 4853e709952beb6abc306abb0303eba59be5d30a
SOURCE_CI_RUN                     = 35407576247
SOURCE_REQUIRED_CHECKS            = 9/9 PASS
ORIGINAL_ONLINE_REVISION          = seiwajyuku-platform-api-253
ORIGINAL_TRAFFIC                  = 100%
ORIGINAL_HEALTH                   = HTTP 200
ORIGINAL_BUILD_ID                 = g5-c3-3-4853e709
ORIGINAL_G5_4_FLAG                = unset（应用默认 false）
ORIGINAL_SETTLEMENT               = false
ORIGINAL_RUN_BOOTSTRAP             = false
```

本轮所有新修订版均固定使用上述代码提交；没有将后续 report-only 提交作为生产运行代码。

## 3. 新生产备份

在重试前创建并核验了新的生产全量快照：

```text
backup_id      = 10630361
backup_name    = pre-g5-4-c3-3-r1-20260919
backup_type    = snapshot / full
snapshot_time  = 2026-09-19 18:20:12
finish_time    = 2026-09-19 18:20:26
status         = success
copy_status    = done
usable         = yes
file_size      = 7656362 bytes
```

上一份备份 `10628868` 保留，未删除。

## 4. APPLY 前只读 PRE-FLIGHT

通过生产数据库只读通道完成实时校验：

```text
course rule version        = STANDARD_3Y_2026 / 2026.1 / DRAFT
course rule version id     = 3
rule_count_before          = 14
mapping_count              = 0
active_binding_count       = 19
generic_frozen             = 0
course_frozen              = 0
ledger_count               = 0
ledger_points              = 0.00
settlement                 = false
0064 / 0065 / 0066         = NOT_APPLIED
apply_audit_count          = 0
```

实时指纹：

```text
production_fingerprint_before = f6de71877387d294a9ec43fc613294e008b2317f274837d3f6f5331c83554877
canonical_fingerprint         = 1aac914783b78a4b377bcf3f0ff6ea3762387df0f3a8f39c66b878e1c0c84674
```

实时计划：

```text
KEEP                       = 7
UPDATE                     = 4
ADD                        = 14
REMOVE_UNUSED_PLACEHOLDER  = 3
extra                      = 0
conflict                   = 0
```

三条 placeholder 均为 `PENDING / 0 points`，且 generic mapping、course mapping、study meeting course、completion fact 和 ledger 引用全部为 0。

## 5. 开关开启修订版 254

服务基础配置仅开启本轮专用门禁，继续保持结算与启动迁移关闭：

```text
G5_4_PRODUCTION_RULE_APPLY_ENABLED = true
LEARNING_CREDIT_SETTLEMENT_ENABLED = false
RUN_BOOTSTRAP_ON_STARTUP            = false
```

随后使用修订版 `253` 的同一生产镜像创建真实新修订版：

```text
FLAG_ON_REVISION       = seiwajyuku-platform-api-254
DEPLOY_ID              = 254
RUNTIME_COMMIT         = 4853e709952beb6abc306abb0303eba59be5d30a
REVISION_G5_4_FLAG     = true
REVISION_SETTLEMENT    = false
REVISION_RUN_BOOTSTRAP = false
```

修订版级环境变量回读确认：相对于 `253`，业务门禁变量的唯一变化是 G5.4 专用开关由未设置变为 `true`；没有输出或变更任何密钥值。

在 `5%` 灰度阶段，`/health/live` 连续 `20/20` 通过，build-info 仅回读固定提交 `4853e709952beb6abc306abb0303eba59be5d30a`。提升至 `100%` 后，数据库型健康检查 `/health` 与 `/api/v1/health` 持续返回 HTTP 500；连续六次 `/api/v1/health` 均失败。

运行日志的根因证据为：

```text
pymysql.err.OperationalError: (2003, "Can't connect to MySQL server on '172.17.0.33' (timed out)")
```

因此数据库就绪门禁失败，APPLY 端点未被调用。

## 6. 恢复尝试与最终回滚

先将服务基础配置恢复为：

```text
G5_4_PRODUCTION_RULE_APPLY_ENABLED = false
LEARNING_CREDIT_SETTLEMENT_ENABLED = false
RUN_BOOTSTRAP_ON_STARTUP            = false
```

随后从固定提交创建开关关闭的恢复候选：

```text
FLAG_OFF_CANDIDATE      = seiwajyuku-platform-api-255
DEPLOY_ID               = 255
RUNTIME_COMMIT          = 4853e709952beb6abc306abb0303eba59be5d30a
BUILD_ID                = g5-c3-3-r1-recovery-off-4853e709
REVISION_G5_4_FLAG      = false
REVISION_SETTLEMENT     = false
REVISION_RUN_BOOTSTRAP  = false
```

该候选在 `100%` 验证窗口内同样因生产 MySQL 私网地址连接超时而使 `/api/v1/health` 返回 HTTP 500，故未晋升为稳定修订版。之后使用官方服务回滚接口将生产稳定修订版从 `254` 回滚至原 `253`：

```text
ROLLBACK_CURRENT_REVISION = seiwajyuku-platform-api-254
ROLLBACK_TARGET_REVISION  = seiwajyuku-platform-api-253
ROLLBACK_TASK_ID          = 2187581
ROLLBACK_RELEASE_TYPE     = FULL
ROLLBACK_STATUS           = finished
```

最终生产回读：

```text
FINAL_ONLINE_REVISION   = seiwajyuku-platform-api-253
FINAL_TRAFFIC           = 100%
FINAL_SERVICE_STATUS    = normal
FINAL_REVISION_STATUS   = normal
FINAL_HEALTH            = HTTP 200
FINAL_RUNTIME_COMMIT    = 4853e709952beb6abc306abb0303eba59be5d30a
FINAL_BUILD_ID          = g5-c3-3-4853e709
FINAL_G5_4_FLAG         = unset（应用默认 false）
FINAL_BASE_G5_4_FLAG    = false
FINAL_SETTLEMENT        = false
FINAL_RUN_BOOTSTRAP     = false
```

修订版 `254`（开关开启）和 `255`（开关关闭恢复候选）均为 `0%` 流量；当前线上只有 `253` 承载流量。

## 7. 失败关闭后的数据库复核

回滚完成并恢复健康后，再次通过生产数据库只读通道核验：

```text
production_fingerprint_after  = f6de71877387d294a9ec43fc613294e008b2317f274837d3f6f5331c83554877
canonical_fingerprint         = 1aac914783b78a4b377bcf3f0ff6ea3762387df0f3a8f39c66b878e1c0c84674
course_rule_version           = DRAFT
course_rules                  = 14
canonical_exact_match         = 7/25
pending_diff                  = KEEP 7 / UPDATE 4 / ADD 14 / REMOVE 3
placeholder_rules             = 3（PENDING / 0 points / 全部引用为 0）
apply_call_count              = 0
apply_audit_count             = 0
mapping                       = 0
bindings_frozen               = 0/19
0064 / 0065 / 0066            = NOT_APPLIED
ledger_before / after         = 0 / 0
ledger_points_before / after  = 0.00 / 0.00
settlement_before / after     = false / false
```

执行前后生产指纹完全相同，证明本轮没有产生规则写入；预期的 `4 UPDATE / 14 ADD / 3 DELETE` 均未执行。

## 8. 停止点与后续授权边界

本轮停止于：

```text
RULE_RECONCILIATION = NOT_APPLIED
STOP_REASON         = FLAG_ON_REVISION_DATABASE_READINESS_FAILED
0064                = NOT_STARTED
0065                = NOT_STARTED
0066                = NOT_STARTED
C3.4                = NOT_STARTED
```

后续若要再次执行，必须先解决“新 CloudRun 修订版无法连接生产 MySQL 私网地址”的部署问题，并取得新的生产 `RULE_RECONCILIATION APPLY` 授权。新的授权窗口仍需重新完成备份、修订版级环境变量、运行提交、健康、数据库 PRE-FLIGHT、两枚指纹、ledger 与 settlement 核验。本报告不构成重试、`0064`、`0065`、`0066` 或 C3.4 授权。
