# G5.4-C3.3-R1 生产规则原子收口授权执行报告

执行日期：2026-09-19
生产环境：CloudBase `shengheshu-d2g2zyyl99f6c6fc2`
服务：`seiwajyuku-platform-api`

## 1. 执行结论

本次仅按已批准的 G5.4-C3.3-R1 范围建立一次真实生产运行窗口。固定提交形成的开关开启修订版在 100% 流量健康门禁阶段失败，故在 APPLY 前停止并完成官方回滚：

```text
RULE_RECONCILIATION             = NOT_APPLIED
APPLY_CALL_COUNT                = 0
APPLY_RESULT                    = NOT_CALLED
STOP_REASON                     = FLAG_ON_REVISION_DATABASE_READINESS_FAILED
COURSE_RULE_VERSION             = DRAFT
COURSE_RULES                    = 14
PRODUCTION_APPLY_FEATURE_FLAG   = false（最终服务配置与在线修订版）
0064 / 0065 / 0066             = NOT_APPLIED
RULE_MAPPING                    = 0
ACTIVE_BINDINGS_FROZEN          = 0/19
learning_credit_entries         = 0
ledger_points                   = 0.00
LEARNING_CREDIT_SETTLEMENT      = false
C3.4                            = NOT_STARTED
```

未发送 APPLY 请求，未产生业务规则写入、ledger 写入、结算或后续迁移。

## 2. 固定来源、备份与 APPLY 前状态

```text
RUNTIME_COMMIT         = 4853e709952beb6abc306abb0303eba59be5d30a
SOURCE_CI_RUN          = 35407576247
SOURCE_REQUIRED_CHECKS = 9/9 PASS
ORIGINAL_ONLINE        = seiwajyuku-platform-api-253 / 100% / normal
BACKUP_ID              = 10630858
BACKUP_TYPE            = snapshot / full
BACKUP_SNAPSHOT_TIME   = 2026-09-19 19:03:19
BACKUP_FINISH_TIME     = 2026-09-19 19:03:31
BACKUP_STATUS          = success
BACKUP_COPY_STATUS     = done
BACKUP_USABLE          = yes
```

实时只读 PRE-FLIGHT：

```text
course rule version        = STANDARD_3Y_2026 / 2026.1 / DRAFT (id=3)
rule_count_before          = 14
production_fingerprint    = f6de71877387d294a9ec43fc613294e008b2317f274837d3f6f5331c83554877
canonical_fingerprint     = 1aac914783b78a4b377bcf3f0ff6ea3762387df0f3a8f39c66b878e1c0c84674
exact / missing / different / extra / conflict = 7 / 14 / 4 / 0 / 0
plan                       = KEEP 7 / UPDATE 4 / ADD 14 / REMOVE_UNUSED_PLACEHOLDER 3
mapping                    = 0
bindings                   = total 19 / generic_frozen 0 / course_frozen 0
ledger                     = 0 entries / 0.00 points
settlement                 = false
0064 / 0065 / 0066         = NOT_APPLIED
apply_audit_count          = 0
```

三个 placeholder 均为 `PENDING / 0 points`，所有 mapping、study-meeting、completion 和 ledger 引用均为 0。复核通过。

## 3. 开关开启修订版与健康门禁

服务级配置与修订版级配置均核验为：

```text
G5_4_PRODUCTION_RULE_APPLY_ENABLED = true
LEARNING_CREDIT_SETTLEMENT_ENABLED = false
RUN_BOOTSTRAP_ON_STARTUP            = false
```

新建真实生产修订版：

```text
FLAG_ON_REVISION = seiwajyuku-platform-api-256
BUILD_ID         = 2605729087
RUN_ID           = multi_tenant_1x7t8OGzu1bIXK
UPLOAD_TYPE      = package
REVISION_STATUS  = normal
REVISION_TRAFFIC = 5% 灰度后 100%
```

修订版 `256` 的 build-info 返回完整固定提交 `4853e709952beb6abc306abb0303eba59be5d30a`。5% 灰度阶段 `/health/live` 连续 `20/20` 为 HTTP 200，灰度阶段 `/health`、`/api/v1/health` 亦为 HTTP 200。提升到 100% 后：

```text
/health                   = HTTP 500
/api/v1/health            = HTTP 500
/health/live              = HTTP 200
/api/v1/system/build-info = HTTP 200（提交仍为 4853e709…）
```

因此数据库健康/就绪门禁不通过。候选修订版级 `VpcId/SubnetIds` 为空；生产 MySQL VIP `172.17.0.33` 位于 `vpc-5l5vzkr2` / `subnet-1xw5yqjh` 私网，部署连接路径问题仍未解决。本次未修改网络适配、连接串或其他不在授权范围内的配置。

## 4. 恢复与最终状态

灰度完成后 CLI 灰度回滚接口已结束，随后使用官方服务回滚接口提交并完成恢复：

```text
ROLLBACK_CURRENT_REVISION = seiwajyuku-platform-api-256
ROLLBACK_TARGET_REVISION  = seiwajyuku-platform-api-253
ROLLBACK_REQUEST_ID       = 101e83be-b798-4c46-bae6-e0cad4a4399a
ROLLBACK_TASK_ID          = 2187906
ROLLBACK_STATUS           = finished
```

随后将服务级 G5.4 开关恢复为 `false`（请求 `5c6f1419-c90f-4748-a9b7-bdba40141f95`）。最终回读：

```text
FINAL_ONLINE_REVISION     = seiwajyuku-platform-api-253
FINAL_TRAFFIC             = 100%
FINAL_SERVICE_STATUS      = normal
FINAL_REVISION_STATUS     = normal
FINAL_BASE_G5_4_FLAG      = false
FINAL_REVISION_G5_4_FLAG  = unset（应用默认 false）
FINAL_SETTLEMENT          = false
FINAL_RUN_BOOTSTRAP       = false
/health                   = HTTP 200
/health/live              = HTTP 200
/api/v1/health            = HTTP 200
/api/v1/system/build-info = HTTP 200，commit_sha=4853e709952beb6abc306abb0303eba59be5d30a
```

`256` 保留为 `0%`、`normal`，没有承载生产流量；线上只有 `253` 承载流量。

## 5. 失败关闭后的数据库复核

回滚完成后再次通过生产只读通道复核，结果仍为 PASS：

```text
production_fingerprint_after = f6de71877387d294a9ec43fc613294e008b2317f274837d3f6f5331c83554877
canonical_fingerprint        = 1aac914783b78a4b377bcf3f0ff6ea3762387df0f3a8f39c66b878e1c0c84674
rule_count_after             = 14
pending_diff                 = KEEP 7 / UPDATE 4 / ADD 14 / REMOVE 3
placeholder_rules            = 3（PENDING / 0 points / 全部引用为 0）
apply_call_count             = 0
apply_audit_count            = 0
mapping                      = 0
bindings_frozen              = 0/19
0064 / 0065 / 0066          = NOT_APPLIED
ledger_before / after        = 0 / 0
ledger_points_before / after = 0.00 / 0.00
settlement_before / after    = false / false
```

执行前后生产指纹完全相同；本次未发生规则数据或学习积分业务写入。

## 6. 停止点与后续边界

```text
RULE_RECONCILIATION = NOT_APPLIED
0064 / 0065 / 0066 = NOT_STARTED
C3.4               = NOT_STARTED
```

后续若要再次执行，必须先解决新 CloudRun 修订版访问生产 MySQL 私网地址的问题，并取得新的、边界明确的生产 APPLY 授权。新的授权窗口仍需重新完成备份、修订版环境变量、健康、数据库 PRE-FLIGHT、双指纹、ledger、settlement 和恢复核验。本报告不构成 `0064`、`0065`、`0066`、C3.4 或再次 APPLY 的授权。
