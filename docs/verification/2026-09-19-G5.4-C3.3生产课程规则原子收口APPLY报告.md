# G5.4-C3.3 生产课程规则原子收口 APPLY 报告

执行日期：2026-09-19

生产环境：CloudBase `shengheshu-d2g2zyyl99f6c6fc2`

服务：`seiwajyuku-platform-api`

## 1. 结论

本轮唯一一次 APPLY 请求返回：

```text
HTTP 404
code = FEATURE_DISABLED
message = Not found
```

因此生产规则没有发生任何写入。本轮按批准指令立即停止，没有修改参数、绕过门禁、手工补 SQL 或进行第二次 APPLY。

```text
RULE_RECONCILIATION             = NOT_APPLIED
STOP_REASON                     = FEATURE_DISABLED
COURSE_RULE_VERSION             = DRAFT
COURSE_RULES                    = 14
PRODUCTION_APPLY_FEATURE_FLAG   = false
0064                            = NOT_APPLIED
0065                            = NOT_APPLIED
0066                            = NOT_APPLIED
RULE_MAPPING                    = 0
ACTIVE_BINDINGS_FROZEN          = 0/19
learning_credit_entries         = 0
ledger_points                   = 0.00
LEARNING_CREDIT_SETTLEMENT      = false
C7.2                            = CLOSED
```

`0064` 不在本轮授权范围内，未执行。

## 2. Release 与 CI

```text
PRODUCTION_RUNTIME_RELEASE_COMMIT = 4853e709952beb6abc306abb0303eba59be5d30a
CI_RUN                           = 35407576247
REQUIRED_CHECKS                  = 9/9 PASS
REPORT_COMMIT                    = 本报告所在的后续 report-only Git 提交，以交付记录为准
```

部署结果：

- CloudRun revision：`seiwajyuku-platform-api-253`
- 状态：`normal`
- 流量：`100%`
- `/api/v1/health`：PASS
- `/api/v1/system/build-info`：回读 commit `4853e709952beb6abc306abb0303eba59be5d30a`
- `RUN_BOOTSTRAP_ON_STARTUP=false`

## 3. 新生产备份

```text
backup_id      = 10628868
backup_type    = snapshot / full
snapshot_time  = 2026-09-19 15:58:39
finish_time    = 2026-09-19 15:58:49
status         = success
copy_status    = done
usable         = yes
file_size      = 7655490 bytes
```

旧备份 `10619067` 保留，未删除。

## 4. APPLY 前只读 PRE-FLIGHT

```text
course rule version        = STANDARD_3Y_2026 / 2026.1 / DRAFT
course rule version id     = 3
rule_count_before          = 14
mapping_count              = 0
active_binding_count       = 19
generic_frozen             = 0
course_frozen              = 0
ledger_count               = 0
ledger_points              = 0.00
settlement                 = false
0064 / 0065 / 0066         = NOT_APPLIED
```

实时指纹：

```text
production_fingerprint_before = f6de71877387d294a9ec43fc613294e008b2317f274837d3f6f5331c83554877
canonical_fingerprint         = 1aac914783b78a4b377bcf3f0ff6ea3762387df0f3a8f39c66b878e1c0c84674
```

实时 diff 仍为：

```text
KEEP                       = 7
UPDATE                     = 4
ADD                        = 14
REMOVE_UNUSED_PLACEHOLDER  = 3
extra                      = 0
conflict                   = 0
```

三条 placeholder 均为 `PENDING / 0 points`，且以下引用全部为 0：generic mapping、course mapping、study meeting course、completion fact、ledger。

## 5. Feature Flag 窗口与唯一一次请求

```text
feature flag before = false（变量未设置，应用默认 false）
control-plane during = true
runtime request result = FEATURE_DISABLED
feature flag after = false
settlement before/during/after = false
```

控制面 `UpdateCloudRunServerConfig` 将服务基础配置显示为 `true`，但没有创建新 revision，也没有改变 revision `253` 运行进程的部署时环境变量。唯一一次 POST 因而在应用运行时门禁返回 `FEATURE_DISABLED`。

调用身份只读验证：

```text
actor_user_id = 1
role = system_admin
plans:production_rule_reconciliation_apply = granted
```

请求使用批准的 release commit、实时 production/canonical fingerprint、版本 ID `3`、`DRAFT`、14 条规则和固定三条 placeholder 白名单；未包含 raw SQL。

## 6. AFTER VERIFY

唯一一次请求后只读核验：

```text
adapter_status                 = FEATURE_DISABLED / NOT_APPLIED
production_fingerprint_after   = f6de71877387d294a9ec43fc613294e008b2317f274837d3f6f5331c83554877
course_rule_version            = DRAFT
course_rules                   = 14
canonical_exact_match          = 7/25
placeholder_rules              = 3
apply_audit_count              = 0
mapping                        = 0
bindings_frozen                = 0/19
0064 / 0065 / 0066             = NOT_APPLIED
ledger_before / after          = 0 / 0
ledger_points_before / after   = 0.00 / 0.00
settlement_before / after      = false / false
```

预期的 `4 UPDATE / 14 ADD / 3 DELETE` 没有执行，因此相应审计不存在；这是本轮正确的失败关闭结果，不得标记为 APPLY 成功。

## 7. 停止点与后续边界

本轮停止于：

```text
RULE_RECONCILIATION = NOT_APPLIED_FEATURE_DISABLED
0064                = NOT_STARTED
0065                = NOT_STARTED
0066                = NOT_STARTED
```

如需再次执行，必须取得新的生产 `RULE_RECONCILIATION APPLY` 授权，并先用能生成新 CloudRun revision 的配置发布方式把专用开关注入实际运行环境，再重新完成 PRE-FLIGHT、备份适用性、两枚指纹、ledger、settlement 和 runtime provenance 核验。本报告不构成重试或 `0064` 授权。
