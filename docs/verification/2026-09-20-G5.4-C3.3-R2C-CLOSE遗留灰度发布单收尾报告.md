# G5.4-C3.3-R2C-CLOSE 遗留灰度发布单收尾报告

核验日期：2026-09-20（Asia/Shanghai）

## 结论

`R2C-CLOSE = PASS`（控制面关闭与流量恢复通过）。

用户已在腾讯云控制台对当前发布单执行“取消灰度”。本报告只记录该动作后的只读结果；Codex 未执行控制台写操作、未猜测 `OperateType`、未调用 RULE_RECONCILIATION APPLY，也未进入 R2C-VERIFY 或 R3。

## 授权目标与边界

```text
ReleaseOrder = 2680075
ManageTask   = 2190170
Stable       = 253
Candidate    = 257
```

本阶段不包含重新上线 257、URL_PARAMS 定向验证、普通灰度、创建新 candidate、数据库迁移、规则收口、历史账本写入或 settlement。

## 控制台动作记录

| 项目 | 结果 |
| --- | --- |
| 用户报告的控制台动作 | `取消灰度` |
| 实际执行次数 | 1（用户确认） |
| 实际 `OperateServerManage.OperateType` | `NOT_CAPTURED_MANUAL_CONSOLE_ACTION`；未推断、未调用 |
| Codex 生产控制面写入 | 0 |
| Codex 数据库写入 | 0 |

控制台 API Inspector 未由 Codex 取得，因此本次不能把“取消灰度”映射成具体 `OperateType`；以后仍不得根据 `cancel/go_back/done` 名称猜测。

## 取消灰度后的实时只读验收

由 `r2c_final.py` 与 `r2c_status.py` 重新读取 CloudRun 控制面：

| 验收项 | 实时结果 |
| --- | --- |
| 服务状态 | `normal` |
| `TrafficType` | `FLOW` |
| `TRAFFIC_RESTORED` | `true` |
| 253 普通流量 | `100%`，`HasTraffic=true` |
| 257 普通流量 | `0%`，`HasTraffic=false` |
| `TrafficTypeValues` | 空 |
| 253/257 URL 参数 | 均为空 |
| `ReleaseOrder=2680075` | `GrayStatus=success`，`ReleaseStatus=fail`（终态），`IsReleasing=false` |
| `RELEASE_ORDER_CLOSED` | `true` |
| `ManageTask=2190170` | `stopped`，不再 `running` |

因此：

```text
TRAFFIC_RESTORED      = PASS
RELEASE_ORDER_CLOSED  = PASS
253                   = 100% default
257                   = 0% ordinary traffic
```

## 运行时与网络只读复核

```text
stable runtime commit = 4853e709952beb6abc306abb0303eba59be5d30a
/api/v1/system/build-info = HTTP 200
/health/live             = HTTP 200
/api/v1/health           = HTTP 200
```

253 与 257 当前均为 `normal`，且四项 `VpcConf` 相同：

```text
VpcId      = vpc-5l5vzkr2
VpcCIDR    = 172.17.0.0/16
SubnetId   = subnet-1xw5yqjh
SubnetCIDR = 172.17.0.0/20
```

257 仍存在、状态为 `normal`、当前 0% 流量，配置未被删除或改动。因此：

```text
CANDIDATE_257_REUSE = RETAINED_FOR_SEPARATE_REVIEW
```

这只表示 candidate 资源仍在且可供后续单独评估，不表示本阶段已重新上线或已完成定向路由验证。

## 运行时门禁

| Revision | G5 APPLY | settlement | bootstrap |
| --- | --- | --- | --- |
| 253 | 未设置（代码默认关闭） | `false` | `false` |
| 257 | `false` | `false` | `false` |

```text
RULE_RECONCILIATION = NOT_APPLIED
APPLY_CALL_COUNT（本次任务） = 0
```

## 业务数据与迁移安全边界

最近一次生产只读 PRE-FLIGHT（2026-09-18）记录的基线为：

```text
rules = 14
mapping = 0
active bindings = 19
bindings with both frozen rule references = 0/19
learning_credit_entries = 0
ledger points = 0.00
settlement = false
0064 = NOT_APPLIED
0065 = NOT_APPLIED
0066 = NOT_APPLIED
```

本次取消灰度后，253 始终保持 100% 默认流量，257 始终为 0% 普通流量；没有创建新 revision、没有启动迁移、没有调用 APPLY，也没有允许任何业务写入路径。因此本阶段的业务安全结论为：

```text
production_database_writes = 0
business_baseline_change_by_this_action = none observed
```

说明：当前工具没有可安全使用的生产数据库直接只读连接，故本报告不把上述历史基线冒充为取消灰度瞬间重新执行的 SQL 快照；如后续需要新的数据库快照，应另行安排只读核验，不得借此进入 R3。

## 阶段结束

本阶段已在“流量恢复 + 发布单关闭 + 257 资源保留待评估”处停止。

```text
R2C-CLOSE = PASS
R2C-VERIFY = NOT_AUTHORIZED
R3 RULE_RECONCILIATION APPLY = NOT_AUTHORIZED
```
