# G5.4-C3.3-R2C-VERIFY｜257 定向数据库连通性验证报告

核验日期：2026-09-20（Asia/Shanghai）  
执行结果：FAILED_CLEANUP_CONFIRMED  
范围：只尝试本授权允许的 URL_PARAMS 定向与 FLOW 恢复；没有创建 revision、没有显式启动实例、没有生产数据库写入。

## 1. 结论

本次不能判定 NEW_REVISION_DB_CONNECTIVITY = PASS。

正式执行在第一次控制面调用后收到 TencentCloudSDKException，没有进入 URL_PARAMS 激活确认、candidate 实例等待、HTTP 探针或 CLS 身份证明阶段。工具失败路径尝试恢复 FLOW，但工具自身的清理证据为 traffic_restored=false、cleanup_error=TencentCloudSDKException。随后独立只读复核确认控制面已恢复，因此没有继续重试。

~~~
TARGETED_ROUTE_ACTIVE        = NOT_PROVEN
CANDIDATE_INSTANCE_READY     = NOT_RUN
HTTP_PROBES                  = 0/23 (NOT_RUN)
CLS_CANDIDATE_HITS           = NOT_QUERIED
NEW_REVISION_DB_CONNECTIVITY = NOT_PROVEN
TRAFFIC_RESTORED             = PASS (独立只读复核)
RELEASE_ORDER_CLOSED         = PASS
MANUAL_CLOSE_REQUIRED        = false
R3 RULE_RECONCILIATION APPLY = NOT_AUTHORIZED
~~~

不创建第二个 candidate，不重试第三次路由，不执行 APPLY 或迁移。

## 2. 固定 provenance

~~~
CONTROL_TOOL_COMMIT       = caefcf5689474f3be79f7d8b45385f8cb18706a0
CANDIDATE_RUNTIME_COMMIT  = 4853e709952beb6abc306abb0303eba59be5d30a
MAIN_CI_REQUIRED_CHECKS   = 9/9 PASS
APPROVAL_REF              = G5.4-C3.3-R2C-VERIFY-20260920
ENV_ID                    = shengheshu-d2g2zyyl99f6c6fc2
SERVICE                   = seiwajyuku-platform-api
STABLE                    = seiwajyuku-platform-api-253
CANDIDATE                 = seiwajyuku-platform-api-257
~~~

控制器工作树 HEAD 与 origin/main 均为 caefcf5689474f3be79f7d8b45385f8cb18706a0；指定 release manifest 已通过 9 个 required checks 的 provenance 校验。

## 3. 执行前实时只读基线

### 3.1 控制面与 candidate

dry-run 通过：

~~~
mode                       = VERIFY_EXISTING_CANDIDATE
new_revision_creation_path = NOT_USED
explicit_start_instance    = DISALLOWED
revision_writes            = 0
route_writes               = 0
network_writes             = 0
database_writes            = 0
~~~

执行前读取到 service=normal、TrafficType=FLOW、253=100% ordinary、257=0% ordinary、candidate=normal。

四项 VpcConf 完全一致：

| 字段 | stable 253 | candidate 257 | 结果 |
| --- | --- | --- | --- |
| VpcId | vpc-5l5vzkr2 | vpc-5l5vzkr2 | PASS |
| VpcCIDR | 172.17.0.0/16 | 172.17.0.0/16 | PASS |
| SubnetId | subnet-1xw5yqjh | subnet-1xw5yqjh | PASS |
| SubnetCIDR | 172.17.0.0/20 | 172.17.0.0/20 | PASS |

candidate 257 安全开关：

~~~
G5_4_PRODUCTION_RULE_APPLY_ENABLED = false
LEARNING_CREDIT_SETTLEMENT_ENABLED = false
RUN_BOOTSTRAP_ON_STARTUP            = false
~~~

stable build-info runtime commit 为 4853e709952beb6abc306abb0303eba59be5d30a。

### 3.2 生产数据库只读 PRE-FLIGHT

CloudBase RunSql 本次只执行 SELECT，未执行 INSERT、UPDATE、DELETE、DDL 或事务提交。

~~~
course rule version     = DRAFT (id=3, STANDARD_3Y_2026 / 2026.1)
rules                   = 14
canonical exact         = 7/25
update                  = 4
add                     = 14
placeholder             = 3
mapping                 = 0
active bindings         = 19
generic frozen          = 0/19
course frozen           = 0/19
learning_credit_entries = 0
ledger points           = 0.00
settlement              = false
0064/0065/0066          = NOT_APPLIED
RULE_RECONCILIATION     = NOT_APPLIED
APPLY audit count       = 0
~~~

三个占位规则的 generic mapping、course mapping、study-meeting reference、completion fact、ledger reference 均为 0。

~~~
production fingerprint before = f6de71877387d294a9ec43fc613294e008b2317f274837d3f6f5331c83554877
~~~

## 4. 正式执行与失败证据

正式 execute 使用了同一 stable、candidate、控制器提交、manifest 和 approval ref。错误摘要：

~~~
state                     = FAILED
error                     = UNEXPECTED_RELEASE_ERROR
message                   = existing candidate verification stopped after TencentCloudSDKException
restore_attempted         = true
tool traffic_restored     = false
cleanup_error             = TencentCloudSDKException
tool release_order_closed = true
~~~

代码路径只允许一次 URL_PARAMS ReleaseGray 和一次 FLOW 恢复 ReleaseGray。本次失败路径已尝试这两次调用；没有调用 StartVersionInstance、OperateServerManage，没有创建 258 或其他 revision。

因为 URL_PARAMS 激活未被确认，后续门禁全部未执行：

~~~
TARGETED_ROUTE_ACTIVE    = NOT_PROVEN
CANDIDATE_INSTANCE_READY = NOT_RUN
build-info probe         = NOT_RUN
/health/live             = NOT_RUN
/api/v1/health           = 0/20 (NOT_RUN)
CLS SearchClsLog         = NOT_RUN
candidate/stable/unknown = NOT_PROVEN / NOT_PROVEN / NOT_PROVEN
~~~

## 5. 执行后独立只读控制面验收

失败返回后重新读取控制面，未发送新的 ReleaseGray：

~~~
service TrafficType         = FLOW
253 ordinary flow           = 100%
257 ordinary flow           = 0%
TrafficTypeValues           = empty
253/257 URL parameters      = empty
ReleaseOrder                = 2680075
ReleaseOrder.IsReleasing    = false
ReleaseOrder.ReleaseStatus  = fail (terminal)
ManageTask                  = 2190170
ManageTask.Status           = stopped
~~~

独立只读复核：

~~~
TRAFFIC_RESTORED      = PASS
RELEASE_ORDER_CLOSED  = PASS
MANUAL_CLOSE_REQUIRED = false
~~~

257 仍存在、status=normal、普通流量=0，四项 VpcConf 仍与 stable 一致；但定向路由身份和 MySQL 连通性本次均未被证明。

## 6. 执行后数据库不变量

失败收尾后再次执行同一组只读 SQL：

~~~
production fingerprint after = f6de71877387d294a9ec43fc613294e008b2317f274837d3f6f5331c83554877
fingerprint before == after  = true
rules                       = 14
mapping                     = 0
active bindings             = 19
generic/course frozen       = 0/19
learning_credit_entries     = 0
ledger points               = 0.00
settlement                  = false
0064/0065/0066             = NOT_APPLIED
RULE_RECONCILIATION         = NOT_APPLIED
APPLY_CALL_COUNT            = 0
production_database_writes  = 0
~~~

## 7. 阶段停止点

~~~
R2C-VERIFY                   = FAILED_CLEANUP_CONFIRMED
NEW_REVISION_VPC_INHERITANCE = PASS (只读配置比对)
NEW_REVISION_DB_CONNECTIVITY = NOT_PROVEN
TRAFFIC_RESTORED             = PASS
RELEASE_ORDER_CLOSED         = PASS
R3 RULE_RECONCILIATION APPLY = NOT_AUTHORIZED
~~~

本阶段在控制面恢复、发布单终态确认和数据库不变量复核后停止。后续若要再次验证，必须先单独诊断并修复本次 TencentCloudSDKException，再提出新的、明确路由调用次数边界的授权；本报告不授权重试、重新上线 257、创建新 candidate 或进入 R3。

