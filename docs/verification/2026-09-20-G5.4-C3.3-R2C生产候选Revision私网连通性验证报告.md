# G5.4-C3.3-R2C 生产候选 Revision 私网连通性验证报告

执行日期：2026-09-20（Asia/Shanghai）
授权：用户明确批准按 R2C 边界执行；仅一个 candidate、三个门禁 false、定向健康、恢复 stable，不授权 APPLY、迁移或第二个 candidate。

## 1. 验收结论

**R2C 未通过：版本级 VPC 继承成功，但定向请求实际命中 stable，候选数据库连通性未获证明。**

```text
NEW_REVISION_VPC_INHERITANCE = PASS
NEW_REVISION_DB_CONNECTIVITY = NOT_PROVEN
TARGETED_CANDIDATE_IDENTITY = FAIL
R2C = FAIL_CLOSED
```

发布器返回 `READY_FOR_RELEASE` 和 `targeted_database_health=20/20`，但事后独立 CLS 日志证明全部 23 条带 `sj_canary` 的请求由 stable 253 响应。该返回值不作为候选验收通过证据。没有重新设置定向路由重试，没有创建第二个 candidate，没有进入 RULE_RECONCILIATION。

## 2. 控制代码与运行代码

| 项目 | 值 |
|---|---|
| CONTROL_TOOL_COMMIT | `b1f151a87dde7ec203f1fe7ebfaac0d91a13968d` |
| 控制脚本 | `scripts/deploy_cloudrun_api.py`，批准提交原文未修改 |
| 控制代码 CI | [35452181786](https://github.com/spring-chao/seiwajyuku-platform/actions/runs/35452181786)，9/9 PASS |
| 预期 CANDIDATE_RUNTIME_COMMIT | `4853e709952beb6abc306abb0303eba59be5d30a` |
| 运行代码 CI | [35407576247](https://github.com/spring-chao/seiwajyuku-platform/actions/runs/35407576247)，9/9 PASS |
| stable revision | `seiwajyuku-platform-api-253` |
| candidate revision | `seiwajyuku-platform-api-257` |
| 镜像核验 | 253 与 257 的 DeployRecord.ImageUrl 完全相同，257 为 image 发布 |
| candidate build-info | 未独立证实；带 token 返回了预期 SHA，但来源是 253 |
| 创建任务 | `2190170` |
| 发布单 | `2680075` |

本地启动辅助程序仅在进程内加载已有 CloudBase 临时凭据并调用批准版本的 `main()`；不修改正式发布器、不输出凭据、不增加生产写接口。首次 SDK 只读请求返回 `AuthFailure.TokenFailure`；现有 CLI `env list` 正常读取并更新会话后，SDK 只读核验恢复，候选创建前所有前置条件重新通过。

## 3. PRE-FLIGHT 与 dry-run

生产只读查询确认：唯一 stable 为 253，流量 100%，服务与版本 `normal`，无活动发布任务；build-info 返回批准的 runtime commit，liveness 和数据库健康均为 HTTP 200。

生产 MySQL 资源通过 `DescribeClusterDetail` 回读：`cynosdbmysql-1u9lqtf0`，状态 running，VIP `172.17.0.33:3306`，VPC `vpc-5l5vzkr2`、subnet `subnet-1xw5yqjh`，与 stable 一致。

正式 dry-run 参数：`--reuse-stable-image --dry-run --promotion ready`，commit 为运行提交；加载该提交的 CI release manifest，并传入只含三个 false 门禁的临时 JSON。随后唯一一次执行将 `--dry-run` 改为 `--execute`，批准引用为 `G5.4-C3.3-R2C-20260920`。

dry-run 输出：

```text
ci_provenance_verified = true
deploy_type = image
release_type = CONFIG_RELEASE
candidate_request_items = [VpcConf, EnvParam]
candidate_request_vpc_conf = stable_vpc_conf
revision_writes = 0
network_writes = 0
database_writes = 0
requested_promotion = ready
G5_4_PRODUCTION_RULE_APPLY_ENABLED = false (changed)
LEARNING_CREDIT_SETTLEMENT_ENABLED = false (unchanged)
RUN_BOOTSTRAP_ON_STARTUP = false (unchanged)
```

## 4. VPC 继承实测

257 于 07:14:00 创建，07:15:20 版本详情为 normal。发布器在路由调用之前完成四字段断言；独立版本详情回读也一致。

| 字段 | stable 253 | candidate 257 | 结果 |
|---|---|---|---|
| VpcId | vpc-5l5vzkr2 | vpc-5l5vzkr2 | PASS |
| VpcCIDR | 172.17.0.0/16 | 172.17.0.0/16 | PASS |
| SubnetId | subnet-1xw5yqjh | subnet-1xw5yqjh | PASS |
| SubnetCIDR | 172.17.0.0/20 | 172.17.0.0/20 | PASS |

两者 VPC fingerprint：`81d200538c4b7ff8cc5bd0e09f6d8e5f3caa2c26f0f18b494463079c8d76f271`。

257 版本级 EnvParams 回读确认 G5、settlement、bootstrap 均为字符串 `false`。没有修改 VPC/subnet 资源、安全组或 MySQL；唯一创建请求按批准显式携带继承的 VpcConf。

## 5. 定向验证与反证

发布器按代码调用 `ReleaseGray(TrafficType=URL_PARAMS)`，使用进程内一次性 `sj_canary` token，然后执行 2 次 build-info、1 次 liveness 和 20 次 DB health，返回 READY_FOR_RELEASE，并调用恢复 stable 100% 的路由操作。没有使用普通百分比灰度或 FULL。

独立使用 [SearchClsLog](https://cloud.tencent.com/document/api/1243/126754) 查询本轮 `07:13:00—07:17:00` 的 `sj_canary` 请求，完整返回 23 条，`ListOver=true`：

| 路径 | HTTP 200 数量 | 日志证明的实际 revision |
|---|---:|---|
| /api/v1/system/build-info | 2 | 253 |
| /health/live | 1 | 253 |
| /api/v1/health | 20 | 253 |

请求时间为 `07:15:26—07:15:32`。全部日志的结构化标签一致：

```text
container_name = seiwajyuku-platform-api-253
pod_label_k8s-app = seiwajyuku-platform-api-253
pod_name = seiwajyuku-platform-api-253-744b9f6f4c-cbgtk
FileName = /var/log/eks-log-agent/ca-tcluygqu-seiwajyuku-platform-api-0/stdout-containerd/seiwajyuku-platform-api-253/0.log
```

判定依据是 container/pod 标签与日志文件来源，不是镜像标签（257 本就复用 253 镜像）。报告不保存 token、请求完整 URL 或原始日志。

因此不能把这些响应称为 candidate build-info、candidate liveness 或 candidate DB 20/20。两个版本的运行 SHA 相同，单纯比较 commit 无法识别路由错误。已确认的问题是发布器缺少路由实际生效及目标 revision 身份门禁；尚不能仅凭本轮证据区分路由字段/优先级问题与异步生效时序问题。本轮停止生产验证，不修改发布器重试。

## 6. 最终流量及控制面状态

最后只读回读：

```text
service status = normal
OnlineVersionInfos = [253 / 100%]
253 FlowRatio = 100, HasTraffic = true
257 FlowRatio = 0, HasTraffic = false, status = normal
TrafficType = FLOW
TrafficTypeValues = empty
CurrentVersion.UrlParam.Value = empty
ReleaseVersion.UrlParam.Value = empty
stable build-info = 4853e709952beb6abc306abb0303eba59be5d30a
stable /health/live = HTTP 200 / ok
stable /api/v1/health = HTTP 200 / ok
```

定向参数已清除，stable 100%，candidate 保留 0% 供审计。**控制面发布单仍为 `GrayStatus=success / ReleaseStatus=gray / IsReleasing=true`，任务 2190170 仍为 running**；恢复流量并不等于发布单已结束。本轮未追加取消/全量/新部署操作，也不能把最终状态报告为“无活动任务”。后续操作前必须处理并核验该未结束发布单，不能直接再次运行发布器。

## 7. 前后业务不变量

使用既有 `tcb db execute --read-only` 查询，前后校验均 PASS：

| 项目 | 前置 | 结束后 |
|---|---|---|
| 课程规则版本 | id=3，DRAFT | 相同 |
| rules | 14 | 14 |
| mapping | 0 | 0 |
| bindings | 19，generic/course frozen 均 0 | 相同 |
| ledger | 0 entries / 0.00 points | 相同 |
| settlement | false | false |
| 0064/0065/0066 | NOT_APPLIED | NOT_APPLIED |
| 本运行提交对应规则收口审计 | 0 | 0 |

规则 fingerprint 前后均为 `f6de71877387d294a9ec43fc613294e008b2317f274837d3f6f5331c83554877`。

```text
production_candidate_created = 1 (257)
second_candidate_created = 0
ordinary_candidate_traffic = 0%
production_database_writes = 0
learning_credit_entries_writes = 0
APPLY_CALL_COUNT = 0
RULE_RECONCILIATION = NOT_APPLIED
0064/0065/0066 = NOT_APPLIED
historical_staging = NOT_EXECUTED
R3 = NOT_STARTED
```

本阶段到此停止。下一步需要先解决并验证定向路由的真实命中与发布单收尾；本报告不授权新 candidate、再次定向验证或 R3 APPLY。
