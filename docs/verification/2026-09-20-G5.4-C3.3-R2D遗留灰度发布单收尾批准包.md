# G5.4-C3.3-R2D 遗留灰度发布单收尾批准包

状态：待单独批准；本文件不授权执行。

## 目标

仅收尾当前遗留发布单 `2680075` 与管理任务 `2190170`，确认 stable 253 继续承接 100% 默认流量、candidate 257 保持 0% 普通流量，并让 `DescribeReleaseOrder.IsReleasing=false`、`DescribeServerManageTask.Status` 不再为 `running`。

## 已知状态

```text
stable = 253 / 100%
candidate = 257 / 0%
ReleaseOrder = 2680075 / ReleaseStatus=gray / IsReleasing=true
ManageTask = 2190170 / status=running
```

## 执行前只读门禁

重新读取并保存脱敏结果：

1. `DescribeReleaseOrder`：确认仍为 2680075，记录 CurrentVersion、ReleaseVersion、TrafficType、TrafficTypeValues、GrayStatus、ReleaseStatus、IsReleasing；
2. `DescribeServerManageTask(TaskId=2190170)`：确认版本与状态；
3. `DescribeCloudRunServerDetail`：确认服务 normal、253=100%、257=0%、TrafficType=FLOW；
4. stable 与 candidate 版本详情：确认 VPC 四字段一致，三个门禁均为 false；
5. stable `/health/live`、`/api/v1/health` 和 build-info 正常；
6. rules=14、mapping=0、bindings=0/19、ledger=0/0.00、settlement=false、0064/0065/0066 未应用、APPLY_CALL_COUNT=0。

## 唯一允许的写动作

在 API Inspector 明确确认控制台“放弃灰度、保持 253、257=0%”对应的 `OperateServerManage.OperateType` 后，另行授权只调用一次该已确认动作。动作不得由 `cancel`、`go_back`、`done` 的名称自行推断；本批准包不预先指定其值。

## 禁止

```text
创建新 revision
重新设置 URL_PARAMS
普通 FLOW 灰度或 FULL
删除 257
修改 VPC/subnet/安全组/MySQL
生产数据库写入
RULE_RECONCILIATION APPLY
0064/0065/0066
```

## 成功条件

```text
ReleaseOrder.IsReleasing = false
ManageTask 2190170 status != running
253 = 100% default
257 = 0% ordinary traffic
TrafficType = FLOW
TrafficTypeValues = empty
production route writes beyond the single approved close action = 0
production database writes = 0
APPLY_CALL_COUNT = 0
```

失败时停止并报告，不重试、不创建第二个 candidate、不进入 R3。收尾成功也只关闭遗留发布单，不等同于 candidate MySQL 连通性通过；后续候选身份验证仍需独立授权。
