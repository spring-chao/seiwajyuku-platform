# G5.4-C3.3-R2F-CAPTURE｜ReleaseGray 异常元数据单次捕获

日期：2026-09-20（Asia/Shanghai）。结果：CAPTURE_COMPLETED / SERVER_REJECTED / CONTROL_PLANE_UNCHANGED。

## 结论

获批的唯一一次 `ReleaseGray(URL_PARAMS)` 复现 SDK 异常，腾讯云返回 `ResourceUnavailable` 及非空 RequestId。服务端安全错误消息：

```text
{seiwajyuku-platform-api}状态异常，无法发布
```

因此已经证明本次请求抵达腾讯云服务端，并以资源状态异常被拒绝；尚未证明导致该状态的内部条件。不能据此断言 257 永久不可复用，也不能将本次错误追溯为上一轮丢失 metadata 的确切错误。

```text
SDK_EXCEPTION_REPRODUCED = true
REQUEST_REACHED_TENCENT_SERVICE = true
FAILURE_CLASS = RELEASE_GRAY_RESOURCE_UNAVAILABLE (PROVEN)
ROOT_CAUSE = NOT_PROVEN (内部资源/发布资格条件尚未查明)
NEW_REVISION_DB_CONNECTIVITY = NOT_PROVEN
```

## 控制器 provenance 与执行方式

```text
CONTROL_TOOL_COMMIT = b0a71c74cd44a5d1f85ff7e939a95db9e2122959
MAIN_CI_RUN = 35513349393
MAIN_CI = 9/9 PASS, including build-provenance
CANDIDATE_RUNTIME_COMMIT = 4853e709952beb6abc306abb0303eba59be5d30a (批准基线，未发送 runtime 探针)
STABLE = seiwajyuku-platform-api-253
CANDIDATE = seiwajyuku-platform-api-257
```

在固定到该 main SHA 的干净 detached worktree，调用原版 `verify_controller_provenance()` 验证 manifest、Git commit/tree、远端 main 与 GitHub 9 项成功 job。manifest 来自 `release-provenance-35513349393/release-manifest.json`，没有使用 branch manifest。

任务执行器位于工作树外，仅导入获批提交的请求构造及 SDK 异常捕获边界；没有调用会自动探测及恢复的完整 `verify_existing_candidate()`。写调用前使用独占创建的本地 attempt lock 防止重复执行。主调用使用 `build_targeted_release_request(253,257,一次性token)`，通过原版 `_tcbr_request()` 捕获元数据。SDK ClientProfile 默认不配置 retryer，关闭 region breaker。没有重试。

本地原登录临时凭据已于北京时间 19:28:05 到期。官方 CLI 的只读 `env list` 成功刷新已有会话；执行使用刷新后的临时凭据，expiry 为 `1789921500000`（Unix 毫秒），执行时已检查至少还有 10 分钟有效期。只保存 credential_mode=TEMPORARY、token_present=true 和 expiry，未保存密钥、会话 token、真实 canary 值。

## 捕获结果

调用位于北京时间 22:31:41 控制面预检之后、22:31:55 立即复核之前。

| 字段 | 实际结果 |
| --- | --- |
| action | ReleaseGray |
| TrafficType | URL_PARAMS |
| tencent_error_code | ResourceUnavailable |
| request_id | d9f8e51f-734d-4906-a406-b630a4285357 |
| request_id_present | true |
| request_reached_service | true |
| diagnostic_classification | RELEASE_GRAY_RESOURCE_UNAVAILABLE |
| message_redacted | {seiwajyuku-platform-api}状态异常，无法发布 |
| credential_mode | TEMPORARY |
| token_present | true |
| endpoint | https://tcbr.tencentcloudapi.com |

## 控制面前后对比

| 项目 | 执行前 | 调用后立即只读复核 |
| --- | --- | --- |
| service status | normal | normal |
| service TrafficType | FLOW | FLOW |
| OnlineVersionInfos | 仅 253，100% | 仅 253，100% |
| 257 普通流量 | 0%，未列入在线流量集合 | 0%，未列入在线流量集合 |
| ReleaseOrder | 2680075 | 2680075 |
| IsReleasing | false | false |
| ReleaseStatus | fail（终态） | fail（终态） |
| order TrafficType | FLOW | FLOW |
| TrafficTypeValues | empty | empty |
| CurrentVersion/ReleaseVersion UrlParam | Key/Value 均为空 | Key/Value 均为空 |
| 本次 canary 是否出现在发布单 | false | false |
| ManageTask | 2190170 | 2190170 |
| ManageTask status | stopped | stopped |

说明：已关闭发布单里的历史 CurrentVersion.FlowRatio=0、IsDefaultPriority=false，不作为当前稳定流量依据。实时服务在线流量只有 253=100%；257 存在但不在在线流量集合。UrlParam 是含空值的对象，不能用“对象非空”误判为存在定向 token。

执行前四字段完全一致：VpcId=vpc-5l5vzkr2、VpcCIDR=172.17.0.0/16、SubnetId=subnet-1xw5yqjh、SubnetCIDR=172.17.0.0/20。257 前后均 normal，G5、settlement、bootstrap 三个门禁均显式 false。253 前后 settlement/ bootstrap 显式 false；其 G5 环境键未设置，未将此表述为显式 false。

```text
cleanup required = false
cleanup attempted = false
production route writes attempted = 1
cleanup writes = 0
TRAFFIC_RESTORED = true (实际始终未改变)
RELEASE_ORDER_CLOSED = true
MANUAL_CLOSE_REQUIRED = false
```

## 业务数据库只读复核

通过官方 CloudBase CLI `db execute --read-only` 执行限定 SELECT。首次账本查询误用了 `credit_points` 列名，收到 Unknown column；按仓库 schema 改为 `points` 后查询成功。该失败查询没有写入，未进入生产调用门禁。正式执行前再次完整重读基线。

规则指纹使用获批提交中原版 `production_fingerprint()`，仅导出目标规则版本及其 14 条规则计算；不输出学员、手机号等业务隐私数据。

```text
fingerprint before = f6de71877387d294a9ec43fc613294e008b2317f274837d3f6f5331c83554877
fingerprint after  = f6de71877387d294a9ec43fc613294e008b2317f274837d3f6f5331c83554877
before == after = true
course rule version = DRAFT
rules = 14
mapping = 0
active bindings = 19
generic frozen = 0/19
course frozen = 0/19
ledger entries = 0
ledger points = 0.00
settlement = false
RULE_RECONCILIATION = NOT_APPLIED
APPLY audit = 0
0064/0065/0066 = NOT_APPLIED
```

## 安全计数与停止点

```text
probe count = 0
CLS queries = 0
new revisions = 0
StartVersionInstance / StopVersionInstance = 0
OperateServerManage = 0
production database writes = 0
ledger writes = 0
APPLY_CALL_COUNT = 0
migration executions = 0
R2C-VERIFY = NOT_AUTHORIZED
R3 = NOT_AUTHORIZED
```

脱敏原始证据保存在本地 `.codex-tmp/r2f-capture-e988cd36/`：control_before.json、version_gates.json、database_before.json、capture.json、control_immediately_after.json、database_after.json、summary.json。没有生成 cleanup 调用证据，因为没有执行恢复写调用。

捕获、立即只读复核、数据库前后不变量及安全开关复核完成后停止。后续若继续，应围绕本次 RequestId 查明服务发布资格或资源状态拒绝的条件；本报告不授权任何再次路由、实例启停、重新创建或完整候选验证。
