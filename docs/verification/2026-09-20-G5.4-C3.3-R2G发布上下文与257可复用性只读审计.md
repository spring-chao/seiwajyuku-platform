# G5.4-C3.3-R2G 发布上下文与257可复用性只读审计

## 结论与范围

只读 API 审计完成；控制台表现未能观察，腾讯云后台解释尚未取得。ROOT_CAUSE = NOT_PROVEN。不得据此进入 R2C-VERIFY 或 R3。

R2F-CAPTURE 的 PASS 仅表示异常证据捕获成功。原始事实提交 `20b49ce67b87c352fae893b1a2a5e0de59d2c6b8` 已原样推送至 `codex/g5-c3-3-r2g-readonly-audit`，未重写。控制工具基线仍为 `b0a71c74cd44a5d1f85ff7e939a95db9e2122959`；本轮不修改发布器。

## 实时只读证据

采集批次时间：2026-09-20 14:50:35 UTC（北京时间22:50:35）。时间为批次标记，不是每个响应的独立服务端时间。完整脱敏响应位于 `fixtures/r2g-20260920/`。EnvParams 整体脱敏；请求ID、非敏感状态字段保留。采集器仅允许本节七个 Describe API。

| 接口 | 关键结果 |
| --- | --- |
| DescribeCloudRunServerDetail | BaseInfo.Status=normal；TrafficType=FLOW；UpdateTime=2026-09-20 07:14:00；OnlineVersionInfos 仅253、FlowRatio="100" |
| DescribeCloudRunServers | Total=1；目标服务Status=normal，TrafficType=FLOW；无更多分页 |
| DescribeVersionDetail(257) | Name正确；Status=normal；MinNum=1、MaxNum=5；CreatedTime=07:14:00，UpdatedTime=07:45:17 |
| DescribeCloudRunDeployRecord | 接口返回248–257共10条；保留全部返回记录；未提供分页参数或历史总数，不宣称覆盖服务自创建以来全部版本 |
| DescribeServerManageTask(2190170) | Status=stopped；ChangeType=CODE；ReleaseType=GRAY；DeployType=image；PreVersion=253、Version=257；ReleaseId=2680075；FailReason空 |
| DescribeReleaseOrder | Id=2680075；GrayStatus=success；ReleaseStatus=fail；IsReleasing=false；TrafficType=FLOW；TrafficTypeValues=[] |
| DescribeCloudRunPodList(257) | PodList=[]；TotalCount=0，无后续分页 |

管理任务 Steps：CreateVersion=finished（07:13:59–07:15:20）；RunPipeline=notInvolve；GrayRelease=stopped（开始07:15:23、结束时间空）；ReleaseVersion=notInvolve。没有返回“允许继续ReleaseGray”的字段。

发布单 CurrentVersion=253、ReleaseVersion=257，两者历史 FlowRatio=0、IsDefaultPriority=false、CurrentReplicas=0；UrlParam.Key/Value均为空。它是终态历史发布单，**不得将其中253的0误作当前服务流量**；当前流量以服务OnlineVersionInfos与部署记录交叉确认。

### 253–257部署记录

| DeployId | DeployTime | Status | FlowRatio | HasTraffic | ScaleStatus | IsReleasing |
| --- | --- | --- | --- | --- | --- | --- |
| 253 | 2026-09-19 09:57:22 | normal | 100 | true | 空 | false |
| 254 | 2026-09-19 18:22:04 | normal | 0 | false | zero | false |
| 255 | 2026-09-19 18:32:46 | normal | 0 | false | zero | false |
| 256 | 2026-09-19 19:21:32 | normal | 0 | false | zero | false |
| 257 | 2026-09-20 07:14:00 | normal | 0 | false | zero | false |

以上全部TrafficType=FLOW。257 BuildId=0、RunId=multi_tenant_1x84FreRxIFPig，镜像指向253镜像。接口未返回VersionName、ReleaseType、DeployType、TaskId、ReleaseId、FailReason等额外部署字段；不将缺失字段填作false。版本详情也未提供FlowRatio/HasTraffic/CurrentReplicas/可发布资格字段，相关判断分别来自部署记录和Pod接口。

257 VpcConf：VpcId=vpc-5l5vzkr2；VpcCIDR=172.17.0.0/16；SubnetId=subnet-1xw5yqjh；SubnetCIDR=172.17.0.0/20。与当前服务配置一致；本轮未另查253版本级VpcConf，不替代此前版本级比对。

## 官方语义与接口清单

[ReleaseGray文档](https://cloud.tencent.com/document/product/1243/75872)将其定义为灰度发布，ResourceUnavailable仅有资源不可用的通用说明；没有明确列出活动发布上下文前提，也没有声明任意normal历史版本可复用。RequestId可供后台定位。本轮不重发此前失败请求。

[更新服务文档](https://cloud.tencent.com/document/product/1243/77192)说明放弃灰度会使新版本下线。这是产品流程语义，不能直接证明257具体内部资格字段为何值。

[流量配置说明](https://cloud.tencent.com/document/product/1243/49178)的定向流程使用新部署版本与当前运行旧版本，并让旧版兜底。文档适用云开发中的云托管；并未定义取消后的历史版本是否还能参与。257是返回记录中的最新编号，不等于仍处于该流程认可的最新部署阶段。

[官方服务API清单](https://cloud.tencent.com/document/product/1243/75708)与本机TCBR SDK（3.1.155，API版本2022-02-17）清单已比对：可见ReleaseGray、更新服务、配置更新、回滚、实例启停等；未发现独立公开SetTraffic/UpdateTraffic接口。SDK清单见fixture（FMT为类常量而非API）。这不证明控制台没有其他内部路径；没有Inspector记录，无法确认控制台当前“流量配置”实际调用。未调用任何替代写API。

## 控制台与证据缺口

浏览器读取工具两次均报nodeRepl.fetch request failed，无可访问浏览器。未绕过浏览器访问会话、未点击任何生产按钮。以下全部NOT_OBSERVED：按钮是否可用、257是否可见/可选、URL参数是否可选257、下线提示、实际Inspector调用。旧截图不能替代本轮实时证据。即使可见也不等于后端允许发布。

没有可用腾讯云工单连接器；支持问题已整理于本报告下一节，**未提交工单、未取得后台回复**。

## 独立假设评级

| 假设 | 等级 | 证据和限制 |
| --- | --- | --- |
| H1 ReleaseGray要求活动发布/管理上下文 | LIKELY | 先前活动期可接受；关闭后task stopped/order终态，本次捕获拒绝。符合流程相关性，但没有官方直接前提说明或后台解释，非因果证明 |
| H2 放弃灰度使257退出可发布生命周期 | NOT_PROVEN | 官方下线语义支持调查方向；本轮仅见normal、zero、无流量、无Pod，没有eligibility/offline机器字段 |
| H3 服务存在隐藏发布状态不匹配 | NOT_PROVEN | 服务端明确拒绝且公开Status正常；具体隐藏状态未暴露，不能把消息当成字段证明 |
| H4 payload/API语义不匹配 | NOT_PROVEN | 错误非InvalidParameter，优先级降低但未排除；公开示例和字段说明不足以穷尽语义限制 |

PAYLOAD_PARAMETER_ROOT_CAUSE = LOW_CONFIDENCE。没有修改参数或再次调用来试错。

```text
SERVICE_RUNTIME_STATE = API_STATUS_NORMAL（本轮未发送HTTP健康探针）
SERVICE_PUBLISH_STATE = NOT_PROVEN
257_RUNTIME_STATE = API_STATUS_NORMAL / ZERO_RUNNING_PODS
VERSION_EXISTS = true
VERSION_RUNTIME_NORMAL = true（仅API Status含义，不表示进程已运行）
VERSION_HAS_TRAFFIC = false
VERSION_HAS_RUNNING_INSTANCE = false
257_RELEASE_ELIGIBILITY = NOT_PROVEN
VERSION_ELIGIBLE_FOR_RELEASE_GRAY = NOT_PROVEN
ACTIVE_RELEASE_CONTEXT = false（本次返回的发布单/关联任务）
RELEASEGRAY_REQUIRES_ACTIVE_CONTEXT = LIKELY
257_REUSABLE_WITHOUT_REDEPLOY = NOT_PROVEN
ROOT_CAUSE = NOT_PROVEN
```

## 腾讯云技术支持问题描述（待提交）

产品：CloudBase Run / TCBR。服务：seiwajyuku-platform-api。

Action：ReleaseGray；RequestId：d9f8e51f-734d-4906-a406-b630a4285357。

2026-09-20约22:31北京时间，唯一一次URL_PARAMS请求返回ResourceUnavailable，安全消息为“{seiwajyuku-platform-api}状态异常，无法发布”。253当前100%流量；257为此前创建的候选，版本Status=normal，部署记录FlowRatio=0、HasTraffic=false、ScaleStatus=zero，无Pod。此前执行放弃灰度，发布单2680075为ReleaseStatus=fail、IsReleasing=false；管理任务2190170=stopped。

请用RequestId确认：

1. 后台具体哪个资源、哪一个状态字段触发拒绝？当前值与允许值是什么？
2. ReleaseGray是否要求活动的灰度发布单/管理任务？已停止上下文能否再次使用？
3. 257经放弃灰度后，是否仍允许重新配置URL_PARAMS？normal状态与发布资格有何区别？
4. 若不允许，官方重新验证路径是什么，是否必然创建版本或新任务？请明确写操作影响，暂勿代执行任何修复。
5. 控制台流量配置对应哪个公开API？是否存在不重新部署即可恢复合法发布上下文的受支持路径？

不含凭据、Token或数据库连接信息；技术支持回复之前不执行新生产动作。

## 安全边界与停止点

```text
production route writes = 0
revision writes = 0
instance writes = 0
database writes = 0
APPLY = 0
HTTP probe_count = 0
CLS = NOT_RUN
R2C-VERIFY = NOT_AUTHORIZED
R3 = NOT_AUTHORIZED
```

本轮未查业务数据库，也未重复业务健康探针；因此不把此前fingerprint、rules14、ledger0、0064/65/66未应用等基线宣称为本轮重新验证的事实。本轮无数据库写调用，原R2F-CAPTURE前后核验事实保持在原报告中。

R2G在此停止。需后台解释才能进一步确定内部根因；新建candidate、重新部署257、启动实例或建立新发布单均需独立授权，不能通过反复ReleaseGray试错。
