# GATE-0-CLOSE 与 R3 只读预检报告

核验日期：2026-09-23，北京时间约21:40—21:44。范围：只读控制面、学分规则/引用/账本聚合查询、GitHub provenance与备份元数据。未执行生产写操作。

## 结论

```text
CANDIDATE_258_DB_CONNECTIVITY = PASS
GATE-0 = PASS
R3_DATA_PREFLIGHT = PASS
R3_EXECUTION_READY = NO
R3 = NOT_AUTHORIZED
```

258已是稳定版，不再沿用253/257候选路由假设。100%生产流量、匹配的在线build-info、数据库健康证据可以证明本次258连通性；不需要制造URL_PARAMS灰度，也不追溯修改旧257失败记录。

## 控制面实时证据

- 服务：seiwajyuku-platform-api；环境：shengheshu-d2g2zyyl99f6c6fc2。
- DescribeCloudRunServerDetail：Status=normal，TrafficType=FLOW；OnlineVersionInfos仅258，FlowRatio=100，UrlParam为空。旧版无普通在线流量。
- DescribeReleaseOrder：Id=2691926，IsReleasing=false，ReleaseStatus=success，TrafficType=FLOW，TrafficTypeValues为空，CurrentVersion及ReleaseVersion的UrlParam均为空。
- DescribeServerManageTask(TaskId=0)：Id=2201828，ReleaseId=2691926，Status=finished；无活动任务。
- 终态发布单保留的CurrentVersion=253与ReleaseVersion=258两条FlowRatio均为0。这是终态记录字段，不用它替代服务OnlineVersionInfos的当前100%流量证据。
- DescribeVersionDetail(258)：G5_4_PRODUCTION_RULE_APPLY_ENABLED=false，LEARNING_CREDIT_SETTLEMENT_ENABLED=false，RUN_BOOTSTRAP_ON_STARTUP=false。
- 同日上一轮已确认258实例Running、生产网关DB health连续20/20 HTTP200；本轮再次取得生产网关build-info与DB health HTTP200。

## provenance重新固定

```text
CONTROL_TOOL_COMMIT_PROPOSED = 478fe6ce40f2b51f54d8d9d2b38762cfeb346a66
RUNTIME_COMMIT = b3136c7f60506abeae90e74b841e9edd22fa6c90
RUNTIME_BUILD_ID = 35576488554
MAIN_CI_RUN = 35793315248
MAIN_CI = 9/9 success (including build-provenance)
SOURCE_TREE_SHA = edfb910b0266cd4dcb38818b9859149d9d5d2eb5
RELEASE_MANIFEST_SHA256 = 2cd3c5905e8c34400d93b71c216cb1af67ce3218c56ceaa2b9dd066c540da1a5
```

- 实时GitHub main查询得到上述478fe6ce完整SHA，CI headSha一致，9个job均success。
- 本轮重新下载release-provenance-35793315248（artifact 10722283892，未过期），存于工作区 `.codex-tmp/gate0-close-provenance-20260923/release-manifest.json`。
- build_provenance.py validate通过；manifest的release_commit、CI编号及source_tree_sha与Git对象核对一致。
- b3136c7到478fe6ce差异为7份文档、734新增、0删除，无业务/API/迁移变化。
- 本轮只读脚本加载现有g5-c3-3-r2h工作树；已比较其scripts/apps/migrations/data与478fe6ce完全一致。该工作树HEAD不是478fe6ce，未来真正执行时仍须使用获批SHA的干净checkout，不能把本次检查描述为已经执行main控制器。
- CONTROL_TOOL_COMMIT_PROPOSED是经本次核验后建议填写的值，尚待R3批准包确认，不构成生产控制器授权。runtime image_digest仍为unknown，不宣称完成运行时镜像摘要比对。

## 学分数据库实时基线

通过既有CloudBase CLI `db execute --read-only`，查询均为SELECT；未直接读取数据库连接密钥。

| 项目 | 本次结果 |
|---|---|
| 课程规则版本 | 唯一STANDARD_3Y_2026 / 2026.1，id=3，DRAFT |
| 课程规则 | 14 |
| canonical | 25，BUSINESS_CONFIRMED |
| exact | 7/25 |
| 只读差异计划 | 7 KEEP / 4 UPDATE / 14 ADD / 3 REMOVE_UNUSED_PLACEHOLDER |
| 3个placeholder引用 | mapping、学习会课程/场次、completion、ledger引用分别为0 |
| 通用规则 | 版本id=1，PUBLISHED，6条ACTIVE；结算模型、分值、月上限与固定基线一致 |
| mapping | 0 |
| active bindings | 19；generic frozen=0，course frozen=0 |
| ledger | 0 entries / 0.00 |
| APPLY audit | 0 |
| 0064/0065/0066 | schema_migrations匹配计数0，均NOT_APPLIED |
| settlement | 258环境开关false |

```text
production_fingerprint = f6de71877387d294a9ec43fc613294e008b2317f274837d3f6f5331c83554877
canonical_fingerprint = 1aac914783b78a4b377bcf3f0ff6ea3762387df0f3a8f39c66b878e1c0c84674
course_rule_version_id = 3
RULE_RECONCILIATION = NOT_APPLIED
APPLY_CALL_COUNT_THIS_PHASE = 0
production_database_writes_THIS_PHASE = 0
```

只读计划生成器最终返回READY_FOR_SEPARATE_APPROVAL。首轮临时检查脚本只传入引用合计、未传全部细分字段，生成器按设计BLOCK；补齐真实只读细分查询后通过。未改业务代码、未放宽门禁。

本次多次SELECT不构成单一事务快照；正式APPLY仍必须在同一事务内重读、加锁和校验，不允许沿用此次快照直接写入。

## R3批准包剩余项

- BACKUP_ID_FOR_R3=PENDING。只读查询确认历史备份10630858存在，snapshot/full、success、IsUsable=yes，完成于2026-09-19 19:03:31。它是旧R1备份，不能仅凭可用标记认定可恢复当前全部生产事实。本轮未创建新备份、未恢复备份；需确认本次适用的恢复点和恢复范围。
- actor_user_id=PENDING：未擅自选择管理员身份。
- APPROVAL_ID、APPROVAL_WINDOW、execution_reason：待独立授权固定。
- 当前G5 APPLY开关false：R3模板本身不授权修改环境变量或新发布。必须先明确受控执行环境及临时开关开启/关闭方案，并取得该动作独立授权；本轮没有打开开关。
- CONTROL_TOOL_COMMIT在正式批准包确认后，使用干净对应checkout；执行前再次检查provenance及全部实时不变量。

## 本轮变更与停止点

仅新增本地只读检查辅助脚本和本报告、下载CI证据；未提交/推送代码。没有ReleaseGray、OperateServerManage、revision/实例/网络写入、备份创建、APPLY、迁移、历史staging或ledger写入。

可以进入GATE-1的授权准备，但不能执行R3。下一次有写操作的任务必须明确覆盖备份适用性、执行身份、临时APPLY门禁与唯一APPLY动作；0064及以后仍独立授权。
