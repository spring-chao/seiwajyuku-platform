# G5.4-C3.3-R3 正式执行批准包（未授权草案）

任务：R3-AUTH-PREP。2026-09-24开始，2026-09-25继续只读核验。文件名沿用用户指定日期。

```text
R3_READY_FOR_AUTHORIZATION = NO
BACKUP_READY = false
GATE-0 = PASS
R3 = NOT_AUTHORIZED
CONTROL_TOOL_COMMIT = NOT_AUTHORIZED
```

## 1. 批准字段

| 字段 | 值 |
|---|---|
| APPROVAL_ID | NOT_ISSUED；建议未来R3授权编号单独签发，当前只有AUTH-PREP任务 |
| APPROVAL_WINDOW | NOT_AUTHORIZED；不存在有效生产写入窗口 |
| PROPOSED_CONTROL_TOOL_COMMIT | 478fe6ce40f2b51f54d8d9d2b38762cfeb346a66 |
| RUNTIME_COMMIT / expected_release_commit | b3136c7f60506abeae90e74b841e9edd22fa6c90 |
| MAIN_CI_RUN | 35793315248，9/9 success，含build-provenance |
| RELEASE_MANIFEST_SHA256 | 2cd3c5905e8c34400d93b71c216cb1af67ce3218c56ceaa2b9dd066c540da1a5 |
| BACKUP_ID | BACKUP_NOT_READY；不得以10630858替代新鲜备份 |
| proposed actor_user_id | 1（尚未签发该身份的执行授权） |
| course_rule_version_id | 3 |
| production_fingerprint | f6de71877387d294a9ec43fc613294e008b2317f274837d3f6f5331c83554877 |
| canonical_fingerprint | 1aac914783b78a4b377bcf3f0ff6ea3762387df0f3a8f39c66b878e1c0c84674 |

未签发项显式写为NOT_AUTHORIZED/NOT_ISSUED，而不是编造批准号或日期。

## 2. 实时状态及来源

- 9月25日只读API核验：服务normal，FLOW，OnlineVersionInfos仅258=100%；无URL参数。发布单2691926 IsReleasing=false / success，TrafficTypeValues为空；任务2201828 finished。
- DescribeVersionDetail(258)得到完整六门禁：

| 门禁 | 当前258 | 未来R3执行瞬间 |
|---|---|---|
| APP_ENV | production | production |
| DEPLOYMENT_READ_ONLY | false | false |
| ALLOW_PRODUCTION_MUTATIONS | true | true |
| G5_4_PRODUCTION_RULE_APPLY_ENABLED | false | true，仅获批窗口 |
| LEARNING_CREDIT_SETTLEMENT_ENABLED | false | false |
| RUN_BOOTSTRAP_ON_STARTUP | false | false |

敏感变量DATABASE_URL、FIELD_ENCRYPTION_KEY、JWT_SECRET及各集成密钥只确认PRESENT；未输出或保存值。

VpcId=vpc-5l5vzkr2，VpcCIDR=172.17.0.0/16，SubnetId=subnet-1xw5yqjh，SubnetCIDR=172.17.0.0/20。未来完整继承并逐字段比对；DATABASE_URL仅在内存中验证语义一致，报告只能记比较结果。

## 3. provenance与干净控制器

机器核对b3136c7到478fe6ce：仅R2H的7份docs文件，734新增、0删除，无apps/scripts/migrations/data变化。

新建独立detached工作树 `.codex-worktrees/r3-auth-prep-478fe6`，在新增本报告前执行build_provenance.py verify：目标478fe6ce，工作树clean，source_tree_sha=edfb910b0266cd4dcb38818b9859149d9d5d2eb5。新增的批准包及GATE-0证据属于docs-only交付，不使新文档提交自动成为生产控制器。

重新从GitHub下载CI 35793315248的release-provenance artifact，并validate及SHA256核验。选择478fe6ce是因为它是明确审核、CI通过且与运行业务实现一致的固定提交，不是因为它恰好叫main。将来真正执行时必须再次使用干净478fe6ce checkout或另行审批SHA。

## 4. 入口、身份与代码门禁

推荐既有HTTP入口：POST `/api/v1/ops/production-actions/g5-4-course-rule-reconciliation/apply`。不使用通用SQL、RunSql、手工UPDATE/INSERT。

审计依据：production_actions.py的G54CourseRuleApplyPayload、require_permission及system_admin检查；production_operations.py的_verify_runtime_gates、_verify_actor及事务执行；settings.py的环境解析。

- HTTP require_permission先检查plans:production_rule_reconciliation_apply，再检查system_admin。服务事务内再次确认账户和角色启用。
- 代码允许隔离test环境，但本批准包仅允许production；不能借test绕过生产门禁。
- expected_release_commit必须匹配真实承载请求的runtime，不填控制器SHA。
- B入口 `scripts/apply_g5_4_course_rule_reconciliation.py`存在并调用同一服务，但CLI以actor ID入参，不经过HTTP认证依赖。本轮不选择它，也不把CLI存在当作已证明的官方一次性执行通道。
- 实时查询只有一个system_admin：user_id=1，账户is_active=1，角色is_active=1，角色valid_from/valid_until均null。scope=ALL，org_unit_id=null，scope有效期均null。
- iam.py user_context明确给有效system_admin并集加入本操作权限，不依赖0064后续迁移或新增role_permissions写入。因此ACTOR_ACTIVE=true、SYSTEM_ADMIN=true、REQUIRED_PERMISSION=true（数据库状态+代码计算依据）。未借此创建session或获取token；实际HTTP身份认证仍在未来执行前核验。

## 5. 数据基线与逻辑快照

9月25日重新读取并计算，未照抄旧指纹：课程版本STANDARD_3Y_2026/2026.1，id=3，DRAFT，14条；canonical25 BUSINESS_CONFIRMED；7 KEEP / 4 UPDATE / 14 ADD / 3 REMOVE_UNUSED_PLACEHOLDER，21项业务变更。三placeholder的各类引用均0。通用规则唯一版本id=1 PUBLISHED，6条ACTIVE，分值、cap、模型符合固定常量。

mapping=0；bindings=19、generic/course frozen均0；ledger=0/0.00；APPLY audit=0；0064/0065/0066匹配迁移计数0。只读规划器READY_FOR_SEPARATE_APPROVAL不代表获批或允许写入。

逻辑快照本地位置：`.codex-tmp/r3-auth-prep-20260924/logical-snapshot.json`。

SHA256：5929027585fd5e49e5be37aab284c688c9e578fdd35ee46049c0acd49b94ed73。

包含课程版本1行、规则14行、限定R3审计0行、限定mapping0行、限定bindings19行、ledger计数/总分聚合1行（不是1条ledger）、0064—0066迁移记录0行。只导出非敏感字段，未导出人员档案、账户凭据或全库数据。该快照只用于验收、差异定位及前向修复设计，非恢复脚本，且多次只读SELECT不是原子事务备份。正式APPLY必须事务内重新读取并校验。

## 6. 备份硬停止条件

只读执行 `db backup list --start-time '2026-09-23 21:44:00' --limit 100`：data=[]、total=0。该起点是GATE-0基线确认之后；没有符合时点条件的备份。更不能满足本轮9月25日新快照之后的备份要求。

此前实测10630858：cluster/full snapshot、success、IsUsable=yes，完成于9月19日19:03:31。其旧时点不适用本次R3。恢复旧全库快照可能覆盖随后正常签到、会员和运营事实；不能因学分指纹没变就认定整库没变。可用标记也不等于本轮恢复演练已通过。

BACKUP_READY=false。按授权文本在此停止生产核验；没有创建、删除或恢复备份。下一步仅申请R3-BACKUP授权：重新只读基线后创建一次当前生产cluster/full备份，确认success、IsUsable、时点、范围及恢复路径，再刷新逻辑快照对应关系。失败不自动重复创建，不包含恢复或B/C/D授权。

## 7. 临时运行包络方案比较（设计，禁止执行）

官方依据：[腾讯云更新服务](https://cloud.tencent.com/document/product/1243/77192)、[CloudBase灰度发布](https://docs.cloudbase.net/run/deploy/deploy/gray-release)。配置保存会部署并可能全量切换；手工灰度支持按规则验证，结束时必须完成或取消。

1. **推荐新revision复用258相同不可变镜像**：保留258及原false配置作为明确回退对象；业务配置只变G5 APPLY为true。新revision需验证镜像摘要、runtime、VPC、六门禁，再进行获批路由和身份核验。风险是新增发布上下文和后续关闭动作，必须分别有额度与失败处置。
2. 原258编辑并重部署：不推荐。配置修改不是内存开关，会部署；不能同时把258当作不变回退基线。不得假定部署ID仍258或无流量影响。
3. 一次性脚本/进程：仓库有受控CLI，但尚无本轮核实的官方无漂移执行通道；不得用Webshell注入环境、通用远程命令或自行拼SQL替代。

推荐设计的ENABLE/DISABLE采用两份可审计配置：一个true临时revision和一个false关闭revision，均复用已核验的同一镜像。先在各自发布上下文检查就绪与路由，再按独立授权切换。不要把“恢复258流量”单独等同于APPLY已关闭：还须确认true版本不再可接收请求且无运行实例、无活动路由/发布单；若须额外stop/delete或其他控制面动作，必须预先列明并独立获批，禁止猜OperateType。

此处是推荐架构，不是可运行发布命令。精确镜像digest、部署/收尾请求及最大底层API写调用数尚未完成验证；备份硬停止后本轮不继续探索。未证明可控关闭之前不得把包改为YES。

## 8. HTTP业务payload草案

```json
{
  "expected_release_commit": "b3136c7f60506abeae90e74b841e9edd22fa6c90",
  "expected_production_fingerprint": "f6de71877387d294a9ec43fc613294e008b2317f274837d3f6f5331c83554877",
  "expected_canonical_fingerprint": "1aac914783b78a4b377bcf3f0ff6ea3762387df0f3a8f39c66b878e1c0c84674",
  "expected_course_rule_version_id": 3,
  "expected_course_rule_status": "DRAFT",
  "expected_rule_count": 14,
  "expected_placeholder_keys": ["AUTO-QR-EXCELLENT-IMPROVEMENT", "AUTO-QR-HAPPINESS-CARE", "AUTO-QR-IMPROVEMENT-INNOVATION"],
  "execution_reason": "G5.4-C3.3-R3：仅按独立批准执行2026课程规则原子收口，不执行映射、冻结、迁移或入账"
}
```

actor由经认证HTTP身份产生，不能在payload伪造actor_user_id。该草案只供审核，未来提交前必须刷新所有实时条件；缺少批准号/窗口或任一不符均不发送。

## 9. 未来授权拆分与硬检查点

- A：新鲜备份。必须先单独完成、验收，未通过不能进入B。
- B：ENABLE。固定exact image、代码、VPC、配置差异及控制面API额度，检查六门禁、健康、请求落点后才能进入C。
- C：最多一次HTTP原子APPLY；不得因超时、断连或响应不明自动重试。先只读审计/结果核验。期望7保留、4更新、14新增、3删除；25条canonical exact、版本仍DRAFT；mapping/bindings/ledger/settlement及后续迁移不变。
- D：DISABLE。成功或失败均需执行获批关闭路径，终态G5=false、settlement=false、bootstrap=false；验证业务版本健康、唯一普通流量、无true版本可服务、无活动发布任务。
- B/C/D可以在未来同一明确批准窗口签发，但必须分别逐项授权、给出可执行参数和最大次数，保留B→C、C→D及D最终验收硬检查点。失败恢复所需控制面动作也要预先列明，不授予无限重试或隐含全库恢复。
- 当前最大生产写次数=0；未来建议上限：A创建备份1次、B创建true revision1个、C APPLY1次、D创建false revision1个。路由/关闭/实例动作的精确次数尚未固定，故本包不可执行，不能用上述对象数冒充底层API调用预算。

## 10. 保存与停止

9月23日GATE-0关闭报告已原样保留到本工作树docs/verification，与本文件一起作docs-only保护性收口。未修改业务实现。辅助脚本只增加SELECT结果空集的严格识别；CLI对空集返回rowsAffected=0，未放宽任何生产业务门禁。

最终：R3_READY_FOR_AUTHORIZATION=NO；R3=NOT_AUTHORIZED；本轮APPLY_CALL_COUNT=0；0064/0065/0066=NOT_APPLIED；ledger=0/0.00；G5 APPLY=false。不进入GATE-2，不执行A/B/C/D。
